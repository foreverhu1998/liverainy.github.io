<?xml version="1.0" encoding="utf-8"?>
<search> 
  
  
    
    <entry>
      <title>Java基础与并发面试考题</title>
      <link href="/2026/06/08/Java%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%B9%B6%E5%8F%91%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/Java%E5%9F%BA%E7%A1%80%E4%B8%8E%E5%B9%B6%E5%8F%91%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="01-Java基础与并发-·-高频面试考题背诵指南"><a href="#01-Java基础与并发-·-高频面试考题背诵指南" class="headerlink" title="01 - Java基础与并发 · 高频面试考题背诵指南"></a>01 - Java基础与并发 · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1arrayList-%E5%92%8C-linkedList-%E7%9A%84%E5%8C%BA%E5%88%AB%E5%88%86%E5%88%AB%E9%80%82%E7%94%A8%E4%BB%80%E4%B9%88%E5%9C%BA%E6%99%AF">Q1：ArrayList 和 LinkedList 的区别？分别适用什么场景？</a></li><li><a href="#q2hashMap-%E5%BA%95%E5%B1%82%E6%95%B0%E6%8D%AE%E7%BB%93%E6%9E%84%E6%98%AF%E4%BB%80%E4%B9%88jdk8-%E5%81%9A%E4%BA%86%E5%93%AA%E4%BA%9B%E4%BC%98%E5%8C%96">Q2：HashMap 底层数据结构是什么？JDK8 做了哪些优化？</a></li><li><a href="#q3hashMap-%E7%9A%84%E6%89%A9%E5%AE%B9%E6%9C%BA%E5%88%B6%E4%B8%BA%E4%BB%80%E4%B9%88%E5%AE%B9%E9%87%8F%E5%BF%85%E9%A1%BB%E6%98%AF-2-%E7%9A%84%E5%B9%82%E6%AC%A1%E6%96%B9">Q3：HashMap 的扩容机制？为什么容量必须是 2 的幂次方？</a></li><li><a href="#q4hashMap-%E4%B8%BA%E4%BD%95%E7%BA%BF%E7%A8%8B%E4%B8%8D%E5%AE%89%E5%85%A8jdk7-%E7%9A%84%E6%AD%BB%E9%93%BE%E9%97%AE%E9%A2%98%E5%A6%82%E4%BD%95%E5%8F%91%E7%94%9F%E7%9A%84">Q4：HashMap 为何线程不安全？JDK7 的死链问题如何发生的？</a></li><li><a href="#q5concurrenthashmap-%E5%9C%A8-jdk7-%E5%92%8C-jdk8-%E4%B8%AD%E7%9A%84%E5%AE%9E%E7%8E%B0%E6%9C%89%E4%BD%95%E4%B8%8D%E5%90%8C">Q5：ConcurrentHashMap 在 JDK7 和 JDK8 中的实现有何不同？</a></li><li><a href="#q6hashset-%E7%9A%84%E5%BA%95%E5%B1%82%E5%8E%9F%E7%90%86%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E5%85%83%E7%B4%A0%E4%B8%8D%E9%87%8D%E5%A4%8D">Q6：HashSet 的底层原理？如何保证元素不重复？</a></li><li><a href="#q7volatile-%E5%85%B3%E9%94%AE%E5%AD%97%E7%9A%84%E4%BD%9C%E7%94%A8%E8%83%BD%E4%BF%9D%E8%AF%81%E5%8E%9F%E5%AD%90%E6%80%A7%E5%90%97">Q7：volatile 关键字的作用？能保证原子性吗？</a></li><li><a href="#q8synchronized-%E7%9A%84%E5%AE%9E%E7%8E%B0%E5%8E%9F%E7%90%86%E5%AF%B9%E8%B1%A1%E9%94%81%E5%92%8C%E7%B1%BB%E9%94%81%E7%9A%84%E5%8C%BA%E5%88%AB">Q8：synchronized 的实现原理？对象锁和类锁的区别？</a></li><li><a href="#q9%E9%94%81%E5%8D%87%E7%BA%A7%E7%9A%84%E8%BF%87%E7%A8%8B%E6%97%A0%E9%94%81%E5%81%8F%E5%90%91%E9%94%81%E8%BD%BB%E9%87%8F%E7%BA%A7%E9%94%81%E9%87%8D%E9%87%8F%E7%BA%A7%E9%94%81">Q9：锁升级的过程（无锁→偏向锁→轻量级锁→重量级锁）？</a></li><li><a href="#q10synchronized-%E5%92%8C-reentrantlock-%E6%9C%89%E4%BB%80%E4%B9%88%E5%8C%BA%E5%88%AB">Q10：synchronized 和 ReentrantLock 有什么区别？</a></li><li><a href="#q11java-%E5%86%85%E5%AD%98%E6%A8%A1%E5%9E%8Bjmm%E6%98%AF%E4%BB%80%E4%B9%88happens-before-%E5%8E%9F%E5%88%99%E6%9C%89%E5%93%AA%E4%BA%9B">Q11：Java 内存模型（JMM）是什么？happens-before 原则有哪些？</a></li><li><a href="#q12%E4%BB%80%E4%B9%88%E6%98%AF-aqs%E5%AE%83%E7%9A%84%E6%A0%B8%E5%BF%83%E5%8E%9F%E7%90%86%E6%98%AF%E4%BB%80%E4%B9%88">Q12：什么是 AQS？它的核心原理是什么？</a></li><li><a href="#q13countdownlatchcyclicbarriersemaphore-%E7%9A%84%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF">Q13：CountDownLatch、CyclicBarrier、Semaphore 的使用场景？</a></li><li><a href="#q14%E6%AD%BB%E9%94%81%E7%9A%84%E5%9B%9B%E4%B8%AA%E5%BF%85%E8%A6%81%E6%9D%A1%E4%BB%B6%E5%A6%82%E4%BD%95%E7%94%A8-jstack-%E6%8E%92%E6%9F%A5%E7%BA%BF%E4%B8%8A%E6%AD%BB%E9%94%81">Q14：死锁的四个必要条件？如何用 jstack 排查线上死锁？</a></li><li><a href="#q15%E7%BA%BF%E7%A8%8B%E7%9A%84-6-%E7%A7%8D%E7%8A%B6%E6%80%81%E5%8F%8A%E8%BD%AC%E6%8D%A2">Q15：线程的 6 种状态及转换</a></li><li><a href="#q16threadpoolexecutor-%E7%9A%84%E4%B8%83%E5%A4%A7%E6%A0%B8%E5%BF%83%E5%8F%82%E6%95%B0%E8%AF%A6%E8%A7%A3">Q16：ThreadPoolExecutor 的七大核心参数详解</a></li><li><a href="#q17%E5%9B%9B%E7%A7%8D%E6%8B%92%E7%BB%9D%E7%AD%96%E7%95%A5%E7%9A%84%E8%A1%8C%E4%B8%BA%E5%92%8C%E9%80%82%E7%94%A8%E5%9C%BA%E6%99%AF">Q17：四种拒绝策略的行为和适用场景</a></li><li><a href="#q18completablefuture-%E5%BC%82%E6%AD%A5%E7%BC%96%E6%8E%92%E5%9C%A8-srm-%E9%A1%B9%E7%9B%AE%E4%B8%AD%E7%9A%84%E5%AE%9E%E6%88%98%E5%BA%94%E7%94%A8">Q18：CompletableFuture 异步编排在 SRM 项目中的实战应用</a></li><li><a href="#q19threadlocal-%E5%86%85%E5%AD%98%E6%B3%84%E6%BC%8F%E5%8E%9F%E5%9B%A0%E5%8F%8A%E5%A6%82%E4%BD%95%E9%81%BF%E5%85%8D">Q19：ThreadLocal 内存泄漏原因及如何避免</a></li><li><a href="#q20%E5%B9%B6%E5%8F%91%E7%BA%BF%E7%A8%8B%E6%B1%A0%E7%9A%84%E5%AE%9E%E9%99%85%E5%8F%82%E6%95%B0%E9%85%8D%E7%BD%AE%E7%BB%8F%E9%AA%8C%E4%B8%8E%E7%9B%91%E6%8E%A7%E5%AE%9E%E6%88%98">Q20：并发线程池的实际参数配置经验与监控实战</a></li></ul><hr><span id="more"></span><h2 id="一、集合框架"><a href="#一、集合框架" class="headerlink" title="一、集合框架"></a>一、集合框架</h2><h3 id="Q1：ArrayList-和-LinkedList-的区别？分别适用什么场景？"><a href="#Q1：ArrayList-和-LinkedList-的区别？分别适用什么场景？" class="headerlink" title="Q1：ArrayList 和 LinkedList 的区别？分别适用什么场景？"></a>Q1：ArrayList 和 LinkedList 的区别？分别适用什么场景？</h3><p><strong>核心考点</strong>：底层数据结构、时间复杂度、内存占用、迭代性能。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>ArrayList</code> 底层是<strong>动态数组</strong>，支持随机访问，通过下标读取元素的时间复杂度是 O(1)；但在中间插入或删除元素时，需要移动后续所有元素，时间复杂度是 O(n)。默认初始容量是 10，每次扩容为原来的 1.5 倍。</p><p><code>LinkedList</code> 底层是<strong>双向链表</strong>，在头尾插入或删除元素时时间复杂度是 O(1)，但随机访问需要从头遍历，时间复杂度是 O(n)，同时每个节点需要额外存储前后指针，内存占用更大。</p><p><strong>使用场景</strong>：如果业务以<strong>查询为主</strong>（比如我们 SRM 系统中的报价列表展示），优先选 ArrayList；如果业务频繁在<strong>首尾进行插入删除</strong>（比如用作队列），可以考虑 LinkedList。实际上由于 CPU 缓存行对连续内存更友好，ArrayList 的迭代性能通常也优于 LinkedList，所以生产中 ArrayList 使用频率更高。</p></blockquote><hr><h3 id="Q2：HashMap-底层数据结构是什么？JDK8-做了哪些优化？"><a href="#Q2：HashMap-底层数据结构是什么？JDK8-做了哪些优化？" class="headerlink" title="Q2：HashMap 底层数据结构是什么？JDK8 做了哪些优化？"></a>Q2：HashMap 底层数据结构是什么？JDK8 做了哪些优化？</h3><p><strong>核心考点</strong>：数组+链表→数组+链表+红黑树，扩容时尾插法，树化阈值。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>HashMap</code> 的底层数据结构在 JDK7 是<strong>数组 + 链表</strong>，在 JDK8 优化为<strong>数组 + 链表 + 红黑树</strong>。</p><p><strong>JDK8 的核心优化</strong>主要有两点：</p><ol><li><strong>链表转红黑树</strong>：当链表长度超过 8 且数组长度大于等于 64 时，链表会自动转换为红黑树，将查询复杂度从 O(n) 降低到 O(logN)，解决了 hash 冲突严重时查询退化的问题。当红黑树节点减少到 6 时，会退化回链表。</li><li><strong>扩容时改用尾插法</strong>：JDK7 扩容使用头插法，在多线程环境下会导致链表成环（死链），JDK8 改为尾插法并重新计算索引位置（利用 <code>(e.hash &amp; oldCap) == 0</code> 判断），避免了这个问题。虽然 JDK8 的 HashMap 依然线程不安全，但至少不会出现死循环导致 CPU 100% 的问题。</li></ol></blockquote><hr><h3 id="Q3：HashMap-的扩容机制？为什么容量必须是-2-的幂次方？"><a href="#Q3：HashMap-的扩容机制？为什么容量必须是-2-的幂次方？" class="headerlink" title="Q3：HashMap 的扩容机制？为什么容量必须是 2 的幂次方？"></a>Q3：HashMap 的扩容机制？为什么容量必须是 2 的幂次方？</h3><p><strong>核心考点</strong>：负载因子 0.75、容量翻倍、位运算取模。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>HashMap</code> 默认初始容量是 <strong>16</strong>，负载因子是 <strong>0.75</strong>。当元素个数超过 <code>容量 × 0.75</code>（即 12）时，触发扩容，新容量变为原来的 <strong>2 倍</strong>。扩容时需要对所有旧元素重新 rehash 分配位置，是一个较重的操作，所以在初始化时如果能预估元素数量，应该手动指定初始容量，避免频繁扩容。</p><p><strong>容量必须是 2 的幂次方</strong>，原因在于 HashMap 通过 <code>(n - 1) &amp; hash</code> 来计算数组下标。当 <code>n</code> 是 2 的幂次方时，<code>n - 1</code> 的二进制全是 1（如 16-1&#x3D;15 即 <code>1111</code>），此时 <code>&amp;</code> 运算等价于取模 <code>% n</code>，但位运算比取模运算快得多。同时，这也保证了 hash 值能均匀分布在各个桶中，减少碰撞。</p></blockquote><hr><h3 id="Q4：HashMap-为何线程不安全？JDK7-的死链问题如何发生的？"><a href="#Q4：HashMap-为何线程不安全？JDK7-的死链问题如何发生的？" class="headerlink" title="Q4：HashMap 为何线程不安全？JDK7 的死链问题如何发生的？"></a>Q4：HashMap 为何线程不安全？JDK7 的死链问题如何发生的？</h3><p><strong>核心考点</strong>：多线程同时 put 触发扩容，头插法导致链表环形结构。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>HashMap</code> 线程不安全主要体现在以下几个场景：</p><ol><li><strong>数据丢失</strong>：两个线程同时 <code>put</code>，如果 hash 冲突，可能互相覆盖对方的值。</li><li><strong>JDK7 的死链（CPU 100%）</strong>：在 JDK7 中，扩容时使用头插法。当线程 A 和线程 B 同时触发扩容时，两个线程会各自维护节点的 <code>next</code> 指针，由于操作顺序交错，可能导致节点 A 的 next 指向节点 B，同时节点 B 的 next 又指向节点 A，形成<strong>环形链表</strong>。之后一旦有线程执行 <code>get</code> 操作遍历这个链表，就会陷入死循环，导致 CPU 飙到 100%。这是 JDK7 HashMap 最严重的 Bug。</li></ol><p>多线程场景下，应该使用 <code>ConcurrentHashMap</code> 来代替 <code>HashMap</code>。</p></blockquote><hr><h3 id="Q5：ConcurrentHashMap-在-JDK7-和-JDK8-中的实现有何不同？"><a href="#Q5：ConcurrentHashMap-在-JDK7-和-JDK8-中的实现有何不同？" class="headerlink" title="Q5：ConcurrentHashMap 在 JDK7 和 JDK8 中的实现有何不同？"></a>Q5：ConcurrentHashMap 在 JDK7 和 JDK8 中的实现有何不同？</h3><p><strong>核心考点</strong>：JDK7 分段锁 Segment，JDK8 CAS + synchronized。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>JDK7 的实现</strong>：采用**分段锁（Segment）**机制。整个 HashMap 被划分为若干个 Segment（默认 16 个），每个 Segment 内部是一个小 HashMap，每个 Segment 持有一把 ReentrantLock 锁。并发写操作时，只需锁住对应的 Segment，不同 Segment 可以并行操作，理论上支持 16 个线程同时写。</p><p><strong>JDK8 的实现</strong>：放弃了 Segment，改为<strong>数组 + 链表 + 红黑树</strong>，并发控制使用<strong>CAS + synchronized</strong>。</p><ul><li>当数组的目标槽位为空时，使用 <strong>CAS 无锁插入</strong>，完全不需要加锁。</li><li>当槽位不为空（发生 hash 冲突）时，使用 <strong>synchronized 锁住链表&#x2F;红黑树的头节点</strong>，只锁住当前桶的头节点而不是整个 Segment，锁的粒度更细，并发度提升显著。</li><li>JDK8 还引入了 <code>sizeCtl</code> 和 <code>CounterCell</code> 数组来高效统计元素数量，避免了 size() 操作时的全局加锁。</li></ul></blockquote><hr><h3 id="Q6：HashSet-的底层原理？如何保证元素不重复？"><a href="#Q6：HashSet-的底层原理？如何保证元素不重复？" class="headerlink" title="Q6：HashSet 的底层原理？如何保证元素不重复？"></a>Q6：HashSet 的底层原理？如何保证元素不重复？</h3><p><strong>核心考点</strong>：HashSet 内部持有一个 HashMap，以元素为 Key，固定 Object 为 Value。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>HashSet</code> 的底层实际上就是一个 <strong><code>HashMap</code></strong>。每次调用 <code>add(element)</code> 时，本质上是把 <code>element</code> 作为 <code>Key</code>，把一个固定的空 Object（<code>PRESENT</code>）作为 <code>Value</code>，调用 <code>HashMap.put()</code> 方法。</p><p>保证元素不重复的逻辑完全依赖 <code>HashMap</code> 的 Key 唯一性，而 Key 唯一性由 <code>hashCode()</code> 和 <code>equals()</code> 共同保证：先比较 hashCode，相同再调 equals，两者都相同则认为是同一个 Key，新值覆盖旧值（在 HashSet 中就体现为添加失败不重复）。</p><p><strong>这就是为什么要重写 equals() 时必须同时重写 hashCode()</strong>：否则两个”相等”的对象 hashCode 不同，会被存储在不同的桶中，HashSet 就失去了去重能力。</p></blockquote><hr><h2 id="二、并发基础"><a href="#二、并发基础" class="headerlink" title="二、并发基础"></a>二、并发基础</h2><h3 id="Q7：volatile-关键字的作用？能保证原子性吗？"><a href="#Q7：volatile-关键字的作用？能保证原子性吗？" class="headerlink" title="Q7：volatile 关键字的作用？能保证原子性吗？"></a>Q7：volatile 关键字的作用？能保证原子性吗？</h3><p><strong>核心考点</strong>：可见性、有序性（禁止指令重排），不能保证原子性。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>volatile</code> 主要有两个作用：</p><ol><li><strong>保证可见性</strong>：当一个线程修改了 volatile 变量的值，这个修改会立即被刷新回主内存，其他线程读取该变量时会强制从主内存重新加载，而不是使用 CPU 缓存中的旧值。这依赖于 CPU 的 <strong>MESI 缓存一致性协议</strong>和内存屏障（Memory Barrier）实现。</li><li><strong>禁止指令重排（保证有序性）</strong>：volatile 变量的写操作前会插入 <code>StoreStore</code> 屏障，写之后插入 <code>StoreLoad</code> 屏障；读操作前插入 <code>LoadLoad</code> 屏障，读之后插入 <code>LoadStore</code> 屏障。双重检查锁（DCL）单例模式中的 <code>instance</code> 变量必须加 volatile，就是为了禁止”分配内存→返回引用→初始化对象”三步的指令重排，防止其他线程拿到未初始化的对象引用。</li></ol><p><strong>volatile 不能保证原子性</strong>。<code>i++</code> 这个操作在底层是三步：读取 i → 计算 i+1 → 写回 i，volatile 只能保证每次读到的是最新值，但三步之间仍然可能被其他线程打断，所以 <code>i++</code> 在多线程下依然不安全，需要用 <code>AtomicInteger</code> 或 <code>synchronized</code>。</p></blockquote><hr><h3 id="Q8：synchronized-的实现原理？对象锁和类锁的区别？"><a href="#Q8：synchronized-的实现原理？对象锁和类锁的区别？" class="headerlink" title="Q8：synchronized 的实现原理？对象锁和类锁的区别？"></a>Q8：synchronized 的实现原理？对象锁和类锁的区别？</h3><p><strong>核心考点</strong>：Monitor 对象（monitorenter&#x2F;monitorexit 字节码），对象头 Mark Word 存储锁状态。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>synchronized</code> 的底层依赖 JVM 的 <strong>Monitor（监视器锁）</strong> 机制。在字节码层面，对同步代码块加锁会生成 <code>monitorenter</code> 和 <code>monitorexit</code> 两条指令；对同步方法则是在方法的访问标志上加 <code>ACC_SYNCHRONIZED</code> 标志。</p><p>每个 Java 对象都有一个关联的 Monitor，Monitor 中记录了锁的持有者线程、等待队列等信息。对象头中的 <strong>Mark Word</strong>（64位 JVM 下占 8 字节）用来存储锁状态，包括锁标志位（无锁&#x2F;偏向&#x2F;轻量级&#x2F;重量级）、锁对象的 hashCode、GC 年龄等信息。</p><ul><li><strong>对象锁（实例锁）</strong>：<code>synchronized(this)</code> 或 <code>synchronized</code> 修饰非静态方法，锁的是当前实例对象，不同实例的锁互不影响。</li><li><strong>类锁</strong>：<code>synchronized(ClassName.class)</code> 或 <code>synchronized</code> 修饰静态方法，锁的是 Class 对象，全局只有一个，所有实例共享这把锁。</li></ul></blockquote><hr><h3 id="Q9：锁升级的过程（无锁→偏向锁→轻量级锁→重量级锁）？"><a href="#Q9：锁升级的过程（无锁→偏向锁→轻量级锁→重量级锁）？" class="headerlink" title="Q9：锁升级的过程（无锁→偏向锁→轻量级锁→重量级锁）？"></a>Q9：锁升级的过程（无锁→偏向锁→轻量级锁→重量级锁）？</h3><p><strong>核心考点</strong>：锁升级的触发条件，各阶段的 Mark Word 存储内容，自旋的意义。</p><p><strong>背诵话术</strong>：</p><blockquote><p>JDK6 对 <code>synchronized</code> 进行了大幅优化，引入了锁升级机制，从低到高分为四个级别：</p><ol><li><p><strong>无锁</strong>：对象刚创建时，Mark Word 记录 hashCode 和 GC 年龄，锁标志位为 <code>01</code>。</p></li><li><p><strong>偏向锁</strong>：当第一个线程访问同步代码时，JVM 会将 Mark Word 中记录该线程的 ID，之后这个线程再次进入同步块时<strong>无需任何同步操作</strong>，直接检查 Mark Word 中的线程 ID 是否是自己。适用于”只有一个线程访问同步块”的场景，开销极低。</p></li><li><p><strong>轻量级锁</strong>：当有第二个线程来竞争锁时，偏向锁撤销，升级为轻量级锁。线程通过 <strong>CAS（Compare And Swap）</strong> 操作尝试将 Mark Word 替换为指向自己栈帧中 Lock Record 的指针。如果 CAS 成功则获得锁；失败则进行<strong>自旋等待</strong>（忙等循环）。适用于”竞争不激烈、持锁时间短”的场景。</p></li><li><p><strong>重量级锁</strong>：当自旋超过一定次数（JDK8 默认自适应自旋）仍未获得锁，升级为重量级锁。此时线程会进入 <strong>OS 层面的阻塞（调用 pthread_mutex_lock）</strong>，并被挂起，等待被唤醒。重量级锁会涉及用户态和内核态的切换，开销较大。</p></li></ol><p><strong>注意</strong>：锁只能升级不能降级（偏向锁特殊情况下可以撤销）。</p></blockquote><hr><h3 id="Q10：synchronized-和-ReentrantLock-有什么区别？"><a href="#Q10：synchronized-和-ReentrantLock-有什么区别？" class="headerlink" title="Q10：synchronized 和 ReentrantLock 有什么区别？"></a>Q10：synchronized 和 ReentrantLock 有什么区别？</h3><p><strong>核心考点</strong>：可中断性、公平锁、条件变量、tryLock 超时。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>synchronized</code> 是 JVM 层面的关键字，<code>ReentrantLock</code> 是 JDK 提供的 API。两者都是<strong>可重入锁</strong>，主要区别如下：</p><table><thead><tr><th>特性</th><th>synchronized</th><th>ReentrantLock</th></tr></thead><tbody><tr><td>实现层面</td><td>JVM 内置</td><td>JDK API（基于 AQS）</td></tr><tr><td>锁释放</td><td>自动释放（退出同步块）</td><td>必须手动 <code>unlock()</code>（需 finally）</td></tr><tr><td>公平性</td><td>非公平锁</td><td>支持公平锁（<code>new ReentrantLock(true)</code>）</td></tr><tr><td>可中断</td><td>不支持</td><td>支持 <code>lockInterruptibly()</code></td></tr><tr><td>超时获取</td><td>不支持</td><td>支持 <code>tryLock(timeout)</code></td></tr><tr><td>条件变量</td><td>只有一个等待队列</td><td>支持多个 <code>Condition</code>（精确唤醒）</td></tr></tbody></table><p>在我们的招行调度中台项目里，部分业务场景需要在等待锁时支持超时取消（防止长时间阻塞），因此使用了 <code>ReentrantLock.tryLock()</code> 配合超时时间，这是 <code>synchronized</code> 做不到的。</p></blockquote><hr><h3 id="Q11：Java-内存模型（JMM）是什么？happens-before-原则有哪些？"><a href="#Q11：Java-内存模型（JMM）是什么？happens-before-原则有哪些？" class="headerlink" title="Q11：Java 内存模型（JMM）是什么？happens-before 原则有哪些？"></a>Q11：Java 内存模型（JMM）是什么？happens-before 原则有哪些？</h3><p><strong>核心考点</strong>：主内存与工作内存的抽象，happens-before 保证可见性的规则。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>JMM（Java Memory Model）</strong> 定义了 JVM 如何与计算机内存进行交互的抽象规范。核心思想是：每个线程都有自己的<strong>工作内存（本地缓存）</strong>，线程操作的变量都是从主内存拷贝到工作内存进行操作，完成后再写回主内存。这种设计导致了多线程之间的<strong>可见性问题</strong>。</p><p><strong>happens-before</strong> 是 JMM 定义的一组规则，如果操作 A happens-before 操作 B，则 A 的执行结果对 B 可见，且 A 的执行顺序在 B 之前。常见的规则有：</p><ol><li><strong>程序顺序规则</strong>：同一线程中，前面的操作 happens-before 后面的操作。</li><li><strong>监视器锁规则</strong>：<code>unlock</code> happens-before 后续的 <code>lock</code>。</li><li><strong>volatile 规则</strong>：对 volatile 变量的写 happens-before 后续对该变量的读。</li><li><strong>线程启动规则</strong>：<code>Thread.start()</code> happens-before 线程内的所有操作。</li><li><strong>线程终止规则</strong>：线程的所有操作 happens-before <code>Thread.join()</code> 的返回。</li><li><strong>传递性</strong>：A happens-before B，B happens-before C，则 A happens-before C。</li></ol></blockquote><hr><h3 id="Q12：什么是-AQS？它的核心原理是什么？"><a href="#Q12：什么是-AQS？它的核心原理是什么？" class="headerlink" title="Q12：什么是 AQS？它的核心原理是什么？"></a>Q12：什么是 AQS？它的核心原理是什么？</h3><p><strong>核心考点</strong>：<code>AbstractQueuedSynchronizer</code>，state 变量，CLH 双向等待队列，独占&#x2F;共享模式。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>AQS（AbstractQueuedSynchronizer）</code> 是 JDK 并发包的核心框架，<code>ReentrantLock</code>、<code>CountDownLatch</code>、<code>Semaphore</code>、<code>ReadWriteLock</code> 等都是基于 AQS 实现的。</p><p><strong>核心原理</strong>：</p><ol><li><strong>state 变量</strong>：AQS 内部维护一个 <code>volatile int state</code> 变量来表示同步状态。以 <code>ReentrantLock</code> 为例，<code>state=0</code> 表示未锁，<code>state&gt;0</code> 表示锁被持有（值代表重入次数）。子类通过重写 <code>tryAcquire()</code> 和 <code>tryRelease()</code> 方法来定义具体的加锁和解锁逻辑，这些操作通过 <strong>CAS</strong> 来保证原子性。</li><li><strong>CLH 队列</strong>：当线程抢锁失败时，AQS 会将该线程封装为一个 <code>Node</code> 节点，加入一个 <strong>FIFO 的双向等待队列（CLH 队列变体）</strong>，并将线程通过 <code>LockSupport.park()</code> 挂起。当锁被释放时，头节点的后继节点会被 <code>LockSupport.unpark()</code> 唤醒，重新尝试抢锁。</li><li><strong>独占模式 vs 共享模式</strong>：独占模式同时只允许一个线程持有（如 ReentrantLock）；共享模式允许多个线程同时持有（如 Semaphore、CountDownLatch）。</li></ol></blockquote><hr><h3 id="Q13：CountDownLatch、CyclicBarrier、Semaphore-的使用场景？"><a href="#Q13：CountDownLatch、CyclicBarrier、Semaphore-的使用场景？" class="headerlink" title="Q13：CountDownLatch、CyclicBarrier、Semaphore 的使用场景？"></a>Q13：CountDownLatch、CyclicBarrier、Semaphore 的使用场景？</h3><p><strong>核心考点</strong>：三者的功能差异和适用场景。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><p><strong><code>CountDownLatch</code>（倒计时门栓）</strong>：主线程等待多个子线程都完成后再继续执行。计数器只能减不能增，减到 0 之后不能重置。在我们 SRM 采购比价系统中，主线程需要等待对多个供应商的并发 API 调用全部完成后，再进行汇总比价，这个场景就可以用 <code>CountDownLatch</code>（实际上我们用了 <code>CompletableFuture.allOf()</code>，原理类似）。</p></li><li><p><strong><code>CyclicBarrier</code>（循环屏障）</strong>：多个线程互相等待，当所有线程都到达屏障点时才继续执行，且可以<strong>循环复用</strong>。适用于多线程分批处理数据，每批处理完后所有线程在屏障点汇集再继续下一批的场景。</p></li><li><p><strong><code>Semaphore</code>（信号量）</strong>：控制同时访问某资源的线程数量，本质是一个计数器。比如数据库连接池最多允许 10 个线程同时访问，可以用 <code>Semaphore(10)</code> 来限制并发数。在我们医博系统中，如果导出任务过多，可以用 Semaphore 限制同时导出的任务数，防止内存撑爆。</p></li></ul></blockquote><hr><h3 id="Q14：死锁的四个必要条件？如何用-jstack-排查线上死锁？"><a href="#Q14：死锁的四个必要条件？如何用-jstack-排查线上死锁？" class="headerlink" title="Q14：死锁的四个必要条件？如何用 jstack 排查线上死锁？"></a>Q14：死锁的四个必要条件？如何用 jstack 排查线上死锁？</h3><p><strong>核心考点</strong>：四个条件（互斥&#x2F;请求保持&#x2F;不可剥夺&#x2F;循环等待），jstack 输出中 BLOCKED 状态线程和 deadlock 关键字。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>死锁的四个必要条件</strong>：</p><ol><li><strong>互斥条件</strong>：资源同一时刻只能被一个线程持有。</li><li><strong>请求与保持条件</strong>：线程持有至少一个资源，同时在等待另一个被其他线程持有的资源。</li><li><strong>不可剥夺条件</strong>：线程已获得的资源在未使用完之前不能被强行剥夺。</li><li><strong>循环等待条件</strong>：若干线程之间形成头尾相接的循环等待链。</li></ol><p><strong>排查方式</strong>：</p><ol><li>执行 <code>jstack &lt;pid&gt;</code> 打印线程堆栈，输出末尾会有 <code>Found one Java-level deadlock:</code> 的提示，并列出死锁的线程和它们等待的锁。</li><li>用 Arthas 执行 <code>thread -b</code> 命令，可以直接找出当前阻塞其他线程的罪魁祸首线程。</li></ol><p><strong>预防措施</strong>：统一锁的获取顺序；使用 <code>tryLock(timeout)</code> 加超时控制；尽量减小锁的粒度和持锁时间。</p></blockquote><hr><h3 id="Q15：线程的-6-种状态及转换"><a href="#Q15：线程的-6-种状态及转换" class="headerlink" title="Q15：线程的 6 种状态及转换"></a>Q15：线程的 6 种状态及转换</h3><p><strong>核心考点</strong>：<code>Thread.State</code> 枚举，各状态的转换条件。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Java 线程有 6 种状态（定义在 <code>Thread.State</code> 枚举中）：</p><ol><li><strong>NEW</strong>：线程对象被创建但尚未调用 <code>start()</code>。</li><li><strong>RUNNABLE</strong>：调用 <code>start()</code> 后进入此状态，包含”就绪（Ready）”和”运行中（Running）”两个子状态，由 CPU 调度器决定是否真正在执行。</li><li><strong>BLOCKED</strong>：线程等待获取一个<strong>被其他线程持有的 synchronized 锁</strong>时进入此状态。</li><li><strong>WAITING</strong>：调用 <code>Object.wait()</code>、<code>Thread.join()</code>、<code>LockSupport.park()</code> 等不带超时参数的方法后进入此状态，需要被其他线程显式唤醒（<code>notify()</code>&#x2F;<code>unpark()</code>）。</li><li><strong>TIMED_WAITING</strong>：调用带超时参数的 <code>Thread.sleep(ms)</code>、<code>Object.wait(ms)</code>、<code>Thread.join(ms)</code> 等方法，超时后自动返回 RUNNABLE。</li><li><strong>TERMINATED</strong>：线程执行完毕或抛出未捕获的异常，进入终止状态。</li></ol><p>用 <code>jstack</code> 排查问题时，最需要关注的是大量线程处于 <strong>BLOCKED</strong> 状态（说明锁竞争激烈或有死锁）和 <strong>WAITING</strong> 状态（说明大量线程在等待某个条件）。</p></blockquote><hr><h2 id="三、线程池与异步"><a href="#三、线程池与异步" class="headerlink" title="三、线程池与异步"></a>三、线程池与异步</h2><h3 id="Q16：ThreadPoolExecutor-的七大核心参数详解"><a href="#Q16：ThreadPoolExecutor-的七大核心参数详解" class="headerlink" title="Q16：ThreadPoolExecutor 的七大核心参数详解"></a>Q16：ThreadPoolExecutor 的七大核心参数详解</h3><p><strong>核心考点</strong>：七个参数的含义、工作队列选型、拒绝策略。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>ThreadPoolExecutor</code> 的构造函数有七个核心参数：</p><ol><li><strong><code>corePoolSize</code>（核心线程数）</strong>：线程池长期维持的最小线程数，即使空闲也不会销毁（除非设置 <code>allowCoreThreadTimeOut=true</code>）。</li><li><strong><code>maximumPoolSize</code>（最大线程数）</strong>：线程池最多能创建的线程数。当工作队列满了，且当前线程数小于 maxPoolSize 时，才会继续创建线程。</li><li><strong><code>keepAliveTime</code>（空闲线程存活时间）</strong>：超过 corePoolSize 的空闲线程，存活超过此时间后会被销毁。</li><li><strong><code>unit</code>（时间单位）</strong>：keepAliveTime 的单位，如 <code>TimeUnit.SECONDS</code>。</li><li><strong><code>workQueue</code>（工作队列）</strong>：存储等待执行任务的阻塞队列。常用的有：<code>LinkedBlockingQueue</code>（无界队列，有 OOM 风险）、<code>ArrayBlockingQueue</code>（有界队列，推荐）、<code>SynchronousQueue</code>（不存储任务，直接移交）。</li><li><strong><code>threadFactory</code>（线程工厂）</strong>：用于创建线程，建议自定义工厂并为线程命名（如 <code>&quot;order-pool-%d&quot;</code>），方便在 jstack 中识别。</li><li><strong><code>handler</code>（拒绝策略）</strong>：当线程数达到 max 且队列已满时，对新任务的处理策略。</li></ol><p>线程池任务提交的执行流程：<strong>核心线程数未满 → 创建核心线程执行 → 核心线程已满 → 任务加入队列 → 队列已满 → 创建非核心线程 → 非核心线程也满 → 执行拒绝策略。</strong></p></blockquote><hr><h3 id="Q17：四种拒绝策略的行为和适用场景"><a href="#Q17：四种拒绝策略的行为和适用场景" class="headerlink" title="Q17：四种拒绝策略的行为和适用场景"></a>Q17：四种拒绝策略的行为和适用场景</h3><p><strong>核心考点</strong>：四种策略的行为差异和业务适用性。</p><p><strong>背诵话术</strong>：</p><blockquote><ol><li><strong><code>AbortPolicy</code>（默认）</strong>：直接抛出 <code>RejectedExecutionException</code> 异常。适用于需要立即感知到任务被拒绝的场景，方便调用方做异常处理和告警。</li><li><strong><code>CallerRunsPolicy</code>（调用者运行）</strong>：由提交任务的线程（调用者线程）自己来执行这个任务，不会丢失任务。有<strong>反压效果</strong>：当线程池满时，让调用方”降速”，避免任务队列无限增长。在我们 SRM 采购报价引擎中，这是一个不错的选择，因为即使线程池满，任务也不会丢失。</li><li><strong><code>DiscardPolicy</code>（静默丢弃）</strong>：直接丢弃新任务，不抛异常。适用于允许丢弃的非关键性任务，如日志上报。</li><li><strong><code>DiscardOldestPolicy</code>（丢弃最旧任务）</strong>：丢弃队列中最旧（等待时间最长）的任务，让新任务入队。慎用，被丢弃的任务可能很重要。</li></ol></blockquote><hr><h3 id="Q18：CompletableFuture-异步编排在-SRM-项目中的实战应用"><a href="#Q18：CompletableFuture-异步编排在-SRM-项目中的实战应用" class="headerlink" title="Q18：CompletableFuture 异步编排在 SRM 项目中的实战应用"></a>Q18：CompletableFuture 异步编排在 SRM 项目中的实战应用</h3><p><strong>核心考点</strong>：<code>supplyAsync</code>、<code>allOf</code>、<code>exceptionally</code>、<code>thenCombine</code>、线程池隔离。</p><p><strong>背诵话术</strong>：</p><blockquote><p>在我们 SRM 采购比价中台里，每次生成询价单时，需要并发地向 5~10 家供应商的接口发起报价请求。如果串行调用，假设每家接口平均耗时 500ms，10 家串行就是 5 秒，完全不可接受。我们用 <code>CompletableFuture</code> 来实现并发编排：</p><figure class="highlight java"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">// 为每个供应商的报价请求创建异步任务，使用独立的 IO 线程池</span></span><br><span class="line">List&lt;CompletableFuture&lt;QuoteResult&gt;&gt; futures = suppliers.stream()</span><br><span class="line">    .map(supplier -&gt; CompletableFuture</span><br><span class="line">        .supplyAsync(() -&gt; fetchQuote(supplier), ioThreadPool)</span><br><span class="line">        .exceptionally(ex -&gt; &#123;</span><br><span class="line">            log.warn(<span class="string">&quot;供应商 &#123;&#125; 报价失败，使用默认值&quot;</span>, supplier.getId(), ex);</span><br><span class="line">            <span class="keyword">return</span> QuoteResult.empty(supplier.getId()); <span class="comment">// 降级返回空对象</span></span><br><span class="line">        &#125;))</span><br><span class="line">    .collect(Collectors.toList());</span><br><span class="line"></span><br><span class="line"><span class="comment">// 等待所有任务完成</span></span><br><span class="line">CompletableFuture.allOf(futures.toArray(<span class="keyword">new</span> <span class="title class_">CompletableFuture</span>[<span class="number">0</span>])).join();</span><br><span class="line"></span><br><span class="line"><span class="comment">// 汇总所有报价结果</span></span><br><span class="line">List&lt;QuoteResult&gt; results = futures.stream()</span><br><span class="line">    .map(CompletableFuture::join)</span><br><span class="line">    .collect(Collectors.toList());</span><br></pre></td></tr></table></figure><p>关键点：</p><ul><li>使用<strong>自定义隔离线程池</strong> <code>ioThreadPool</code> 而非公共的 <code>ForkJoinPool</code>，防止报价任务占满公共资源。</li><li><code>.exceptionally()</code> 对单个供应商失败进行兜底，返回默认空报价，保证整体流程不中断。</li><li>10 家供应商的总耗时从串行 5s 缩短为并发约 500ms（最慢那家的耗时）。</li></ul></blockquote><hr><h3 id="Q19：ThreadLocal-内存泄漏原因及如何避免"><a href="#Q19：ThreadLocal-内存泄漏原因及如何避免" class="headerlink" title="Q19：ThreadLocal 内存泄漏原因及如何避免"></a>Q19：ThreadLocal 内存泄漏原因及如何避免</h3><p><strong>核心考点</strong>：弱引用 Key + 强引用 Value 导致 Value 无法被 GC，线程池场景下线程复用加剧泄漏。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>ThreadLocal</code> 的底层是每个 <code>Thread</code> 对象内部持有一个 <code>ThreadLocalMap</code>，<code>ThreadLocalMap</code> 中的 <code>Entry</code> 结构是 <code>{Key: ThreadLocal的弱引用, Value: 实际存储值的强引用}</code>。</p><p><strong>内存泄漏的原因</strong>：当我们把 <code>ThreadLocal</code> 变量置为 <code>null</code> 时，下次 GC 时 <code>Key（弱引用）</code> 会被回收变为 <code>null</code>，但 <code>Value</code> 由于是<strong>强引用</strong>，只要 <code>Thread</code> 对象本身不被销毁，<code>ThreadLocalMap → Entry → Value</code> 这条引用链就永远存在，Value 永远无法被 GC 回收，导致内存泄漏。</p><p>在<strong>线程池场景</strong>下，线程会被复用，线程永远不会销毁，所以泄漏会随着时间不断累积，最终可能导致 OOM。</p><p><strong>解决方案</strong>：</p><ol><li><strong>在 <code>finally</code> 块中强制调用 <code>remove()</code></strong>，这是最根本的解决方式。</li><li>可以配合代码规范检查（如 Checkstyle）强制要求使用 ThreadLocal 的地方必须有对应的 <code>remove()</code>。</li></ol><p>在我们招行调度中台中，每个请求开始时通过 <code>ThreadLocal</code> 存储当前登录用户的上下文信息，请求处理完毕后在 AOP 的后置拦截器中统一执行 <code>remove()</code>，确保不会产生泄漏。</p></blockquote><hr><h3 id="Q20：并发线程池的实际参数配置经验与监控实战？"><a href="#Q20：并发线程池的实际参数配置经验与监控实战？" class="headerlink" title="Q20：并发线程池的实际参数配置经验与监控实战？"></a>Q20：并发线程池的实际参数配置经验与监控实战？</h3><p><strong>核心考点</strong>：CPU密集型与IO密集型的线程数设置依据，有界队列防OOM，CallerRunsPolicy，动态监控。</p><p><strong>背诵话术</strong>：</p><blockquote><p>线程池的参数配置没有万能公式，需要结合具体业务类型来决定：</p><ul><li><strong>CPU 密集型（如复杂计算、数据加密、音视频编解码）</strong>：<code>corePoolSize = CPU 核数 + 1</code>。多出来的一个线程用于防备线程偶发性缺页中断或其他原因导致的停顿。核心在于减少线程上下文切换开销。</li><li><strong>IO 密集型（如数据库查询、第三方 HTTP 接口调用、文件读写）</strong>：<code>corePoolSize = 2 × CPU 核数</code>。等待 IO 时，CPU 是空闲的，可以多配置一些线程以提高 CPU 利用率。</li></ul><p><strong>结合项目（SRM 采购比价系统）的实际配置经验</strong>：<br>我们在 SRM 系统中对供应商报价接口调用的线程池进行了如下配置（以 8 核 CPU 为例）：</p><ul><li><strong>核心线程数（<code>corePoolSize</code>）</strong>：设置为 16（即 2 × 8）。</li><li><strong>最大线程数（<code>maximumPoolSize</code>）</strong>：设置为 32。</li><li><strong>阻塞队列（<code>workQueue</code>）</strong>：使用有界的 <strong><code>ArrayBlockingQueue</code></strong>，容量为 500。拒绝使用无界的 <code>LinkedBlockingQueue</code>，防止极端并发下队列无限增长导致内存泄漏和 OOM。</li><li><strong>拒绝策略（<code>handler</code>）</strong>：配置为 <strong><code>CallerRunsPolicy</code></strong>（调用者执行）。若发生积压，让主线程（提交任务的 Web 线程）来执行当前任务。这样可以产生“反压（Backpressure）”效果，自然降速，保证任务不丢失，也给线程池缓冲的时间。</li></ul><p><strong>线程池监控实战</strong>：<br>仅仅配置参数是不够的，生产中我们需要监控线程池的状态：</p><ol><li><strong>核心指标采集</strong>：通过暴露 Actuator 端点，或在自定义 ThreadPoolExecutor 中覆写 <code>beforeExecute()</code> 和 <code>afterExecute()</code>，收集活动线程数（<code>getActiveCount()</code>）、当前线程池大小（<code>getPoolSize()</code>）和队列积压大小（<code>getQueue().size()</code>）。</li><li><strong>告警机制</strong>：配合 Prometheus + Grafana 进行监控，一旦工作队列使用率达到 80%（如 500容量积压到 400 以上）或者活跃线程数接近最大线程数，立刻触发钉钉或企业微信告警，以便人工干预或动态扩容。</li></ol></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> Java </tag>
            
            <tag> 并发 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Spring框架与微服务面试考题</title>
      <link href="/2026/06/08/Spring%E6%A1%86%E6%9E%B6%E4%B8%8E%E5%BE%AE%E6%9C%8D%E5%8A%A1%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/Spring%E6%A1%86%E6%9E%B6%E4%B8%8E%E5%BE%AE%E6%9C%8D%E5%8A%A1%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="02-Spring框架与微服务-·-高频面试考题背诵指南"><a href="#02-Spring框架与微服务-·-高频面试考题背诵指南" class="headerlink" title="02 - Spring框架与微服务 · 高频面试考题背诵指南"></a>02 - Spring框架与微服务 · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1spring-ioc-%E7%9A%84%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86beanfactory-%E5%92%8C-applicationcontext-%E7%9A%84%E5%8C%BA%E5%88%AB">Q1：Spring IOC 的工作原理？BeanFactory 和 ApplicationContext 的区别？</a></li><li><a href="#q2spring-bean-%E7%9A%84%E5%AE%8C%E6%95%B4%E7%94%9F%E5%91%BD%E5%91%A8%E6%9C%9F">Q2：Spring Bean 的完整生命周期</a></li><li><a href="#q3spring-aop-%E5%8E%9F%E7%90%86jdk-%E5%8A%A8%E6%80%81%E4%BB%A3%E7%90%86%E5%92%8C-cglib-%E4%BB%A3%E7%90%86%E7%9A%84%E5%8C%BA%E5%88%AB">Q3：Spring AOP 原理？JDK 动态代理和 CGLIB 代理的区别？</a></li><li><a href="#q4spring-%E5%A6%82%E4%BD%95%E8%A7%A3%E5%86%B3%E5%BE%AA%E7%8E%AF%E4%BE%9D%E8%B5%96%E4%B8%89%E7%BA%A7%E7%BC%93%E5%AD%98%E5%90%84%E8%87%AA%E7%9A%84%E4%BD%9C%E7%94%A8">Q4：Spring 如何解决循环依赖？三级缓存各自的作用？</a></li><li><a href="#q5autowired-%E5%92%8C-resource-%E7%9A%84%E5%8C%BA%E5%88%AB">Q5：@Autowired 和 @Resource 的区别？</a></li><li><a href="#q6transactional-%E5%A4%B1%E6%95%88%E7%9A%84%E5%9C%BA%E6%99%AF%E8%87%B3%E5%B0%91-5-%E7%A7%8D">Q6：@Transactional 失效的场景（至少 5 种）</a></li><li><a href="#q7spring-%E4%BA%8B%E5%8A%A1%E7%9A%84-7-%E7%A7%8D%E4%BC%A0%E6%92%AD%E8%A1%8C%E4%B8%BA">Q7：Spring 事务的 7 种传播行为</a></li><li><a href="#q8springboot-%E8%87%AA%E5%8A%A8%E8%A3%85%E9%85%8D%E5%8E%9F%E7%90%86springbootapplication-%E5%81%9A%E4%BA%86%E4%BB%80%E4%B9%88">Q8：SpringBoot 自动装配原理？@SpringBootApplication 做了什么？</a></li><li><a href="#q9mybatis-%E7%9A%84%E4%B8%80%E7%BA%A7%E7%BC%93%E5%AD%98%E5%92%8C%E4%BA%8C%E7%BA%A7%E7%BC%93%E5%AD%98%E5%88%86%E5%88%AB%E6%9C%89%E4%BB%80%E4%B9%88%E5%9D%91">Q9：MyBatis 的一级缓存和二级缓存？分别有什么坑？</a></li><li><a href="#q10mybatis-%E7%9A%84-%E5%92%8C-%E7%9A%84%E5%8C%BA%E5%88%AB">Q10：MyBatis 的 #{} 和 ${} 的区别？</a></li><li><a href="#q11nacos-%E6%B3%A8%E5%86%8C%E4%B8%AD%E5%BF%83%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86%E5%92%8C-eureka-%E7%9A%84%E5%8C%BA%E5%88%AB">Q11：Nacos 注册中心工作原理？和 Eureka 的区别？</a></li><li><a href="#q12sentinel-%E7%9A%84%E9%99%90%E6%B5%81%E7%AE%97%E6%B3%95%E5%92%8C%E7%86%94%E6%96%AD%E7%AD%96%E7%95%A5">Q12：Sentinel 的限流算法和熔断策略</a></li><li><a href="#q13seata-%E7%9A%84-tcc-%E6%A8%A1%E5%BC%8F%E5%8E%9F%E7%90%86at-%E6%A8%A1%E5%BC%8F%E5%92%8C-tcc-%E7%9A%84%E5%8C%BA%E5%88%AB">Q13：Seata 的 TCC 模式原理？AT 模式和 TCC 的区别？</a></li><li><a href="#q14tcc-%E6%A8%A1%E5%BC%8F%E7%9A%84%E4%B8%89%E5%A4%A7%E5%BC%82%E5%B8%B8%E5%9C%BA%E6%99%AF%E7%A9%BA%E5%9B%9E%E6%BB%9A%E6%82%AC%E6%8C%82%E5%B9%82%E7%AD%89">Q14：TCC 模式的三大异常场景（空回滚、悬挂、幂等）</a></li><li><a href="#q15%E5%8F%AF%E9%9D%A0%E6%B6%88%E6%81%AF%E6%9C%80%E7%BB%88%E4%B8%80%E8%87%B4%E6%80%A7%E6%9C%AC%E5%9C%B0%E6%B6%88%E6%81%AF%E8%A1%A8%E6%96%B9%E6%A1%88">Q15：可靠消息最终一致性（本地消息表方案）</a></li><li><a href="#q162pc-3pc-tcc-saga-%E5%8F%AF%E9%9D%A0%E6%B6%88%E6%81%AF-%E4%BA%94%E5%A4%A7%E6%96%B9%E6%A1%88%E5%AF%B9%E6%AF%94">Q16：2PC &#x2F; 3PC &#x2F; TCC &#x2F; Saga &#x2F; 可靠消息 五大方案对比</a></li><li><a href="#q17feign-%E7%9A%84%E8%B6%85%E6%97%B6%E9%87%8D%E8%AF%95%E4%B8%8E%E8%B4%9F%E8%BD%BD%E5%9D%87%E8%A1%A1%E5%8E%9F%E7%90%86">Q17：Feign 的超时、重试与负载均衡原理</a></li><li><a href="#q18spring-cloud-gateway-%E6%A0%B8%E5%BF%83%E6%9E%B6%E6%9E%84%E4%B8%8E%E5%BA%94%E7%94%A8">Q18：Spring Cloud Gateway 核心架构与应用</a></li></ul><hr><span id="more"></span><h2 id="一、Spring-Core"><a href="#一、Spring-Core" class="headerlink" title="一、Spring Core"></a>一、Spring Core</h2><h3 id="Q1：Spring-IOC-的工作原理？BeanFactory-和-ApplicationContext-的区别？"><a href="#Q1：Spring-IOC-的工作原理？BeanFactory-和-ApplicationContext-的区别？" class="headerlink" title="Q1：Spring IOC 的工作原理？BeanFactory 和 ApplicationContext 的区别？"></a>Q1：Spring IOC 的工作原理？BeanFactory 和 ApplicationContext 的区别？</h3><p><strong>核心考点</strong>：控制反转思想、容器启动流程、两种容器的差异。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>IOC（控制反转）</code> 的核心思想是：对象的创建和依赖关系的管理不再由业务代码自己控制，而是<strong>交给 Spring 容器</strong>来负责。我们只需要通过注解或 XML 配置声明 Bean 的定义，容器会在启动时扫描这些定义，完成 Bean 的实例化、属性填充和依赖注入，并将 Bean 存储在内部的 <code>Map&lt;String, BeanDefinition&gt;</code> 结构中。</p><p><strong><code>BeanFactory</code> vs <code>ApplicationContext</code></strong>：</p><ul><li><code>BeanFactory</code> 是最基础的 IOC 容器接口，采用<strong>懒加载</strong>策略，只有在第一次调用 <code>getBean()</code> 时才实例化 Bean，功能较为简单。</li><li><code>ApplicationContext</code> 继承自 <code>BeanFactory</code>，是更完整的企业级容器，支持<strong>饿加载</strong>（容器启动时就实例化所有单例 Bean）、国际化（i18n）、事件发布（<code>ApplicationEvent</code>）、AOP 自动代理等扩展功能。生产项目中一律使用 <code>ApplicationContext</code>。</li></ul></blockquote><hr><h3 id="Q2：Spring-Bean-的完整生命周期"><a href="#Q2：Spring-Bean-的完整生命周期" class="headerlink" title="Q2：Spring Bean 的完整生命周期"></a>Q2：Spring Bean 的完整生命周期</h3><p><strong>核心考点</strong>：从实例化到销毁的完整 11 个步骤。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Spring Bean 的完整生命周期如下：</p><ol><li><strong>实例化（Instantiation）</strong>：容器通过反射调用构造方法创建 Bean 的原始对象（此时属性还未填充）。</li><li><strong>属性填充（Populate Properties）</strong>：将配置文件或注解中声明的属性值和依赖的其他 Bean 注入进来（<code>@Autowired</code>、<code>@Value</code> 在这一步生效）。</li><li><strong><code>Aware</code> 接口回调</strong>：如果 Bean 实现了 <code>BeanNameAware</code>、<code>BeanFactoryAware</code>、<code>ApplicationContextAware</code> 等接口，容器会依次回调对应的 <code>setXxx()</code> 方法，让 Bean 能感知到容器的相关信息。</li><li><strong><code>BeanPostProcessor</code> 前置处理（<code>postProcessBeforeInitialization</code>）</strong>：对 Bean 进行初始化前的增强处理，Spring AOP 的代理对象就是在 BeanPostProcessor 中创建的。</li><li><strong>初始化（Initialization）</strong>：依次执行 <code>@PostConstruct</code> 注解方法 → <code>InitializingBean.afterPropertiesSet()</code> → XML 中配置的 <code>init-method</code>。</li><li><strong><code>BeanPostProcessor</code> 后置处理（<code>postProcessAfterInitialization</code>）</strong>：初始化后的增强，<strong>Spring AOP 代理实际上在此阶段生成并替换原始 Bean</strong>。</li><li><strong>Bean 就绪，放入单例池（singletonObjects）</strong>：Bean 可以被正常使用。</li><li><strong>销毁（Destruction）</strong>：容器关闭时，依次执行 <code>@PreDestroy</code> → <code>DisposableBean.destroy()</code> → XML 配置的 <code>destroy-method</code>。</li></ol></blockquote><hr><h3 id="Q3：Spring-AOP-原理？JDK-动态代理和-CGLIB-代理的区别？"><a href="#Q3：Spring-AOP-原理？JDK-动态代理和-CGLIB-代理的区别？" class="headerlink" title="Q3：Spring AOP 原理？JDK 动态代理和 CGLIB 代理的区别？"></a>Q3：Spring AOP 原理？JDK 动态代理和 CGLIB 代理的区别？</h3><p><strong>核心考点</strong>：代理模式，两种代理的底层实现，何时用哪种。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Spring AOP 的本质是<strong>动态代理</strong>。Spring 会在 Bean 初始化的 <code>BeanPostProcessor</code> 后置处理阶段，判断该 Bean 是否需要被 AOP 切面增强，如果需要，则为该 Bean 创建一个代理对象并返回，后续从容器中获取到的就是这个代理对象。</p><p>Spring 支持两种代理方式：</p><p><strong>JDK 动态代理</strong>：</p><ul><li>要求目标类必须<strong>实现至少一个接口</strong>。</li><li>代理对象通过 <code>java.lang.reflect.Proxy.newProxyInstance()</code> 创建，代理对象实现了与目标类相同的接口。</li><li>方法调用通过 <code>InvocationHandler.invoke()</code> 进行拦截和增强。</li></ul><p><strong>CGLIB 代理</strong>：</p><ul><li><strong>不要求目标类实现接口</strong>，适用于没有接口的普通类。</li><li>通过字节码技术（ASM）在运行时动态生成目标类的<strong>子类</strong>作为代理对象，通过重写父类方法来实现拦截。</li><li>因为是继承方式实现，<code>final</code> 修饰的类和方法<strong>无法被代理</strong>。</li></ul><p><strong>选择策略</strong>：在 Spring Boot 2.x 中，默认强制使用 CGLIB（<code>spring.aop.proxy-target-class=true</code>），因为 CGLIB 不需要依赖接口，更通用。</p></blockquote><hr><h3 id="Q4：Spring-如何解决循环依赖？三级缓存各自的作用？"><a href="#Q4：Spring-如何解决循环依赖？三级缓存各自的作用？" class="headerlink" title="Q4：Spring 如何解决循环依赖？三级缓存各自的作用？"></a>Q4：Spring 如何解决循环依赖？三级缓存各自的作用？</h3><p><strong>核心考点</strong>：三个 Map 的存储内容，解决循环依赖的完整流程，为何需要三级而不是二级。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Spring 通过<strong>三级缓存</strong>来解决单例 Bean 之间的循环依赖：</p><ul><li><strong>一级缓存（<code>singletonObjects</code>）</strong>：存储<strong>完全初始化好的 Bean</strong>，可以直接对外使用。</li><li><strong>二级缓存（<code>earlySingletonObjects</code>）</strong>：存储<strong>提前暴露的、尚未完全初始化的 Bean 对象</strong>（已实例化但属性还未填充）。</li><li><strong>三级缓存（<code>singletonFactories</code>）</strong>：存储 <strong>Bean 的工厂对象（<code>ObjectFactory</code>）</strong>，调用工厂的 <code>getObject()</code> 方法可以得到 Bean 的早期引用（可能是代理对象）。</li></ul><p><strong>解决循环依赖的流程（A 依赖 B，B 依赖 A）</strong>：</p><ol><li>创建 A，A 实例化后，将 A 的 <code>ObjectFactory</code> 放入<strong>三级缓存</strong>。</li><li>A 开始填充属性，发现需要注入 B，触发 B 的创建。</li><li>创建 B，B 实例化后，将 B 的 <code>ObjectFactory</code> 放入三级缓存。</li><li>B 填充属性时，发现需要注入 A，去三级缓存中找到 A 的工厂，调用 <code>getObject()</code> 获取 A 的早期引用，并将其升级到<strong>二级缓存</strong>，A 的工厂从三级缓存中移除。</li><li>B 成功注入了 A 的早期引用，B 完成初始化，进入<strong>一级缓存</strong>。</li><li>A 继续完成属性填充（注入了完整的 B），A 完成初始化，进入一级缓存。</li></ol><p><strong>为什么需要三级而不是二级</strong>：三级缓存中存的是 <code>ObjectFactory</code> 而不是对象本身，目的是支持 <strong>AOP 代理</strong>。如果 A 需要被 AOP 代理，则调用 <code>ObjectFactory.getObject()</code> 时会返回 A 的代理对象而非原始对象，确保 B 注入的是 A 的代理版本。如果只有二级缓存（直接存原始对象），就无法在循环依赖场景下保证注入的是正确的代理对象。</p></blockquote><hr><h3 id="Q5：-Autowired-和-Resource-的区别？"><a href="#Q5：-Autowired-和-Resource-的区别？" class="headerlink" title="Q5：@Autowired 和 @Resource 的区别？"></a>Q5：@Autowired 和 @Resource 的区别？</h3><p><strong>核心考点</strong>：注入方式、来源规范、按类型 vs 按名称。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><strong><code>@Autowired</code></strong>：Spring 原生注解，<strong>优先按类型（byType）注入</strong>。如果容器中有多个相同类型的 Bean，则再按字段名（byName）进行匹配，也可以配合 <code>@Qualifier</code> 指定 Bean 名称。</li><li><strong><code>@Resource</code></strong>：JDK 原生注解（<code>javax.annotation</code>），<strong>优先按名称（byName）注入</strong>。如果找不到匹配名称，再按类型匹配。适合希望减少对 Spring 框架依赖的场景。</li></ul><p>实际开发中两者差别不大，<code>@Autowired</code> 更常用，但当接口有多个实现类时，<code>@Autowired + @Qualifier(&quot;beanName&quot;)</code> 或 <code>@Resource(name=&quot;beanName&quot;)</code> 来明确指定是最清晰的做法。</p></blockquote><hr><h3 id="Q6：-Transactional-失效的场景（至少-5-种）"><a href="#Q6：-Transactional-失效的场景（至少-5-种）" class="headerlink" title="Q6：@Transactional 失效的场景（至少 5 种）"></a>Q6：@Transactional 失效的场景（至少 5 种）</h3><p><strong>核心考点</strong>：AOP 代理机制导致的各种失效场景。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>@Transactional</code> 失效最常见的场景有以下几种：</p><ol><li><strong>自我调用（同类内部调用）</strong>：在同一个 Service 类中，方法 A（无事务）直接调用方法 B（有 <code>@Transactional</code>），因为 A 内部调用 B 是通过 <code>this.B()</code>，没有走 Spring 的代理对象，事务切面无法被触发，事务完全失效。解决方法：通过 <code>ApplicationContext</code> 获取自身代理对象再调用，或将 B 方法抽取到另一个 Service 类中。</li><li><strong>方法非 public</strong>：Spring 事务的 AOP 拦截器只对 <code>public</code> 方法生效，<code>private</code>、<code>protected</code> 或包级别方法上的 <code>@Transactional</code> 会被忽略。</li><li><strong>异常被 catch 吞掉</strong>：方法内部用 <code>try-catch</code> 捕获了异常，没有在 <code>catch</code> 中重新抛出，Spring 的事务管理器感知不到异常，事务不会回滚。</li><li><strong>异常类型不匹配</strong>：<code>@Transactional</code> 默认只对 <code>RuntimeException</code> 和 <code>Error</code> 回滚，如果抛出的是 <code>IOException</code> 等受检异常，事务不会回滚。需要配置 <code>@Transactional(rollbackFor = Exception.class)</code>。</li><li><strong>数据库不支持事务</strong>：如使用 MySQL 的 <code>MyISAM</code> 存储引擎，本身不支持事务，注解无效。</li><li><strong>Bean 未被 Spring 管理</strong>：如果类没有被 Spring 扫描到（忘加 <code>@Service</code>），不在容器中，自然没有代理。</li><li><strong>多线程中失效</strong>：<code>@Transactional</code> 的事务是绑定在当前线程的 <code>Connection</code> 上的，如果方法内开启了新线程去执行数据库操作，新线程拿到的是不同的 <code>Connection</code>，不在同一个事务中。</li></ol></blockquote><hr><h3 id="Q7：Spring-事务的-7-种传播行为"><a href="#Q7：Spring-事务的-7-种传播行为" class="headerlink" title="Q7：Spring 事务的 7 种传播行为"></a>Q7：Spring 事务的 7 种传播行为</h3><p><strong>核心考点</strong>：各传播行为对当前事务的处理逻辑，<code>REQUIRED</code> vs <code>REQUIRES_NEW</code> vs <code>NESTED</code> 的区别。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Spring 事务传播行为定义了”当一个事务方法被另一个事务方法调用时，该如何处理事务”：</p><table><thead><tr><th>传播行为</th><th>说明</th></tr></thead><tbody><tr><td><code>REQUIRED</code>（默认）</td><td>有事务则加入，没有则新建。最常用。</td></tr><tr><td><code>REQUIRES_NEW</code></td><td>无论如何都<strong>新建一个独立事务</strong>，挂起当前事务。两个事务互不影响，互不回滚。</td></tr><tr><td><code>NESTED</code></td><td>在当前事务内创建一个<strong>嵌套事务（保存点 Savepoint）</strong>，子事务可以独立回滚到保存点，不影响父事务；但父事务回滚会导致子事务也回滚。</td></tr><tr><td><code>SUPPORTS</code></td><td>有事务则加入，没有事务则以非事务方式执行。</td></tr><tr><td><code>NOT_SUPPORTED</code></td><td>以非事务方式执行，若当前有事务则挂起事务。</td></tr><tr><td><code>MANDATORY</code></td><td>必须在事务中执行，若没有活动事务则抛异常。</td></tr><tr><td><code>NEVER</code></td><td>以非事务方式执行，若当前有事务则抛异常。</td></tr></tbody></table><p>实际项目中，<code>REQUIRES_NEW</code> 常用于”子操作失败不影响主流程”的场景，比如在招行调度中台中，记录操作日志的方法使用 <code>REQUIRES_NEW</code>，即使日志写入失败也不影响主业务事务的提交。</p></blockquote><hr><h2 id="二、SpringBoot"><a href="#二、SpringBoot" class="headerlink" title="二、SpringBoot"></a>二、SpringBoot</h2><h3 id="Q8：SpringBoot-自动装配原理？-SpringBootApplication-做了什么？"><a href="#Q8：SpringBoot-自动装配原理？-SpringBootApplication-做了什么？" class="headerlink" title="Q8：SpringBoot 自动装配原理？@SpringBootApplication 做了什么？"></a>Q8：SpringBoot 自动装配原理？@SpringBootApplication 做了什么？</h3><p><strong>核心考点</strong>：组合注解、<code>@EnableAutoConfiguration</code>、<code>spring.factories</code> SPI 机制。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>@SpringBootApplication</code> 是一个组合注解，包含了三个核心注解：</p><ol><li><strong><code>@SpringBootConfiguration</code></strong>：标识这是一个 Spring 配置类（本质是 <code>@Configuration</code>）。</li><li><strong><code>@ComponentScan</code></strong>：开启组件扫描，默认扫描主启动类所在包及其子包下的所有 <code>@Component</code>、<code>@Service</code>、<code>@Repository</code>、<code>@Controller</code> 等注解。</li><li><strong><code>@EnableAutoConfiguration</code></strong>：这是自动装配的核心。</li></ol><p><strong><code>@EnableAutoConfiguration</code> 的工作原理</strong>：</p><ol><li>它通过 <code>@Import(AutoConfigurationImportSelector.class)</code> 导入一个选择器。</li><li>这个选择器的 <code>selectImports()</code> 方法会扫描所有 JAR 包的 <code>META-INF/spring.factories</code> 文件（JDK SPI 扩展机制），读取 <code>org.springframework.boot.autoconfigure.EnableAutoConfiguration</code> 键下所有的自动配置类全限定名（通常有 100+ 个）。</li><li>然后通过 <code>@ConditionalOnClass</code>、<code>@ConditionalOnMissingBean</code>、<code>@ConditionalOnProperty</code> 等条件注解进行过滤，只有当条件满足时（比如类路径下存在某个类），对应的自动配置类才会生效。</li><li>最终将满足条件的配置类注册到 Spring 容器中，完成相关 Bean 的自动创建。</li></ol><p><strong>💡 SpringBoot 3.x 版本变更</strong>：<br>从 SpringBoot 3.0 开始，官方不再默认扫描 <code>META-INF/spring.factories</code> 文件。自动装配的配置文件路径改为 <code>META-INF/spring/org.springframework.boot.autoconfigure.AutoConfiguration.imports</code>。每个自动配置类的类名单独占一行，不再需要以逗号分隔，面试时需特别注意项目所使用的版本。</p></blockquote><hr><h3 id="Q9：MyBatis-的一级缓存和二级缓存？分别有什么坑？"><a href="#Q9：MyBatis-的一级缓存和二级缓存？分别有什么坑？" class="headerlink" title="Q9：MyBatis 的一级缓存和二级缓存？分别有什么坑？"></a>Q9：MyBatis 的一级缓存和二级缓存？分别有什么坑？</h3><p><strong>核心考点</strong>：缓存作用域（SqlSession vs Mapper&#x2F;Namespace），脏数据问题。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>一级缓存（SqlSession 级别）</strong>：默认开启，生命周期与 <code>SqlSession</code> 相同。同一个 <code>SqlSession</code> 内，相同的查询语句和参数第二次查询时直接从缓存中返回，不访问数据库。</p><p><strong>坑</strong>：在分布式或多 <code>SqlSession</code> 场景下，如果 Session A 修改了数据，Session B 的一级缓存中还保存着旧数据，会读取到脏数据。另外，执行 <code>insert</code>、<code>update</code>、<code>delete</code> 操作后，当前 <code>SqlSession</code> 的一级缓存会被清空。</p><p><strong>二级缓存（Mapper&#x2F;Namespace 级别）</strong>：需要手动开启（在 mapper.xml 中加 <code>&lt;cache/&gt;</code>），生命周期与 <code>SqlSessionFactory</code> 相同，可以跨 <code>SqlSession</code> 共享。</p><p><strong>坑（非常危险）</strong>：多表联查时，如果查询涉及表 A 和表 B，但缓存的 namespace 只绑定了 Mapper A，当表 B 数据变更时，Mapper A 的缓存不会失效，会读到脏数据。因此，<strong>生产中一般禁用 MyBatis 的二级缓存</strong>，而是使用 Redis 等统一的分布式缓存方案来替代。</p></blockquote><hr><h3 id="Q10：MyBatis-的-和-的区别？"><a href="#Q10：MyBatis-的-和-的区别？" class="headerlink" title="Q10：MyBatis 的 #{} 和 ${} 的区别？"></a>Q10：MyBatis 的 #{} 和 ${} 的区别？</h3><p><strong>核心考点</strong>：预编译占位符 vs 字符串替换，SQL 注入风险。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><strong><code>#{}</code>（预编译占位符）</strong>：MyBatis 会将 <code>#{}</code> 替换为 <code>?</code>，通过 JDBC 的 <code>PreparedStatement</code> 预编译处理，传入的参数值会作为字符串被转义，<strong>完全防止 SQL 注入</strong>。这是绝大多数场景下的首选。</li><li><strong><code>${}</code>（字符串拼接）</strong>：直接将参数值替换到 SQL 语句中，不做任何转义，存在严重的 <strong>SQL 注入风险</strong>。例如：<code>SELECT * FROM user WHERE name = &#39;${name}&#39;</code>，如果 name 传入 <code>&#39; OR 1=1 --</code>，就会执行 <code>SELECT * FROM user WHERE name = &#39;&#39; OR 1=1 --</code>，查出所有用户数据。</li></ul><p><code>${}</code> 唯一合理的使用场景是<strong>动态传入表名或列名</strong>（因为这类内容无法通过 <code>?</code> 参数传递），但此时必须在代码层面对传入值进行严格的白名单校验，杜绝用户直接控制输入。</p></blockquote><hr><h2 id="三、Spring-Cloud-微服务"><a href="#三、Spring-Cloud-微服务" class="headerlink" title="三、Spring Cloud 微服务"></a>三、Spring Cloud 微服务</h2><h3 id="Q11：Nacos-注册中心工作原理？和-Eureka-的区别？"><a href="#Q11：Nacos-注册中心工作原理？和-Eureka-的区别？" class="headerlink" title="Q11：Nacos 注册中心工作原理？和 Eureka 的区别？"></a>Q11：Nacos 注册中心工作原理？和 Eureka 的区别？</h3><p><strong>核心考点</strong>：服务注册&#x2F;发现机制，AP vs CP，Nacos 推送 vs Eureka 拉取。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Nacos 工作原理</strong>：服务启动时，将自身的 IP、端口、服务名等信息向 Nacos Server 进行<strong>注册</strong>，Nacos 维护一个服务注册表。客户端（调用方）向 Nacos 发起<strong>订阅</strong>，Nacos 会主动将服务实例列表推送给订阅方，同时客户端也会定期从 Nacos 拉取最新列表作为兜底（<strong>推拉结合</strong>）。Nacos 通过心跳机制（默认 5s 一次）检测服务健康状态，如果 15s 内未收到心跳，将该实例标记为不健康，30s 后从注册表中剔除。</p><p><strong>与 Eureka 的主要区别</strong>：</p><table><thead><tr><th>特性</th><th>Nacos</th><th>Eureka</th></tr></thead><tbody><tr><td>一致性模型</td><td>支持 AP（默认）和 CP（可切换）</td><td>只支持 AP</td></tr><tr><td>通知机制</td><td><strong>主动推送</strong></td><td>客户端<strong>定时拉取</strong>（30s&#x2F;次，更新慢）</td></tr><tr><td>健康检测</td><td>支持 TCP&#x2F;HTTP&#x2F;MySQL 等多种健康检查</td><td>仅 HTTP 心跳</td></tr><tr><td>配置中心</td><td>内置</td><td>需配合 Spring Cloud Config</td></tr><tr><td>维护状态</td><td>阿里开源，活跃维护</td><td>Netflix 官方停止维护</td></tr></tbody></table><p>在我们 SRM 和招行项目中均使用 Nacos 作为注册和配置中心，主要看中的是其推送机制带来的更快的服务发现响应，以及内置的配置中心能力。</p></blockquote><hr><h3 id="Q12：Sentinel-的限流算法和熔断策略"><a href="#Q12：Sentinel-的限流算法和熔断策略" class="headerlink" title="Q12：Sentinel 的限流算法和熔断策略"></a>Q12：Sentinel 的限流算法和熔断策略</h3><p><strong>核心考点</strong>：滑动窗口限流，三种熔断策略（慢调用&#x2F;异常比例&#x2F;异常数）。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Sentinel 的限流算法</strong>：</p><ul><li><strong>滑动窗口算法（默认）</strong>：将 1 秒划分为多个小窗口（默认 10 个，每个 100ms），统计每个小窗口内的请求量，滑动求和判断是否超阈值。比固定窗口算法更平滑，不会出现边界突刺问题。</li><li><strong>漏桶算法（<code>RuleConstant.CONTROL_BEHAVIOR_RATE_LIMITER</code>）</strong>：请求以固定速率匀速通过，超出速率的请求排队等待（队列模式），适合流量整形场景。</li><li><strong>令牌桶算法</strong>：令牌以固定速率生成，请求消耗令牌，令牌满了停止生成，支持一定程度的流量突发。</li></ul><p><strong>Sentinel 的三种熔断策略</strong>：</p><ol><li><strong>慢调用比例</strong>：统计窗口期内，响应时间超过阈值（如 500ms）的请求占比超过设定比例（如 50%），触发熔断。熔断后进入半开状态，允许少量探测请求通过。</li><li><strong>异常比例</strong>：统计窗口期内，异常请求占总请求的比例超过阈值，触发熔断。</li><li><strong>异常数</strong>：统计窗口期内，异常请求的绝对数量超过阈值，触发熔断。</li></ol><p>在招行调度中台中，我们对所有 Feign 外部 RPC 调用配置了<strong>慢调用比例熔断</strong>，设定响应超过 2s 且慢调用比例超过 30% 时熔断 30s，降级返回兜底数据，防止因下游服务”假死”拖垮上游。</p></blockquote><hr><h3 id="Q13：Seata-的-TCC-模式原理？AT-模式和-TCC-的区别？"><a href="#Q13：Seata-的-TCC-模式原理？AT-模式和-TCC-的区别？" class="headerlink" title="Q13：Seata 的 TCC 模式原理？AT 模式和 TCC 的区别？"></a>Q13：Seata 的 TCC 模式原理？AT 模式和 TCC 的区别？</h3><p><strong>核心考点</strong>：两阶段提交，AT 模式的自动回滚（undo_log），TCC 的业务侵入性。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>AT 模式（自动）</strong>：对业务代码无侵入，由 Seata 自动拦截 SQL，在执行前后生成 <code>undo_log</code>（回滚日志）。第一阶段：直接提交本地事务，释放数据库锁，并将 undo_log 持久化。第二阶段：如果全局事务提交成功，删除 undo_log；如果全局事务回滚，通过 undo_log 自动反向补偿恢复数据。<strong>AT 模式使用了全局行锁</strong>，高并发下性能有一定损耗。</p><p><strong>TCC 模式（手动）</strong>：将业务逻辑拆分为三个方法：</p><ul><li><strong><code>Try</code></strong>：预留资源（如冻结库存、预扣余额），检查业务可行性。</li><li><strong><code>Confirm</code></strong>：确认执行，消耗 Try 阶段预留的资源（真正扣减）。全局事务提交时调用。</li><li><strong><code>Cancel</code></strong>：取消执行，释放 Try 阶段预留的资源（解冻）。全局事务回滚时调用。</li></ul><p><strong>核心区别</strong>：AT 模式对代码无侵入但有全局锁，适合并发量一般的场景；TCC 模式需要开发者手动实现三个方法，有业务侵入性，但不依赖数据库全局锁，<strong>并发性能更高</strong>，适合金融级高并发场景。在招行核心调度中台里，关键的资金流转操作使用了 TCC 模式保证分布式事务的强一致性。</p></blockquote><hr><h3 id="Q14：TCC-模式的三大异常场景（空回滚、悬挂、幂等）"><a href="#Q14：TCC-模式的三大异常场景（空回滚、悬挂、幂等）" class="headerlink" title="Q14：TCC 模式的三大异常场景（空回滚、悬挂、幂等）"></a>Q14：TCC 模式的三大异常场景（空回滚、悬挂、幂等）</h3><p><strong>核心考点</strong>：TCC 的三个难题，各自解决手段。</p><p><strong>背诵话术</strong>：</p><blockquote><p>TCC 的核心难点不在于写 Try&#x2F;Confirm&#x2F;Cancel，而在于处理异常场景：</p></blockquote><blockquote><p><strong>1. 空回滚（Empty Rollback）</strong>：</p><ul><li><strong>场景</strong>：Try 阶段由于网络超时，被判定为失败，TC（事务协调器）发起 Cancel。但 Try 请求其实根本没到达过调用方（或到达了但调用方没执行），Cancel 被执行时本地<strong>根本没有</strong>对应的预留资源。</li><li><strong>解决</strong>：Cancel 操作需要支持<strong>空回滚</strong>——如果没找到对应的 Try 记录，Cancel 不能抛异常，而是记录一条”已回滚”标记，然后返回成功。</li></ul></blockquote><blockquote><p><strong>2. 悬挂（Suspend）</strong>：</p><ul><li><strong>场景</strong>：Cancel 比 Try 先到达。TC 先发起 Cancel（如 Try 超时），Cancel 执行完了，然后之前”堵在半路上的”Try 请求后到达执行了。如果 Try 执行成功并预扣了资源，这笔资源就永久被锁死了（Cancel 已经执行完了，没人来解冻）。</li><li><strong>解决</strong>：Cancel 执行时记录一条”已回滚”标记。Try 执行前先检查该标记，如果发现”已回滚”，则拒绝执行 Try（返回失败），<strong>阻止悬挂的发生</strong>。</li></ul></blockquote><blockquote><p><strong>3. 幂等控制</strong>：Try &#x2F; Confirm &#x2F; Cancel 三个操作都可能因网络重试被多次调用，所以三者都必须是<strong>幂等的</strong>——重复执行结果一致。通常用业务唯一 ID 做唯一索引防重。</p></blockquote><blockquote><p><strong>Seata TCC 框架的处理</strong>：Seata 的 TCC 模式通过维护一张事务记录表自动处理了空回滚和悬挂——Try 前查是否有 Cancel 记录（防悬挂），Cancel 前查是否有 Try 记录（空回滚则记录标记），Confirm&#x2F;Cancel 成功后记录状态（防幂等）。</p></blockquote><hr><h3 id="Q15：可靠消息最终一致性（本地消息表方案）"><a href="#Q15：可靠消息最终一致性（本地消息表方案）" class="headerlink" title="Q15：可靠消息最终一致性（本地消息表方案）"></a>Q15：可靠消息最终一致性（本地消息表方案）</h3><p><strong>核心考点</strong>：本地事务 + 本地消息表 + 定时任务重试，适合高可用场景。</p><p><strong>背诵话术</strong>：</p><blockquote><p>可靠消息最终一致性是分布式事务中最常用的柔性方案，不需要两阶段提交，适合对实时一致性要求不高、但对可用性要求高的业务场景。</p></blockquote><blockquote><p><strong>本地消息表方案（RocketMQ 事务消息同理）</strong>：</p><ol><li><strong>业务方（生产者）</strong>：在自己的数据库中，将<strong>业务操作</strong>和<strong>消息记录</strong>放在同一个本地事务中完成（如”创建订单” + “写入一条 MQ 消息记录”）。本地事务保证要么都成功要么都失败。</li><li><strong>消息发送</strong>：定时任务轮询本地消息表中状态为”未发送”的记录，将消息投递到 MQ。发送成功后将记录状态更新为”已发送”。</li><li><strong>消费方</strong>：消费消息，正常处理业务逻辑。消费完成后提交 offset。如果消费失败，MQ 重投或死信队列兜底。消费者必须有<strong>幂等性</strong>防止重复消费。</li><li><strong>补偿检查</strong>：定时任务扫描超时未确认的消息（已发送但未消费或消费失败），进行人工处理或补偿。</li></ol></blockquote><blockquote><p><strong>在招行调度中台的应用</strong>：流程调度中的回调通知场景使用了可靠消息方案（不是金融转账核心场景，那个才用 TCC）。流程执行完成后，在本地事务中同时写入回调消息记录，后台定时任务投递到 Kafka，下游系统幂等消费。这套方案保证了即使回调系统暂时不可用（网络闪断），消息也不会丢失，最大程度保证了流程状态的最终一致性。</p></blockquote><hr><h3 id="Q16：2PC-3PC-TCC-Saga-可靠消息-五大方案对比"><a href="#Q16：2PC-3PC-TCC-Saga-可靠消息-五大方案对比" class="headerlink" title="Q16：2PC &#x2F; 3PC &#x2F; TCC &#x2F; Saga &#x2F; 可靠消息 五大方案对比"></a>Q16：2PC &#x2F; 3PC &#x2F; TCC &#x2F; Saga &#x2F; 可靠消息 五大方案对比</h3><p><strong>核心考点</strong>：各方案的一致性、性能、业务侵入性的权衡。</p><p><strong>背诵话术</strong>：</p><blockquote><table><thead><tr><th>方案</th><th>一致性</th><th>性能</th><th>业务侵入</th><th>适用场景</th></tr></thead><tbody><tr><td><strong>2PC（XA）</strong></td><td>强一致</td><td>差（全程锁资源）</td><td>低（数据库直接支持）</td><td>传统单体应用、并发低场景</td></tr><tr><td><strong>3PC</strong></td><td>强一致（比 2PC 略好）</td><td>差</td><td>低</td><td>基本不用（2PC 的改良版，引入超时机制防阻塞），成本高，生态差</td></tr><tr><td><strong>TCC</strong></td><td>最终强一致</td><td>好（不长期锁资源）</td><td>高（需实现 Try&#x2F;Confirm&#x2F;Cancel）</td><td>金融核心交易、库存扣减</td></tr><tr><td><strong>Saga</strong></td><td>最终一致</td><td>好（无锁）</td><td>中（需实现正向+补偿）</td><td>长流程业务、微服务编排</td></tr><tr><td><strong>可靠消息最终一致</strong></td><td>最终一致（可能有短暂窗口）</td><td>最好（完全异步）</td><td>低（只需发消息+幂等消费）</td><td>通知类、回调类、解耦场景</td></tr></tbody></table></blockquote><blockquote><p>选型要点：核心金融资金用 TCC（强一致 + 高性能），通知回调用可靠消息（高吞吐、低耦合），跨多微服务的复杂长流程可以考虑 Saga（每个参与者独立提交 + 补偿回滚），2PC 基本不用于分布式场景（锁资源太久，性能太差）。</p><p>在招行项目中，资金流转使用 TCC 模式（如节点流程执行中的状态扣减、冻结），回调通知下游使用可靠消息最终一致性方案（异步通知 + Kafka 投递 + 幂等消费）。</p></blockquote><hr><h3 id="Q17：Feign-的超时、重试与负载均衡原理？"><a href="#Q17：Feign-的超时、重试与负载均衡原理？" class="headerlink" title="Q17：Feign 的超时、重试与负载均衡原理？"></a>Q17：Feign 的超时、重试与负载均衡原理？</h3><p><strong>核心考点</strong>：声明式客户端、Feign 的调用执行流程、超时配置冲突、Ribbon &#x2F; LoadBalancer 负载均衡原理。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Feign 工作原理</strong>：<br>Feign 是一个声明式的 HTTP 客户端。通过在接口上添加 <code>@FeignClient</code> 注解，Spring 容器启动时，会为该接口生成 JDK 动态代理对象（对应的 Handler 为 <code>FeignInvocationHandler</code>）。当调用接口方法时，代理对象拦截请求，通过 <code>SynchronousMethodHandler</code> 的 <code>executeAndDecode()</code> 方法进行处理：将方法参数通过 <code>Encoder</code> 序列化为 HTTP 请求体，生成一个 <code>Request</code> 对象，接着经过负载均衡器选择实例，交由底层的 <code>Client</code>（如 OkHttp&#x2F;HttpClient&#x2F;HttpURLConnection）发送 HTTP 请求，最后通过 <code>Decoder</code> 反序列化为接口返回值。</p><p><strong>超时与重试</strong>：<br>Feign 内部默认有重试机制（默认不启用或重试 5 次，但微服务组件一般关闭它），超时默认是 10s（Read）和 20s（Connect）。<br><strong>在招行分布式环境的超时干预中</strong>：<br>我们一般关闭 Feign 的默认重试，并将超时控制权交给 Ribbon（或 Spring Cloud LoadBalancer），通过如下配置进行精细干预：</p><figure class="highlight yaml"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="attr">ribbon:</span></span><br><span class="line">  <span class="attr">ReadTimeout:</span> <span class="number">2000</span>      <span class="comment"># 业务处理超时时间 2s</span></span><br><span class="line">  <span class="attr">ConnectTimeout:</span> <span class="number">1000</span>   <span class="comment"># 连接超时时间 1s</span></span><br><span class="line">  <span class="attr">MaxAutoRetries:</span> <span class="number">0</span>      <span class="comment"># 同一实例重试次数，设为 0</span></span><br><span class="line">  <span class="attr">MaxAutoRetriesNextServer:</span> <span class="number">1</span>  <span class="comment"># 切换实例重试次数，设为 1</span></span><br></pre></td></tr></table></figure><p>对于写操作等非幂等接口，需关闭重试，以防止网络抖动导致的重复提交。</p><p><strong>负载均衡机制</strong>：<br>Feign 会集成 Ribbon（或 Spring Cloud LoadBalancer）。Ribbon 从注册中心（如 Nacos）获取服务实例列表，本地维护一个缓存，并在发起请求时通过负载均衡算法（如 <code>RoundRobinRule</code> 轮询、<code>RandomRule</code> 随机、<code>BestAvailableRule</code> 最小并发数）计算出一个健康的实例 IP 和端口，最后拦截 Feign 的 Request 并重写 URL 进行请求。</p></blockquote><hr><h3 id="Q18：Spring-Cloud-Gateway-核心架构与应用？"><a href="#Q18：Spring-Cloud-Gateway-核心架构与应用？" class="headerlink" title="Q18：Spring Cloud Gateway 核心架构与应用？"></a>Q18：Spring Cloud Gateway 核心架构与应用？</h3><p><strong>核心考点</strong>：Gateway vs Zuul 1.x、网关三大核心概念（Route, Predicate, Filter）、自定义拦截器鉴权与限流。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Gateway vs Zuul 1.x</strong>：</p><ul><li>Zuul 1.x 基于 Servlet 2.5 构建，是<strong>同步阻塞 I&#x2F;O</strong> 架构。每个请求占用一个线程，在高并发场景下极易耗尽线程池。</li><li>Spring Cloud Gateway 基于 Spring WebFlux 和 Netty 构建，是<strong>异步非阻塞</strong>（Reactive）架构，使用极少量的线程就能处理大量并发请求，吞吐量和并发性能大幅领先。</li></ul><p><strong>核心概念</strong>：</p><ol><li><strong>Route（路由）</strong>：网关的基本模块，由 ID、目标 URI、断言集合、过滤器集合组成。</li><li><strong>Predicate（断言）</strong>：Java 8 的 Predicate 接口，用于匹配请求属性（如 Path 路径、Method 方法、Header 请求头等）。</li><li><strong>Filter（过滤器）</strong>：GatewayFilter 实例，在发送请求到下游服务之前（pre）或之后（post）修改请求&#x2F;响应。</li></ol><p><strong>自定义 Filter 拦截器实战</strong>：<br>在我们医博系统和 OpenAPI 开放平台中，我们自定义了 <code>GlobalFilter</code> 和 <code>GatewayFilterFactory</code> 实现如下功能：</p><ul><li><strong>统一鉴权</strong>：在 <code>pre</code> 阶段，从请求头获取 JWT Token，进行合法性校验、解析并将解析出的 userId&#x2F;role 信息写入 <code>ServerHttpRequest</code> 的 Header 中传递给下游微服务。若无效，直接将 Http 状态码设为 401 并结束请求。</li><li><strong>分布式限流</strong>：使用 Gateway 的 <code>RequestRateLimiter</code> 过滤器，结合 Redis 和 Lua 脚本实现令牌桶限流算法。针对不同 API 或不同租户设定不同的 Rate 和 Capacity。</li><li><strong>灰度发布&#x2F;路由干预</strong>：根据请求头中的特定版本号（如 <code>x-version: v2</code>）动态修改 <code>ServerWebExchange</code> 的路由目标，实现金丝雀&#x2F;灰度流量分流。</li></ul></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> Spring </tag>
            
            <tag> 微服务 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>MySQL数据库面试考题</title>
      <link href="/2026/06/08/MySQL%E6%95%B0%E6%8D%AE%E5%BA%93%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/MySQL%E6%95%B0%E6%8D%AE%E5%BA%93%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="03-MySQL数据库-·-高频面试考题背诵指南"><a href="#03-MySQL数据库-·-高频面试考题背诵指南" class="headerlink" title="03 - MySQL数据库 · 高频面试考题背诵指南"></a>03 - MySQL数据库 · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1b%E6%A0%91%E4%B8%BA%E4%BB%80%E4%B9%88%E6%AF%94-b%E6%A0%91%E5%93%88%E5%B8%8C%E8%A1%A8%E6%9B%B4%E9%80%82%E5%90%88%E5%81%9A%E6%95%B0%E6%8D%AE%E5%BA%93%E7%B4%A2%E5%BC%95">Q1：B+树为什么比 B树、哈希表更适合做数据库索引？</a></li><li><a href="#q2%E8%81%9A%E7%B0%87%E7%B4%A2%E5%BC%95%E5%92%8C%E9%9D%9E%E8%81%9A%E7%B0%87%E7%B4%A2%E5%BC%95%E7%9A%84%E5%8C%BA%E5%88%AB%E4%BB%80%E4%B9%88%E6%98%AF%E5%9B%9E%E8%A1%A8">Q2：聚簇索引和非聚簇索引的区别？什么是回表？</a></li><li><a href="#q3%E4%BB%80%E4%B9%88%E6%98%AF%E8%A6%86%E7%9B%96%E7%B4%A2%E5%BC%95%E5%A6%82%E4%BD%95%E9%81%BF%E5%85%8D%E5%9B%9E%E8%A1%A8%E7%BB%93%E5%90%88%E7%BE%8E%E5%9B%A2%E9%A1%B9%E7%9B%AE%E6%A1%88%E4%BE%8B">Q3：什么是覆盖索引？如何避免回表？（结合美团项目案例）</a></li><li><a href="#q4%E8%81%94%E5%90%88%E7%B4%A2%E5%BC%95%E7%9A%84%E6%9C%80%E5%B7%A6%E5%89%8D%E7%BC%80%E5%8E%9F%E5%88%99%E4%BB%80%E4%B9%88%E6%83%85%E5%86%B5%E4%BC%9A%E5%AF%BC%E8%87%B4%E7%B4%A2%E5%BC%95%E5%A4%B1%E6%95%88">Q4：联合索引的最左前缀原则？什么情况会导致索引失效？</a></li><li><a href="#q5%E7%B4%A2%E5%BC%95%E5%A4%B1%E6%95%88%E7%9A%84%E5%B8%B8%E8%A7%81%E5%9C%BA%E6%99%AF8%E7%A7%8D">Q5：索引失效的常见场景（8种）</a></li><li><a href="#q6explain-%E5%90%84%E5%AD%97%E6%AE%B5%E5%90%AB%E4%B9%89type-%E5%AD%97%E6%AE%B5%E6%80%A7%E8%83%BD%E6%8E%92%E5%BA%8F">Q6：Explain 各字段含义？type 字段性能排序？</a></li><li><a href="#q7mysql-%E5%9B%9B%E7%A7%8D%E4%BA%8B%E5%8A%A1%E9%9A%94%E7%A6%BB%E7%BA%A7%E5%88%AB">Q7：MySQL 四种事务隔离级别</a></li><li><a href="#q8innodb-mvcc-%E5%8E%9F%E7%90%86%E8%AF%A6%E8%A7%A3">Q8：InnoDB MVCC 原理详解</a></li><li><a href="#q9innodb-%E7%9A%84%E9%94%81%E7%B1%BB%E5%9E%8B%E8%AF%A6%E8%A7%A3">Q9：InnoDB 的锁类型详解</a></li><li><a href="#q10mysql-%E6%AD%BB%E9%94%81%E5%A6%82%E4%BD%95%E6%8E%92%E6%9F%A5%E5%92%8C%E9%A2%84%E9%98%B2">Q10：MySQL 死锁如何排查和预防？</a></li><li><a href="#q11%E4%B9%90%E8%A7%82%E9%94%81%E5%92%8C%E6%82%B2%E8%A7%82%E9%94%81%E7%89%88%E6%9C%AC%E5%8F%B7%E6%9C%BA%E5%88%B6%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E4%B9%90%E8%A7%82%E9%94%81">Q11：乐观锁和悲观锁？版本号机制如何实现乐观锁？</a></li><li><a href="#q12mysql-%E6%B7%B1%E5%88%86%E9%A1%B5limit-1000000-10%E4%B8%BA%E4%BB%80%E4%B9%88%E6%85%A2%E5%A6%82%E4%BD%95%E4%BC%98%E5%8C%96">Q12：MySQL 深分页（LIMIT 1000000, 10）为什么慢？如何优化？</a></li><li><a href="#q13%E7%99%BE%E4%B8%87%E7%BA%A7%E5%A4%A7%E8%A1%A8%E5%88%86%E5%BA%93%E5%88%86%E8%A1%A8%E7%AD%96%E7%95%A5">Q13：百万级大表分库分表策略</a></li><li><a href="#q14mysql-%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6%E5%8E%9F%E7%90%86binlog-%E4%B8%89%E7%A7%8D%E6%A0%BC%E5%BC%8F%E7%9A%84%E5%8C%BA%E5%88%AB">Q14：MySQL 主从复制原理？Binlog 三种格式的区别？</a></li><li><a href="#q15%E4%B8%80%E6%9D%A1-sql-%E8%AF%AD%E5%8F%A5%E5%9C%A8-mysql-%E4%B8%AD%E7%9A%84%E5%AE%8C%E6%95%B4%E6%89%A7%E8%A1%8C%E8%BF%87%E7%A8%8B">Q15：一条 SQL 语句在 MySQL 中的完整执行过程</a></li></ul><hr><span id="more"></span><h2 id="一、索引原理"><a href="#一、索引原理" class="headerlink" title="一、索引原理"></a>一、索引原理</h2><h3 id="Q1：B-树为什么比-B树、哈希表更适合做数据库索引？"><a href="#Q1：B-树为什么比-B树、哈希表更适合做数据库索引？" class="headerlink" title="Q1：B+树为什么比 B树、哈希表更适合做数据库索引？"></a>Q1：B+树为什么比 B树、哈希表更适合做数据库索引？</h3><p><strong>核心考点</strong>：B+树的叶子节点存数据、非叶子节点只存索引、支持范围查询。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>对比哈希表</strong>：哈希表等值查询是 O(1)，但完全不支持范围查询（<code>BETWEEN</code>、<code>&gt;</code>、<code>&lt;</code>、<code>ORDER BY</code>），而数据库中范围查询非常常见，所以哈希索引只能作为特定场景的补充（如 Memory 引擎）。</p><p><strong>对比 B 树</strong>：B 树的非叶子节点和叶子节点都存储了实际数据，导致单个节点能存储的索引键数量变少，树的高度更高，磁盘 IO 次数更多。更关键的是，B 树不支持高效的范围扫描，因为数据分散在各层节点中，需要中序遍历。</p><p><strong>B+树的优势</strong>：</p><ol><li><strong>非叶子节点只存键值不存数据</strong>，单个页能存储更多键，树高度更低（通常 3~4 层就能存储千万级数据），磁盘 IO 次数更少。</li><li><strong>所有数据都存储在叶子节点</strong>，且叶子节点通过双向链表相连，支持高效的范围扫描，直接在叶子节点链表上顺序遍历即可，无需回头查非叶子节点。</li><li><strong>查询性能稳定</strong>：任何一次查询都需要从根节点走到叶子节点，路径长度固定，性能稳定。</li></ol></blockquote><hr><h3 id="Q2：聚簇索引和非聚簇索引的区别？什么是回表？"><a href="#Q2：聚簇索引和非聚簇索引的区别？什么是回表？" class="headerlink" title="Q2：聚簇索引和非聚簇索引的区别？什么是回表？"></a>Q2：聚簇索引和非聚簇索引的区别？什么是回表？</h3><p><strong>核心考点</strong>：聚簇索引叶子节点存行数据，非聚簇索引叶子节点存主键值，回表的代价。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>聚簇索引（主键索引）</strong>：InnoDB 的表数据本身就是按照主键顺序组织的 B+树，叶子节点存储的是<strong>完整的行数据</strong>。每张 InnoDB 表有且只有一个聚簇索引（优先使用主键，没有主键则选第一个非空唯一索引，都没有则 InnoDB 自动生成一个隐藏的 rowid 作为主键）。</p><p><strong>非聚簇索引（二级索引&#x2F;辅助索引）</strong>：叶子节点存储的是<strong>索引列的值 + 对应行的主键值</strong>，并不包含完整的行数据。</p><p><strong>回表（Table Lookup）</strong>：当通过二级索引查询数据时，先在二级索引的 B+树中找到对应的主键值，再拿这个主键值去聚簇索引（主键索引）中再查一次，才能获取完整的行数据。这”两次 B+树查询”的过程就叫做回表，会增加磁盘 IO 开销。回表是大数据量下影响性能的重要因素。</p></blockquote><hr><h3 id="Q3：什么是覆盖索引？如何避免回表？（结合美团项目案例）"><a href="#Q3：什么是覆盖索引？如何避免回表？（结合美团项目案例）" class="headerlink" title="Q3：什么是覆盖索引？如何避免回表？（结合美团项目案例）"></a>Q3：什么是覆盖索引？如何避免回表？（结合美团项目案例）</h3><p><strong>核心考点</strong>：索引覆盖查询所需的所有列，Using index，消除回表。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>覆盖索引</strong>是指一个查询所需要的所有列（SELECT 的列、WHERE 的列、ORDER BY 的列）都包含在某一个索引中，查询引擎直接在该索引的 B+树上就能获取全部所需数据，<strong>不需要回表</strong>。<code>Explain</code> 的 <code>Extra</code> 字段显示 <code>Using index</code> 即说明用到了覆盖索引。</p><p><strong>美团项目实战案例</strong>：在美团 SaaS 管家后台，商户对账列表接口有一个高频查询：按 <code>shop_id</code>（门店ID）+ <code>status</code>（状态）过滤，并按 <code>create_time</code> 排序，最后取若干展示字段。原始 SQL 用了多表 <code>Left Join</code> 并且字段全是 <code>SELECT *</code>。</p><p><strong>优化过程</strong>：</p><ol><li>把 3 个 <code>Left Join</code> 拆成独立的单表查询，在 Java 中通过 Stream + Map 做内存聚合。</li><li>对主查询表建立联合索引：<code>idx_shop_status_time(shop_id, status, create_time)</code>，并严格只查询需要展示的列（而不是 <code>SELECT *</code>）。</li><li>调整后，<code>Explain</code> 显示 <code>type = ref</code>（按索引查找），<code>Extra = Using index</code>（命中覆盖索引，无需回表）。最终接口响应时间从 3.2s 降至 0.4s，CPU 负载大幅下降。</li></ol></blockquote><hr><h3 id="Q4：联合索引的最左前缀原则？什么情况会导致索引失效？"><a href="#Q4：联合索引的最左前缀原则？什么情况会导致索引失效？" class="headerlink" title="Q4：联合索引的最左前缀原则？什么情况会导致索引失效？"></a>Q4：联合索引的最左前缀原则？什么情况会导致索引失效？</h3><p><strong>核心考点</strong>：联合索引的存储顺序，跳过中间列或范围查询之后的列会导致索引中断。</p><p><strong>背诵话术</strong>：</p><blockquote><p>联合索引 <code>(a, b, c)</code> 在 B+树中<strong>先按 a 排序，a 相同再按 b 排序，b 相同再按 c 排序</strong>。</p><p><strong>最左前缀原则</strong>：查询时，必须从联合索引的最左列开始匹配，且不能跳过中间的列。以 <code>(a, b, c)</code> 为例：</p><ul><li>✅ <code>WHERE a = 1</code>：命中（使用 a）</li><li>✅ <code>WHERE a = 1 AND b = 2</code>：命中（使用 a, b）</li><li>✅ <code>WHERE a = 1 AND b = 2 AND c = 3</code>：命中（使用 a, b, c）</li><li>✅ <code>WHERE a = 1 AND c = 3</code>：部分命中（只使用 a，c 无法利用索引）</li><li>❌ <code>WHERE b = 2 AND c = 3</code>：完全不命中（跳过了最左列 a）</li><li>⚠️ <code>WHERE a = 1 AND b &gt; 5 AND c = 3</code>：b 之后的 c 无法使用索引（范围查询之后的列索引中断）</li></ul></blockquote><hr><h3 id="Q5：索引失效的常见场景（8种）"><a href="#Q5：索引失效的常见场景（8种）" class="headerlink" title="Q5：索引失效的常见场景（8种）"></a>Q5：索引失效的常见场景（8种）</h3><p><strong>核心考点</strong>：需要背熟的 8 种会导致全表扫描的 SQL 写法。</p><p><strong>背诵话术</strong>：</p><blockquote><p>以下场景会导致索引失效，退化为全表扫描（<code>type = ALL</code>）：</p><ol><li><strong>对索引列使用函数操作</strong>：<code>WHERE YEAR(create_time) = 2024</code>，应改为 <code>WHERE create_time BETWEEN &#39;2024-01-01&#39; AND &#39;2024-12-31&#39;</code>。</li><li><strong>隐式类型转换</strong>：字段类型为 <code>VARCHAR</code>，但查询时传的是整数 <code>WHERE phone = 13800138000</code>（应加引号），MySQL 会对列做隐式转换，导致索引失效。</li><li><strong><code>LIKE &#39;%xxx&#39;</code>（前导通配符）</strong>：<code>LIKE &#39;%abc&#39;</code> 无法利用 B+树索引，<code>LIKE &#39;abc%&#39;</code> 可以利用。</li><li><strong><code>OR</code> 条件中有一列没有索引</strong>：<code>WHERE a = 1 OR b = 2</code>，如果 b 没有索引，整个查询走全表扫描。</li><li><strong><code>!=</code>、<code>&lt;&gt;</code>、<code>NOT IN</code>、<code>NOT EXISTS</code></strong>：这类反向查询通常无法使用索引（MySQL 优化器判断全表扫描成本更低时会放弃索引）。</li><li><strong><code>IS NULL</code> &#x2F; <code>IS NOT NULL</code></strong>：<code>IS NULL</code> 可能走索引，<code>IS NOT NULL</code> 通常不走，取决于优化器。</li><li><strong>范围查询之后的联合索引列</strong>：<code>WHERE a = 1 AND b &gt; 5 AND c = 3</code>，b 使用范围查询后，c 的索引失效。</li><li><strong>索引选择性太低</strong>：如性别字段只有”男&#x2F;女”两个值，索引选择性极低，MySQL 优化器会判断全表扫描更快而放弃索引。</li></ol></blockquote><hr><h3 id="Q6：Explain-各字段含义？type-字段性能排序？"><a href="#Q6：Explain-各字段含义？type-字段性能排序？" class="headerlink" title="Q6：Explain 各字段含义？type 字段性能排序？"></a>Q6：Explain 各字段含义？type 字段性能排序？</h3><p><strong>核心考点</strong>：<code>type</code>、<code>key</code>、<code>rows</code>、<code>Extra</code> 四个最重要的字段的含义。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>Explain</code> 最重要的字段：</p><ul><li><p><strong><code>type</code>（访问类型，性能由好到差）</strong>：<br><code>system &gt; const &gt; eq_ref &gt; ref &gt; range &gt; index &gt; ALL</code></p><ul><li><code>const</code>：主键或唯一索引等值查询，最多匹配 1 行。</li><li><code>ref</code>：非唯一索引等值查询，匹配多行。</li><li><code>range</code>：范围查询，如 <code>BETWEEN</code>、<code>&gt;</code>、<code>&lt;</code>、<code>IN</code>。</li><li><code>index</code>：全索引扫描（比全表扫描省磁盘IO，但仍是全扫描）。</li><li><code>ALL</code>：<strong>全表扫描，需要立即优化！</strong></li></ul></li><li><p><strong><code>key</code></strong>：实际使用的索引名称，<code>NULL</code> 说明没有用索引。</p></li><li><p><strong><code>rows</code></strong>：估算需要扫描的行数，越小越好。</p></li><li><p><strong><code>Extra</code></strong>（最重要的诊断字段）：</p><ul><li><code>Using index</code>：✅ 命中覆盖索引，无需回表，最佳。</li><li><code>Using where</code>：在 Server 层对数据进行过滤（索引可能只是范围扫描）。</li><li><code>Using filesort</code>：⚠️ 需要额外的排序操作（在内存或磁盘中排序），说明 <code>ORDER BY</code> 没有利用索引，需优化。</li><li><code>Using temporary</code>：⚠️ 使用了临时表，通常在 <code>GROUP BY</code> 或 <code>DISTINCT</code> 时出现，开销较大。</li><li><code>Using index condition</code>：索引条件下推（ICP），在存储引擎层就过滤数据，减少回表次数。</li></ul></li></ul></blockquote><hr><h2 id="二、事务与-MVCC"><a href="#二、事务与-MVCC" class="headerlink" title="二、事务与 MVCC"></a>二、事务与 MVCC</h2><h3 id="Q7：MySQL-四种事务隔离级别"><a href="#Q7：MySQL-四种事务隔离级别" class="headerlink" title="Q7：MySQL 四种事务隔离级别"></a>Q7：MySQL 四种事务隔离级别</h3><p><strong>核心考点</strong>：从低到高，各级别解决的问题，RR 是 MySQL 默认级别。</p><p><strong>背诵话术</strong>：</p><blockquote><table><thead><tr><th>隔离级别</th><th>脏读</th><th>不可重复读</th><th>幻读</th></tr></thead><tbody><tr><td>读未提交（Read Uncommitted）</td><td>❌ 存在</td><td>❌ 存在</td><td>❌ 存在</td></tr><tr><td>读已提交（Read Committed，RC）</td><td>✅ 解决</td><td>❌ 存在</td><td>❌ 存在</td></tr><tr><td>可重复读（Repeatable Read，RR）</td><td>✅ 解决</td><td>✅ 解决</td><td>⚠️ 基本解决</td></tr><tr><td>串行化（Serializable）</td><td>✅ 解决</td><td>✅ 解决</td><td>✅ 解决</td></tr></tbody></table><ul><li><strong>脏读</strong>：读到其他事务未提交的数据。</li><li><strong>不可重复读</strong>：同一事务内，两次读同一行数据结果不同（其他事务 UPDATE 后提交）。</li><li><strong>幻读</strong>：同一事务内，两次范围查询结果的行数不同（其他事务 INSERT 后提交）。</li></ul><p><strong>MySQL InnoDB 默认隔离级别是 RR</strong>。InnoDB 通过 MVCC 解决了不可重复读，通过 Next-Key Lock（临键锁）在大多数场景下防止了幻读，因此 RR 级别下幻读被”基本解决”。</p></blockquote><hr><h3 id="Q8：InnoDB-MVCC-原理详解"><a href="#Q8：InnoDB-MVCC-原理详解" class="headerlink" title="Q8：InnoDB MVCC 原理详解"></a>Q8：InnoDB MVCC 原理详解</h3><p><strong>核心考点</strong>：隐藏字段（trx_id + roll_pointer）、Undo Log 版本链、ReadView 可见性算法、RC 和 RR 的区别。</p><p><strong>背诵话术</strong>：</p><blockquote><p>MVCC 是 InnoDB 实现”读不加锁、读写不冲突”的核心机制，它依赖三个组件：</p><p><strong>1. 隐藏字段</strong>：每行数据有两个隐藏字段：</p><ul><li><code>trx_id</code>：最后一次修改该行的事务 ID。</li><li><code>roll_pointer</code>：回滚指针，指向 Undo Log 中该行的上一个版本。</li></ul><p><strong>2. Undo Log 版本链</strong>：每次对行数据进行修改，旧版本会被保存到 Undo Log 中，多次修改会形成一条<strong>版本链</strong>，链头是最新版本，链尾是最旧版本，通过 <code>roll_pointer</code> 串联。</p><p><strong>3. ReadView（读视图）</strong>：事务发起快照读时生成，记录了：</p><ul><li><code>creator_trx_id</code>：当前事务 ID。</li><li><code>m_ids</code>：生成 ReadView 时，所有<strong>活跃（已开启但未提交）的事务 ID 列表</strong>。</li><li><code>min_trx_id</code>：m_ids 中的最小值。</li><li><code>max_trx_id</code>：下一个将要分配的事务 ID（即当前最大 trx_id + 1）。</li></ul><p><strong>可见性判断规则</strong>：沿着版本链从最新版本往旧版本遍历，对每个版本的 <code>trx_id</code> 进行判断：</p><ul><li><code>trx_id == creator_trx_id</code>：自己修改的，<strong>可见</strong>。</li><li><code>trx_id &lt; min_trx_id</code>：在 ReadView 生成前就已提交，<strong>可见</strong>。</li><li><code>trx_id &gt;= max_trx_id</code>：在 ReadView 生成后才开启的事务，<strong>不可见</strong>。</li><li><code>min_trx_id &lt;= trx_id &lt; max_trx_id</code>：如果 trx_id 在 m_ids 中，说明该事务未提交，<strong>不可见</strong>；不在 m_ids 中，说明已提交，<strong>可见</strong>。</li></ul><p><strong>RC 和 RR 的区别</strong>：</p><ul><li><strong>RC（读已提交）</strong>：每次执行 SELECT 都重新生成一个新的 ReadView，所以能读到其他事务已提交的最新修改 → 存在不可重复读。</li><li><strong>RR（可重复读）</strong>：在事务内<strong>只在第一次</strong> SELECT 时生成 ReadView，后续所有查询复用同一个 ReadView → 同一事务内读取结果一致，解决了不可重复读。</li></ul></blockquote><hr><h2 id="三、锁机制"><a href="#三、锁机制" class="headerlink" title="三、锁机制"></a>三、锁机制</h2><h3 id="Q9：InnoDB-的锁类型详解"><a href="#Q9：InnoDB-的锁类型详解" class="headerlink" title="Q9：InnoDB 的锁类型详解"></a>Q9：InnoDB 的锁类型详解</h3><p><strong>核心考点</strong>：各种锁的定义和加锁条件，间隙锁防幻读原理。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>按锁的粒度分</strong>：</p><ul><li><strong>表级锁</strong>：开销小，加锁快，并发度低。DDL 操作会加元数据锁（MDL）。MyISAM 只有表锁，InnoDB 在特定条件下也会退化为表锁。</li><li><strong>行级锁</strong>：开销大，加锁慢，并发度高。InnoDB 的行锁是加在<strong>索引上</strong>的，如果 <code>WHERE</code> 条件没有命中索引，会退化为表锁！</li></ul><p><strong>InnoDB 行锁的三种类型</strong>：</p><ul><li><strong>记录锁（Record Lock）</strong>：锁住某一条具体的记录（索引项）。<code>SELECT ... FOR UPDATE</code> 精确命中某行时使用。</li><li><strong>间隙锁（Gap Lock）</strong>：锁住两个索引记录之间的<strong>空隙</strong>，防止其他事务在这个区间内 INSERT 新记录，解决幻读问题。间隙锁之间不冲突，多个事务可以同时持有同一间隙的间隙锁，但间隙锁与 INSERT 意图锁冲突，可能导致死锁。</li><li><strong>临键锁（Next-Key Lock）</strong>：&#x3D; 记录锁 + 间隙锁，锁住一条记录以及该记录之前的间隙，是 InnoDB 在 RR 级别下的默认行锁算法。</li></ul><p><strong>意向锁</strong>：表级锁，由 InnoDB 自动管理。事务加行锁前先加意向锁，让其他事务判断表上是否有行锁时无需逐行检查，提高加表锁的效率。</p></blockquote><hr><h3 id="Q10：MySQL-死锁如何排查和预防？"><a href="#Q10：MySQL-死锁如何排查和预防？" class="headerlink" title="Q10：MySQL 死锁如何排查和预防？"></a>Q10：MySQL 死锁如何排查和预防？</h3><p><strong>核心考点</strong>：<code>SHOW ENGINE INNODB STATUS</code> 查看死锁日志，预防策略。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>排查死锁</strong>：执行 <code>SHOW ENGINE INNODB STATUS\G</code>，在输出的 <code>LATEST DETECTED DEADLOCK</code> 段落中，可以看到发生死锁的事务信息、它们持有和等待的锁，以及 MySQL 选择回滚的事务（通常是回滚代价较小的）。也可以通过 <code>information_schema.INNODB_LOCK_WAITS</code> 和 <code>INNODB_TRX</code> 表查询当前的锁等待情况。</p><p><strong>预防死锁的策略</strong>：</p><ol><li><strong>统一加锁顺序</strong>：多个事务如果需要锁定多个资源，确保始终以相同的顺序加锁，从根本上打破循环等待。</li><li><strong>缩短事务持锁时间</strong>：尽量减少事务的执行时间，减少持锁窗口，降低锁冲突概率。</li><li><strong>为 WHERE 条件加索引</strong>：确保更新&#x2F;删除操作命中索引，使用行锁而不是退化为表锁，减小锁的粒度。</li><li><strong>合理使用 SELECT … FOR UPDATE</strong>：尽量在事务最开始时就获取所有需要的锁（一次性加锁），而不是在事务中途分散加锁。</li><li><strong>使用乐观锁替代悲观锁</strong>：对于读多写少的场景，乐观锁（版本号）可以避免大量加锁。</li></ol></blockquote><hr><h3 id="Q11：乐观锁和悲观锁？版本号机制如何实现乐观锁？"><a href="#Q11：乐观锁和悲观锁？版本号机制如何实现乐观锁？" class="headerlink" title="Q11：乐观锁和悲观锁？版本号机制如何实现乐观锁？"></a>Q11：乐观锁和悲观锁？版本号机制如何实现乐观锁？</h3><p><strong>核心考点</strong>：两种锁的思想差异，乐观锁的 CAS 实现，适用场景。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><strong>悲观锁</strong>：悲观地认为并发冲突必然发生，每次操作数据前都先加锁。适用于写操作多、并发冲突频繁的场景，如 <code>SELECT ... FOR UPDATE</code>。代价是高并发下锁等待时间长，吞吐量低。</li><li><strong>乐观锁</strong>：乐观地认为并发冲突不常发生，操作数据时不加锁，而是在提交时检测是否有冲突，有则重试。适用于读多写少、并发冲突较少的场景。</li></ul><p><strong>版本号机制实现乐观锁</strong>：在表中增加一个 <code>version</code> 字段。读取数据时把 <code>version</code> 一并读出；更新时在 SQL 中加上 <code>WHERE id = ? AND version = ?</code>，同时将 <code>version + 1</code>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line"><span class="keyword">UPDATE</span> orders <span class="keyword">SET</span> status <span class="operator">=</span> <span class="string">&#x27;PAID&#x27;</span>, version <span class="operator">=</span> version <span class="operator">+</span> <span class="number">1</span></span><br><span class="line"><span class="keyword">WHERE</span> id <span class="operator">=</span> <span class="number">123</span> <span class="keyword">AND</span> version <span class="operator">=</span> <span class="number">5</span>;</span><br></pre></td></tr></table></figure><p>如果 <code>UPDATE</code> 影响的行数为 0，说明在我读取和更新之间，有其他事务已经修改了该行（version 变了），此时触发重试或抛出业务异常。在医博系统的号源扣减场景中，我们就使用了 <code>WHERE stock &gt; 0 AND version = ?</code> 的乐观锁方式，防止超卖。</p></blockquote><hr><h2 id="四、SQL-优化与大表处理"><a href="#四、SQL-优化与大表处理" class="headerlink" title="四、SQL 优化与大表处理"></a>四、SQL 优化与大表处理</h2><h3 id="Q12：MySQL-深分页（LIMIT-1000000-10）为什么慢？如何优化？"><a href="#Q12：MySQL-深分页（LIMIT-1000000-10）为什么慢？如何优化？" class="headerlink" title="Q12：MySQL 深分页（LIMIT 1000000, 10）为什么慢？如何优化？"></a>Q12：MySQL 深分页（LIMIT 1000000, 10）为什么慢？如何优化？</h3><p><strong>核心考点</strong>：LIMIT offset 需要扫描前 N 行再丢弃，延迟关联（子查询先取主键）优化方案。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>LIMIT 1000000, 10</code> 慢的原因：MySQL 执行时会先从索引或全表中顺序扫描并跳过前 100 万行，再取第 100 万零 1 到 100 万零 10 行返回。虽然最终只返回 10 条，但前 100 万行的扫描（包括可能的回表）是实实在在发生的，IO 开销极大。</p><p><strong>优化方案一：延迟关联（子查询先取主键）</strong>：</p><figure class="highlight sql"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- 优化前（慢）</span></span><br><span class="line"><span class="keyword">SELECT</span> <span class="operator">*</span> <span class="keyword">FROM</span> orders <span class="keyword">ORDER</span> <span class="keyword">BY</span> create_time LIMIT <span class="number">1000000</span>, <span class="number">10</span>;</span><br><span class="line"></span><br><span class="line"><span class="comment">-- 优化后（快）：先通过覆盖索引拿主键，再用主键关联取完整数据</span></span><br><span class="line"><span class="keyword">SELECT</span> o.<span class="operator">*</span> <span class="keyword">FROM</span> orders o</span><br><span class="line"><span class="keyword">INNER</span> <span class="keyword">JOIN</span> (</span><br><span class="line">    <span class="keyword">SELECT</span> id <span class="keyword">FROM</span> orders <span class="keyword">ORDER</span> <span class="keyword">BY</span> create_time LIMIT <span class="number">1000000</span>, <span class="number">10</span></span><br><span class="line">) t <span class="keyword">ON</span> o.id <span class="operator">=</span> t.id;</span><br></pre></td></tr></table></figure><p>子查询只查 <code>id</code>（覆盖索引，无需回表），速度极快；外层再用 10 个主键精确查询行数据。</p><p><strong>优化方案二：游标分页（推荐）</strong>：记录上一页最后一条记录的主键或游标值，下一页查询时直接用 <code>WHERE id &gt; last_id LIMIT 10</code>，彻底避免大 offset 扫描，是最优解。适合需要翻页的场景。</p></blockquote><hr><h3 id="Q13：百万级大表分库分表策略"><a href="#Q13：百万级大表分库分表策略" class="headerlink" title="Q13：百万级大表分库分表策略"></a>Q13：百万级大表分库分表策略</h3><p><strong>核心考点</strong>：垂直分表（按列拆）、水平分表（按行拆）、分片键选择、跨库查询问题。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>垂直分表（按列拆）</strong>：将一张宽表中不常用的大字段（如 <code>content</code>、<code>image_url</code> 等 BLOB&#x2F;TEXT 字段）拆分到另一张扩展表，主表只保留常用的热字段。优点是减小主表行宽，提高缓存效率，适合字段很多但实际使用时只需查询部分字段的场景。</p><p><strong>水平分表（按行拆）</strong>：将一张大表按照某个分片键（如 <code>user_id % 16</code> 或按时间范围）将数据分散到多个相同结构的表或库中。核心挑战是<strong>分片键的选择</strong>：</p><ul><li>分片键必须是高频查询条件，否则每次查询都要扫全部分片（扇出查询）。</li><li>分片键一旦确定，几乎不能修改，需要谨慎设计。</li><li>常用方案：取模分片（数据分布均匀，但扩容困难）、范围分片（便于归档，但容易导致热点）、一致性哈希（扩容时数据迁移量小）。</li></ul><p><strong>分库分表引入的新问题</strong>：跨库 JOIN 无法直接执行（需要业务层聚合）、分布式事务复杂化、全局唯一 ID 需要雪花算法等方案、分页和排序需要查询所有分片再汇总。</p></blockquote><hr><h2 id="五、MySQL-架构"><a href="#五、MySQL-架构" class="headerlink" title="五、MySQL 架构"></a>五、MySQL 架构</h2><h3 id="Q14：MySQL-主从复制原理？Binlog-三种格式的区别？"><a href="#Q14：MySQL-主从复制原理？Binlog-三种格式的区别？" class="headerlink" title="Q14：MySQL 主从复制原理？Binlog 三种格式的区别？"></a>Q14：MySQL 主从复制原理？Binlog 三种格式的区别？</h3><p><strong>核心考点</strong>：Binlog + Relay Log 的三步走，statement&#x2F;row&#x2F;mixed 三种格式的取舍。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>主从复制的三步流程</strong>：</p><ol><li>主库（Master）将所有 DDL&#x2F;DML 操作写入 <strong>Binlog 文件</strong>，并通过 dump 线程将 Binlog 推送给从库。</li><li>从库（Slave）的 <strong>IO 线程</strong>接收 Binlog，写入本地的 <strong>Relay Log（中继日志）</strong>。</li><li>从库的 <strong>SQL 线程</strong>读取 Relay Log，在本地重放执行，使从库数据与主库保持一致。</li></ol><p><strong>Binlog 三种格式</strong>：</p><ul><li><strong>STATEMENT</strong>：记录原始 SQL 语句。日志体积小，但某些包含函数（如 <code>NOW()</code>、<code>UUID()</code>）的 SQL 在从库重放时可能得到不同结果（数据不一致风险）。</li><li><strong>ROW</strong>（推荐）：记录每行数据的实际变更（变更前和变更后的值）。精确可靠，不存在数据不一致问题，但日志体积较大（大量更新时会膨胀）。Canal 解析 Binlog 时也要求使用 ROW 格式。</li><li><strong>MIXED</strong>：自动混合使用。普通 SQL 用 STATEMENT，遇到可能导致不一致的 SQL 时自动切换为 ROW 模式。</li></ul></blockquote><hr><h3 id="Q15：一条-SQL-语句在-MySQL-中的完整执行过程"><a href="#Q15：一条-SQL-语句在-MySQL-中的完整执行过程" class="headerlink" title="Q15：一条 SQL 语句在 MySQL 中的完整执行过程"></a>Q15：一条 SQL 语句在 MySQL 中的完整执行过程</h3><p><strong>核心考点</strong>：连接器→查询缓存→解析器→优化器→执行器→存储引擎。</p><p><strong>背诵话术</strong>：</p><blockquote><p>以 <code>SELECT * FROM users WHERE id = 1</code> 为例：</p><ol><li><strong>连接器</strong>：建立 TCP 连接，进行身份认证（用户名&#x2F;密码），获取该用户的权限并缓存。</li><li><strong>查询缓存</strong>（MySQL 8.0 已移除）：如果命中查询缓存，直接返回缓存结果。由于缓存失效频繁，实际效果差，MySQL 8.0 直接移除了这个功能。</li><li><strong>解析器（Parser）</strong>：对 SQL 进行词法分析（识别关键字、表名、列名等 Token）和语法分析（验证 SQL 语法是否正确），生成语法树（AST）。</li><li><strong>优化器（Optimizer）</strong>：根据统计信息（索引选择性、数据分布等），选择最优的执行计划（如选择哪个索引、JOIN 的驱动表顺序等）。这一步决定了 <code>Explain</code> 输出的内容。</li><li><strong>执行器（Executor）</strong>：调用存储引擎的接口，按照执行计划逐步获取数据。执行前会再次检查操作权限。</li><li><strong>存储引擎（InnoDB）</strong>：实际执行数据的读写操作。从 Buffer Pool 中查找数据（命中则直接返回），未命中则从磁盘读取数据页到 Buffer Pool 再返回。</li></ol></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> MySQL </tag>
            
            <tag> 数据库 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Redis与本地缓存面试考题</title>
      <link href="/2026/06/08/Redis%E4%B8%8E%E6%9C%AC%E5%9C%B0%E7%BC%93%E5%AD%98%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/Redis%E4%B8%8E%E6%9C%AC%E5%9C%B0%E7%BC%93%E5%AD%98%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="04-Redis与本地缓存-·-高频面试考题背诵指南"><a href="#04-Redis与本地缓存-·-高频面试考题背诵指南" class="headerlink" title="04 - Redis与本地缓存 · 高频面试考题背诵指南"></a>04 - Redis与本地缓存 · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1redis-%E4%BA%94%E7%A7%8D%E6%A0%B8%E5%BF%83%E6%95%B0%E6%8D%AE%E7%B1%BB%E5%9E%8B%E5%8F%8A%E5%BA%95%E5%B1%82%E5%AE%9E%E7%8E%B0">Q1：Redis 五种核心数据类型及底层实现</a></li><li><a href="#q2zset-%E7%9A%84%E8%B7%B3%E8%A1%A8skiplist%E5%8E%9F%E7%90%86">Q2：ZSet 的跳表（skiplist）原理</a></li><li><a href="#q3redis-%E5%B8%B8%E8%A7%81%E4%B8%9A%E5%8A%A1%E5%9C%BA%E6%99%AF%E5%AE%9E%E7%8E%B0">Q3：Redis 常见业务场景实现</a></li><li><a href="#q4rdb-%E5%92%8C-aof-%E7%9A%84%E4%BC%98%E7%BC%BA%E7%82%B9%E5%AF%B9%E6%AF%94%E7%94%9F%E4%BA%A7%E4%B8%AD%E5%A6%82%E4%BD%95%E9%80%89%E6%8B%A9">Q4：RDB 和 AOF 的优缺点对比？生产中如何选择？</a></li><li><a href="#q5aof-%E7%9A%84%E4%B8%89%E7%A7%8D%E5%86%99%E5%9B%9E%E7%AD%96%E7%95%A5">Q5：AOF 的三种写回策略</a></li><li><a href="#q6redis-%E4%B8%BB%E4%BB%8E%E5%A4%8D%E5%88%B6%E5%8E%9F%E7%90%86">Q6：Redis 主从复制原理</a></li><li><a href="#q7redis-sentinel%E5%93%A8%E5%85%B5%E7%9A%84%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86">Q7：Redis Sentinel（哨兵）的工作原理</a></li><li><a href="#q8redis-cluster-%E5%88%86%E7%89%87%E9%9B%86%E7%BE%A4%E5%8E%9F%E7%90%86">Q8：Redis Cluster 分片集群原理</a></li><li><a href="#q9redis-%E8%BF%87%E6%9C%9F%E9%94%AE%E5%88%A0%E9%99%A4%E7%AD%96%E7%95%A5">Q9：Redis 过期键删除策略</a></li><li><a href="#q10redis-%E5%86%85%E5%AD%98%E6%B7%98%E6%B1%B0%E7%AD%96%E7%95%A5">Q10：Redis 内存淘汰策略</a></li><li><a href="#q11%E7%BC%93%E5%AD%98%E7%A9%BF%E9%80%8F%E5%87%BB%E7%A9%BF%E9%9B%AA%E5%B4%A9%E7%9A%84%E5%8C%BA%E5%88%AB%E5%92%8C%E8%A7%A3%E5%86%B3%E6%96%B9%E6%A1%88">Q11：缓存穿透&#x2F;击穿&#x2F;雪崩的区别和解决方案</a></li><li><a href="#q12%E7%BC%93%E5%AD%98%E4%B8%8E%E6%95%B0%E6%8D%AE%E5%BA%93%E5%8F%8C%E5%86%99%E4%B8%80%E8%87%B4%E6%80%A7%E7%BB%93%E5%90%88%E6%8B%9B%E8%A1%8C%E9%A1%B9%E7%9B%AE">Q12：缓存与数据库双写一致性（结合招行项目）</a></li><li><a href="#q13redisson-%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81-watchdog-%E6%9C%BA%E5%88%B6">Q13：Redisson 分布式锁 WatchDog 机制</a></li><li><a href="#q14redis-pipeline-%E5%92%8C-lua-%E8%84%9A%E6%9C%AC%E7%9A%84%E4%BD%BF%E7%94%A8%E5%9C%BA%E6%99%AF">Q14：Redis Pipeline 和 Lua 脚本的使用场景</a></li><li><a href="#q15caffeine-%E7%9A%84-w-tinylfu-%E7%AE%97%E6%B3%95%E5%8E%9F%E7%90%86">Q15：Caffeine 的 W-TinyLFU 算法原理</a></li><li><a href="#q16%E5%88%86%E5%B8%83%E5%BC%8F%E5%A4%9A%E8%8A%82%E7%82%B9%E6%9C%AC%E5%9C%B0%E7%BC%93%E5%AD%98%E4%B8%80%E8%87%B4%E6%80%A7%E7%BB%93%E5%90%88%E6%8B%9B%E8%A1%8C%E9%A1%B9%E7%9B%AE">Q16：分布式多节点本地缓存一致性（结合招行项目）</a></li><li><a href="#q17redisson-%E5%88%86%E5%B8%83%E5%BC%8F%E9%94%81%E7%9A%84%E5%8F%AF%E9%87%8D%E5%85%A5%E5%8E%9F%E7%90%86%E4%B8%8E%E7%BA%A2%E9%94%81redlock%E4%BA%89%E8%AE%AE">Q17：Redisson 分布式锁的可重入原理与红锁（Redlock）争议</a></li></ul><hr><span id="more"></span><h2 id="一、Redis-数据结构与使用场景"><a href="#一、Redis-数据结构与使用场景" class="headerlink" title="一、Redis 数据结构与使用场景"></a>一、Redis 数据结构与使用场景</h2><h3 id="Q1：Redis-五种核心数据类型及底层实现"><a href="#Q1：Redis-五种核心数据类型及底层实现" class="headerlink" title="Q1：Redis 五种核心数据类型及底层实现"></a>Q1：Redis 五种核心数据类型及底层实现</h3><p><strong>核心考点</strong>：每种数据类型的底层数据结构，小数据量时的 ziplist 压缩优化。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Redis 有 5 种核心数据类型，每种类型底层会根据数据量大小选择不同的编码结构：</p><table><thead><tr><th>数据类型</th><th>小数据量时</th><th>大数据量时</th></tr></thead><tbody><tr><td><strong>String</strong></td><td>int（整数）&#x2F; embstr（≤44字节）</td><td>raw（SDS 简单动态字符串）</td></tr><tr><td><strong>Hash</strong></td><td>ziplist（压缩列表）</td><td>hashtable（哈希表）</td></tr><tr><td><strong>List</strong></td><td>ziplist</td><td>quicklist（双向链表 + ziplist）</td></tr><tr><td><strong>Set</strong></td><td>intset（整数集合，纯整数时）</td><td>hashtable</td></tr><tr><td><strong>ZSet（有序集合）</strong></td><td>ziplist</td><td>skiplist（跳表）+ hashtable</td></tr></tbody></table><p><strong>SDS（Simple Dynamic String）</strong> 相比 C 字符串的优势：记录了字符串长度（O(1) 获取），空间预分配和惰性释放避免频繁内存分配，且是二进制安全的（可存储 <code>\0</code> 字符）。</p><p><strong>ziplist（压缩列表）</strong>：连续内存的紧凑结构，节省内存，但插入删除需要内存重分配，适合数据量少（默认元素数 &lt; 128，单个值 &lt; 64 字节）的情况。</p><p><strong>💡 Redis 7.0 版本变更（ziplist 替换为 listpack）</strong>：<br>在 Redis 7.0 中，官方将所有的 <code>ziplist</code> 编码完全替换为 <code>listpack</code>（紧凑列表）。主要原因在于 <code>ziplist</code> 内部存储了前一个节点占用的字节数（<code>prevlen</code>），当插入或修改元素时，可能会触发“连锁更新”（Cascade Update），严重拖慢性能。而 <code>listpack</code> 摒弃了 <code>prevlen</code> 的设计，仅在节点尾部记录当前节点的长度，从而彻底避免了连锁更新问题。</p></blockquote><hr><h3 id="Q2：ZSet-的跳表（skiplist）原理"><a href="#Q2：ZSet-的跳表（skiplist）原理" class="headerlink" title="Q2：ZSet 的跳表（skiplist）原理"></a>Q2：ZSet 的跳表（skiplist）原理</h3><p><strong>核心考点</strong>：跳表的多层索引结构，查询&#x2F;插入时间复杂度 O(logN)，为什么不用红黑树。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>跳表（Skip List）</strong> 是一种以空间换时间的多层链表结构。底层是一条包含所有元素的有序链表，上层是下层的”稀疏索引”——每隔若干个节点提升一个节点到上层，形成多级索引。</p><p><strong>查询过程</strong>：从最高层的索引链表开始，向右比较大小，如果当前节点值大于目标，则下降到下一层，继续向右比较，直到最底层精确定位。平均时间复杂度 <strong>O(logN)</strong>。</p><p><strong>ZSet 为什么同时持有 skiplist 和 hashtable</strong>：skiplist 支持按分数范围查询（如 <code>ZRANGEBYSCORE</code>），hashtable 支持按成员名精确查找分数（如 <code>ZSCORE</code>，O(1)）。两者互补，构成 ZSet 强大的双向查询能力。</p><p><strong>ZSet 为什么用跳表而不用红黑树</strong>：</p><ol><li>跳表的<strong>范围查询</strong>（<code>ZRANGEBYSCORE</code>）更高效，只需在底层链表上顺序遍历即可；红黑树的范围查询需要中序遍历，实现复杂。</li><li>跳表实现更简单，代码更易维护和调试。</li><li>跳表的并发修改（无锁化）比平衡树更容易实现。</li></ol></blockquote><hr><h3 id="Q3：Redis-常见业务场景实现"><a href="#Q3：Redis-常见业务场景实现" class="headerlink" title="Q3：Redis 常见业务场景实现"></a>Q3：Redis 常见业务场景实现</h3><p><strong>核心考点</strong>：各数据类型对应最典型的业务用途。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><p><strong>排行榜（ZSet）</strong>：以用户 ID 为 member，积分为 score，存入 ZSet。<code>ZADD rank 100 user1</code>，<code>ZREVRANGEBYSCORE</code> 获取 Top N 排行。医博系统的医生接诊量排行用的就是这个方案。</p></li><li><p><strong>计数器（String + INCR）</strong>：Redis 的 <code>INCR</code>&#x2F;<code>INCRBY</code> 是原子操作，用于统计接口调用次数、点赞数等，天然防并发。</p></li><li><p><strong>分布式锁（String + SET NX EX）</strong>：<code>SET lock_key value NX EX 30</code>，NX 保证只有一个线程能设置成功，EX 设置过期时间防死锁。生产中用 Redisson 封装，自动处理 WatchDog 续期。</p></li><li><p><strong>接口限流（String + INCR &#x2F; Lua + 滑动窗口）</strong>：对每个用户 IP 的请求次数计数，超过阈值则拒绝。美团开放平台的 Redis Lua 脚本限流就是这个思路。</p></li><li><p><strong>购物车（Hash）</strong>：以用户 ID 为 key，商品 ID 为 field，数量为 value。<code>HSET cart:user1 item:101 3</code>，<code>HGETALL cart:user1</code> 获取整个购物车。</p></li><li><p><strong>消息队列（List）</strong>：<code>LPUSH</code> 生产，<code>BRPOP</code> 阻塞消费，实现简单的消息队列。</p></li><li><p><strong>社交关注（Set）</strong>：存储用户的关注列表和粉丝列表，利用 <code>SINTER</code> 求两个用户共同关注的人。</p></li><li><p><strong>Session 共享（String）</strong>：分布式系统中将用户 Session 存入 Redis，各服务节点从 Redis 统一读取，解决多节点 Session 不共享问题。</p></li></ul></blockquote><hr><h2 id="二、Redis-持久化"><a href="#二、Redis-持久化" class="headerlink" title="二、Redis 持久化"></a>二、Redis 持久化</h2><h3 id="Q4：RDB-和-AOF-的优缺点对比？生产中如何选择？"><a href="#Q4：RDB-和-AOF-的优缺点对比？生产中如何选择？" class="headerlink" title="Q4：RDB 和 AOF 的优缺点对比？生产中如何选择？"></a>Q4：RDB 和 AOF 的优缺点对比？生产中如何选择？</h3><p><strong>核心考点</strong>：两种持久化的触发方式、恢复速度、数据安全性。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>RDB（Redis DataBase Backup，快照持久化）</strong>：</p><ul><li><strong>原理</strong>：在满足配置的触发条件时（如 900s 内有 1 次写操作），Redis 调用 <code>BGSAVE</code> 命令，<code>fork()</code> 出一个子进程，子进程将当前内存中的全量数据生成一个紧凑的 <code>.rdb</code> 二进制文件，父进程继续处理请求。</li><li><strong>优点</strong>：恢复速度极快（直接加载二进制文件），适合灾备和全量数据迁移，RDB 文件体积小。</li><li><strong>缺点</strong>：两次快照之间的数据可能丢失（最多丢几分钟的数据）；<code>fork()</code> 时有短暂的 STW，对实时性要求高的场景有影响。</li></ul><p><strong>AOF（Append Only File，追加日志持久化）</strong>：</p><ul><li><strong>原理</strong>：将每一条写命令追加写入 AOF 日志文件，Redis 重启时重放所有命令来恢复数据。</li><li><strong>优点</strong>：数据丢失风险低，默认 <code>everysec</code> 模式最多丢 1 秒数据；日志可读，可做数据审计。</li><li><strong>缺点</strong>：AOF 文件体积大（记录所有操作，有大量冗余），恢复速度比 RDB 慢；AOF 重写（<code>BGREWRITEAOF</code>）时也需要 fork 子进程。</li></ul><p><strong>生产选择</strong>：优先开启 <strong>RDB + AOF 混合持久化</strong>（Redis 4.0+），AOF 重写时先生成 RDB 快照写入 AOF 文件，之后追加写操作。这样既有 RDB 的快速恢复，又有 AOF 的数据完整性，是生产的最佳实践。</p></blockquote><hr><h3 id="Q5：AOF-的三种写回策略"><a href="#Q5：AOF-的三种写回策略" class="headerlink" title="Q5：AOF 的三种写回策略"></a>Q5：AOF 的三种写回策略</h3><p><strong>核心考点</strong>：性能和数据安全性的取舍，<code>everysec</code> 是生产默认选择。</p><p><strong>背诵话术</strong>：</p><blockquote><p>AOF 的写回策略控制写命令何时从操作系统的 Page Cache 刷入磁盘（<code>fsync</code>）：</p><ul><li><strong><code>always</code>（同步写）</strong>：每条写命令执行后立即 <code>fsync</code> 刷盘。数据丢失风险最低（最多丢失 1 条命令），但 IO 频繁，性能最差，对磁盘压力大。适合对数据安全性要求极高的金融场景。</li><li><strong><code>everysec</code>（每秒写，生产默认推荐）</strong>：每秒执行一次 <code>fsync</code>，由后台线程完成。最多丢失 1 秒的数据，在性能和数据安全性之间取得了最佳平衡，是<strong>生产环境的首选</strong>。</li><li><strong><code>no</code>（操作系统控制）</strong>：Redis 不主动 <code>fsync</code>，由操作系统决定何时刷盘（通常 30 秒左右）。性能最好，但丢失数据量最多，不推荐用于重要业务。</li></ul></blockquote><hr><h2 id="三、Redis-高可用"><a href="#三、Redis-高可用" class="headerlink" title="三、Redis 高可用"></a>三、Redis 高可用</h2><h3 id="Q6：Redis-主从复制原理"><a href="#Q6：Redis-主从复制原理" class="headerlink" title="Q6：Redis 主从复制原理"></a>Q6：Redis 主从复制原理</h3><p><strong>核心考点</strong>：全量复制（psync）和增量复制（Replication Buffer），复制积压缓冲区。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>全量复制（首次连接或重连偏移量差异过大）</strong>：</p><ol><li>从库向主库发送 <code>psync replicationid offset</code> 命令（replicationid 为主库复制 ID，首次为 <code>?</code>）。</li><li>主库执行 <code>BGSAVE</code> 生成 RDB 快照，将快照发送给从库，从库接收后清空旧数据，加载 RDB 文件。</li><li>主库将生成 RDB 期间新产生的写命令缓存在 <code>Replication Buffer</code> 中，RDB 传输完后一并发送给从库执行。</li></ol><p><strong>增量复制（断线重连，且偏移量在复制积压缓冲区范围内）</strong>：</p><ol><li>从库重连后发送自己记录的 <code>replication offset</code>（复制偏移量）。</li><li>主库检查该偏移量是否在 <strong>Replication Backlog Buffer（复制积压缓冲区，默认 1MB 的环形缓冲区）</strong> 中。</li><li>如果在，只需将缓冲区中从偏移量开始的命令发给从库（增量复制），大大减少了全量复制的开销。</li><li>如果不在（主库的 buffer 已被覆盖），则触发全量复制。</li></ol></blockquote><hr><h3 id="Q7：Redis-Sentinel（哨兵）的工作原理"><a href="#Q7：Redis-Sentinel（哨兵）的工作原理" class="headerlink" title="Q7：Redis Sentinel（哨兵）的工作原理"></a>Q7：Redis Sentinel（哨兵）的工作原理</h3><p><strong>核心考点</strong>：哨兵的三个职责（监控&#x2F;通知&#x2F;自动故障转移），主观下线和客观下线，Leader 选举。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Sentinel 的三大职责</strong>：</p><ol><li><strong>监控（Monitoring）</strong>：哨兵节点每秒向主节点、从节点和其他哨兵节点发送 PING 命令，检测它们是否在线。</li><li><strong>通知（Notification）</strong>：当发现某个节点出现问题，哨兵会通过发布订阅（<code>pub/sub</code>）通知客户端和其他哨兵。</li><li><strong>自动故障转移（Automatic Failover）</strong>：主节点宕机时，哨兵自动选举新的主节点，并通知从节点和客户端更新连接。</li></ol><p><strong>故障转移流程</strong>：</p><ol><li><strong>主观下线（SDOWN）</strong>：某个哨兵判断主节点超时（默认 30s 无响应），标记为”主观下线”。</li><li><strong>客观下线（ODOWN）</strong>：该哨兵向其他哨兵询问（<code>SENTINEL is-master-down-by-addr</code>），当超过半数（<code>quorum</code> 配置）的哨兵都认为主节点下线，才标记为”客观下线”，防止单个哨兵误判（如网络分区）。</li><li><strong>Leader 选举</strong>：哨兵之间通过 Raft 算法选举出一个 Leader 哨兵来主导故障转移。</li><li><strong>新主节点选择</strong>：Leader 哨兵从从节点中选择最优的一个（优先级高、复制进度最靠近主库的）晋升为新主节点，修改其他从节点的复制目标，并通知客户端。</li></ol></blockquote><hr><h3 id="Q8：Redis-Cluster-分片集群原理"><a href="#Q8：Redis-Cluster-分片集群原理" class="headerlink" title="Q8：Redis Cluster 分片集群原理"></a>Q8：Redis Cluster 分片集群原理</h3><p><strong>核心考点</strong>：16384 个 Hash Slot、CRC16 哈希分片、MOVED 和 ASK 重定向、Gossip 协议。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>数据分片</strong>：Redis Cluster 将所有键的空间划分为 <strong>16384 个 Hash Slot（哈希槽）</strong>，每个 Key 通过 <code>CRC16(key) % 16384</code> 计算得到它所属的槽位，每个槽位分配给某个主节点负责。这样数据就被均匀分散到各个节点上，每个节点只负责一部分数据。</p><p><strong>为什么是 16384 个槽</strong>：16384 &#x3D; 2^14，心跳报文中传输的槽信息用 bitmap 表示，16384 个槽只需要 2KB，既够用又不会让心跳包太大（如果是 65536 个槽则需要 8KB）。</p><p><strong>请求重定向</strong>：客户端可以连接集群中任意节点，如果访问的 Key 不在当前节点：</p><ul><li><strong><code>MOVED</code> 重定向</strong>：Key 对应的槽不在当前节点，返回 <code>MOVED slotId ip:port</code>，客户端应永久更新本地槽路由表并重定向到正确节点。</li><li><strong><code>ASK</code> 重定向</strong>：在槽迁移过程中，数据可能部分在旧节点、部分已迁移到新节点，返回 <code>ASK</code> 表示临时重定向，客户端只是此次请求去新节点查询，不更新本地路由表。</li></ul><p><strong>节点间通信</strong>：集群内各节点通过 <strong>Gossip 协议</strong> 相互传播节点状态信息（上线&#x2F;下线&#x2F;槽信息变更），最终达到全集群状态一致，无需中心化的协调节点。</p></blockquote><hr><h2 id="四、Redis-过期与淘汰"><a href="#四、Redis-过期与淘汰" class="headerlink" title="四、Redis 过期与淘汰"></a>四、Redis 过期与淘汰</h2><h3 id="Q9：Redis-过期键删除策略"><a href="#Q9：Redis-过期键删除策略" class="headerlink" title="Q9：Redis 过期键删除策略"></a>Q9：Redis 过期键删除策略</h3><p><strong>核心考点</strong>：定期删除 + 惰性删除的组合策略，为何不用定时删除。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Redis 采用<strong>定期删除 + 惰性删除</strong>的组合策略：</p><ul><li><p><strong>惰性删除（Lazy Expiration）</strong>：Key 过期后<strong>不立即删除</strong>，只有当客户端访问该 Key 时，Redis 才检查它是否过期，如果已过期则删除并返回 null。优点：对 CPU 影响最小；缺点：大量过期 Key 没有被访问，会一直占用内存。</p></li><li><p><strong>定期删除（Periodic Expiration）</strong>：Redis 每隔 100ms（<code>hz</code> 参数控制）随机抽取一批设置了过期时间的 Key 进行检查，删除其中已过期的 Key。这是一种后台主动清理机制，补充了惰性删除的不足。</p></li></ul><p><strong>为什么不用定时删除</strong>：为每个 Key 都创建一个定时器，过期时立即删除，虽然内存最友好，但大量定时器会严重消耗 CPU 资源，影响 Redis 的服务能力。</p><p><strong>淘汰策略是最后一道防线</strong>：如果以上两种策略还不够（大量过期 Key 没被及时清理），当内存达到 <code>maxmemory</code> 上限时，Redis 的内存淘汰策略（如 <code>allkeys-lru</code>）会强制淘汰部分 Key 来腾出内存。</p></blockquote><hr><h3 id="Q10：Redis-内存淘汰策略"><a href="#Q10：Redis-内存淘汰策略" class="headerlink" title="Q10：Redis 内存淘汰策略"></a>Q10：Redis 内存淘汰策略</h3><p><strong>核心考点</strong>：8 种策略，生产中最常用 <code>allkeys-lru</code>。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Redis 提供 8 种内存淘汰策略（通过 <code>maxmemory-policy</code> 配置）：</p><ul><li><strong><code>noeviction</code>（默认）</strong>：内存满时拒绝所有写请求，直接报错。适合不允许丢失任何数据的场景（如数据库），但会导致业务报错。</li><li><strong><code>allkeys-lru</code>（生产最常用）</strong>：从所有 Key 中淘汰最近最少使用的 Key（LRU 算法）。适合缓存场景，是绝大多数业务的推荐选择。</li><li><strong><code>volatile-lru</code></strong>：只对设置了过期时间的 Key 进行 LRU 淘汰。保留了没有过期时间的 Key（可能是重要数据）。</li><li><strong><code>allkeys-lfu</code></strong>（Redis 4.0+）：从所有 Key 中淘汰访问频率最低的 Key（LFU 算法）。比 LRU 更精准，适合访问模式稳定的场景。</li><li><strong><code>volatile-lfu</code></strong>：只对有过期时间的 Key 进行 LFU 淘汰。</li><li><strong><code>allkeys-random</code></strong>：随机淘汰所有 Key，适合 Key 访问频率差不多均匀的场景。</li><li><strong><code>volatile-random</code></strong>：随机淘汰有过期时间的 Key。</li><li><strong><code>volatile-ttl</code></strong>：优先淘汰剩余存活时间（TTL）最短的 Key。</li></ul><p><strong>招行调度中台的选择</strong>：业务规则缓存是读多写少的热数据，使用 <code>allkeys-lru</code> 配合合理的 <code>maxmemory</code> 设置，让 Redis 自动淘汰冷数据，无需手动管理。</p></blockquote><hr><h2 id="五、缓存高频问题"><a href="#五、缓存高频问题" class="headerlink" title="五、缓存高频问题"></a>五、缓存高频问题</h2><h3 id="Q11：缓存穿透-击穿-雪崩的区别和解决方案"><a href="#Q11：缓存穿透-击穿-雪崩的区别和解决方案" class="headerlink" title="Q11：缓存穿透&#x2F;击穿&#x2F;雪崩的区别和解决方案"></a>Q11：缓存穿透&#x2F;击穿&#x2F;雪崩的区别和解决方案</h3><p><strong>核心考点</strong>：三种问题的本质、场景和对应的解决方案。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>1. 缓存穿透（查询不存在的数据）</strong>：</p><ul><li><strong>现象</strong>：大量请求查询一个数据库中不存在的 Key（如 <code>id = -1</code>），缓存中也没有，每次都直接穿透到数据库，引起数据库压力激增。恶意攻击者可以利用这一点对数据库发起 DDoS。</li><li><strong>解决方案</strong>：<ol><li><strong>布隆过滤器（BloomFilter）</strong>：将所有存在的数据的 Key 加入布隆过滤器，请求时先判断 Key 是否存在，不存在直接拒绝。布隆过滤器有小概率误判（误判为存在），但不会漏判（存在的一定判存在）。</li><li><strong>空值缓存</strong>：即使数据库查不到数据，也在 Redis 中缓存一个 null 值（设置短过期时间，如 5 分钟），防止反复穿透。</li></ol></li></ul><p><strong>2. 缓存击穿（热点 Key 过期瞬间大量并发）</strong>：</p><ul><li><strong>现象</strong>：一个热点 Key（如某明星微博）在缓存中过期的瞬间，大量并发请求同时涌向数据库，导致数据库被打崩。</li><li><strong>解决方案</strong>：<ol><li><strong>互斥锁（Mutex）</strong>：Key 过期后，第一个请求用 <code>SETNX</code> 加锁查数据库并重建缓存，其他请求等待。安全但有阻塞。</li><li><strong>逻辑过期（热点 Key 不设 TTL）</strong>：缓存中永远不让 Key 真正过期，而是在 Value 中存储一个”逻辑过期时间”字段。请求时检查逻辑时间，若已过期则异步启动后台线程重建缓存，当前请求返回旧数据（容忍短暂不一致）。<strong>适合允许数据短暂不一致的高并发场景，是我们医院排班缓存采用的方案。</strong></li></ol></li></ul><p><strong>3. 缓存雪崩（大量 Key 同时过期）</strong>：</p><ul><li><strong>现象</strong>：大量缓存 Key 在同一时刻集体过期（如系统初始化时批量设置了相同的过期时间），导致大量请求同时打到数据库，引发雪崩效应。</li><li><strong>解决方案</strong>：<ol><li><strong>过期时间加随机值</strong>：在设置缓存时，在基础过期时间上加上一个随机的偏移量（如 <code>TTL + random(0, 300s)</code>），打散过期时间，防止集中过期。</li><li><strong>多级缓存</strong>：Caffeine 本地缓存 + Redis 分布式缓存，即使 Redis 中缓存全部失效，本地缓存还能扛住大部分流量，降低数据库压力。</li><li><strong>Redis 高可用集群</strong>：主从 + 哨兵或 Cluster 方案，防止 Redis 服务本身宕机导致的雪崩。</li></ol></li></ul></blockquote><hr><h3 id="Q12：缓存与数据库双写一致性（结合招行项目）"><a href="#Q12：缓存与数据库双写一致性（结合招行项目）" class="headerlink" title="Q12：缓存与数据库双写一致性（结合招行项目）"></a>Q12：缓存与数据库双写一致性（结合招行项目）</h3><p><strong>核心考点</strong>：Cache-Aside 的顺序选择，并发下的脏数据问题，延迟双删和 Canal 兜底。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Cache-Aside 模式</strong>（旁路缓存）是最常用的缓存一致性模式：</p><ul><li><strong>读</strong>：先读缓存，命中直接返回；未命中则读数据库，写入缓存，再返回。</li><li><strong>写</strong>：先更新数据库，再删除缓存（而非更新缓存）。</li></ul><p><strong>为什么是”先更库再删缓存”而不是”先删缓存再更库”？</strong><br>如果先删缓存，在删缓存和更库之间的窗口期，其他线程读到缓存 miss，去库里读到旧值（此时库还没更新），并把旧值写入缓存，之后库更新完，缓存里依然是旧值——造成脏数据。而”先更库再删缓存”出现脏数据的概率极低（需要缓存失效 + 极短时间内的并发读写组合）。</p><p><strong>延迟双删弥补漏洞</strong>：</p><ol><li>更新数据库后，立即删除一次缓存。</li><li>通过 MQ&#x2F;异步线程，延迟 500ms~1s 后再删除一次缓存。<br>第二次延迟删除是为了清理那些在第一次删除和更新库之间、被其他线程”污染”写入的旧值缓存。</li></ol><p><strong>Canal + Kafka 强一致性兜底（招行项目实战）</strong>：<br>对于招行规则缓存这类一致性要求极高的数据，我们引入了 Canal 订阅 MySQL Binlog 的方案。Canal 伪装成 MySQL 从库，实时接收主库的 Binlog（ROW 格式），解析出数据变更事件，推送到 Kafka。消费服务接收到消息后，立即失效 Redis 中对应的缓存条目（同时发送 Redis Pub&#x2F;Sub 广播，触发各节点 Caffeine 本地缓存失效）。如果失效失败，Kafka 重试机制确保最终一致性。这套方案完全解耦了业务代码与缓存失效逻辑，是一种<strong>事件驱动的最终一致性</strong>方案。</p></blockquote><hr><h3 id="Q13：Redisson-分布式锁-WatchDog-机制"><a href="#Q13：Redisson-分布式锁-WatchDog-机制" class="headerlink" title="Q13：Redisson 分布式锁 WatchDog 机制"></a>Q13：Redisson 分布式锁 WatchDog 机制</h3><p><strong>核心考点</strong>：默认 30s 过期，每 10s 续期，业务执行完自动停止，防止死锁。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>使用 <code>setnx + expire</code> 自实现分布式锁的两大痛点</strong>：</p><ol><li>过期时间设太短：业务还没执行完，锁就释放了，其他线程可以进来，出现并发安全问题。</li><li>过期时间设太长：如果客户端宕机，锁会长时间占用（死锁），严重影响系统可用性。</li></ol><p><strong>Redisson WatchDog 的解法</strong>：Redisson 的 <code>lock()</code> 方法（不指定 leaseTime 时）默认将锁的过期时间设为 <strong>30 秒</strong>，同时在后台启动一个<strong>守护线程（WatchDog）</strong>，以 <code>lockWatchdogTimeout / 3</code>（即 10 秒）为间隔，检查当前持有锁的线程是否还在运行：</p><ul><li><strong>线程仍在运行</strong>：执行 Lua 脚本将锁的过期时间重置为 30 秒（续期）。</li><li><strong>线程已结束或业务主动 unlock()</strong>：WatchDog 守护线程自动停止，不再续期，锁在 30 秒内自然过期释放。</li><li><strong>客户端宕机（JVM 挂掉）</strong>：WatchDog 线程随之消亡，锁最长在 30 秒后自动释放，不会永久死锁。</li></ul><p>Redisson 的锁操作底层使用 <strong>Lua 脚本</strong>来保证”判断 + 操作”的原子性，避免了非原子操作导致的误删他人锁的问题。</p></blockquote><hr><h3 id="Q14：Redis-Pipeline-和-Lua-脚本的使用场景"><a href="#Q14：Redis-Pipeline-和-Lua-脚本的使用场景" class="headerlink" title="Q14：Redis Pipeline 和 Lua 脚本的使用场景"></a>Q14：Redis Pipeline 和 Lua 脚本的使用场景</h3><p><strong>核心考点</strong>：Pipeline 减少网络往返，Lua 脚本保证多命令原子性。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Pipeline（管道）</strong>：Redis 的 Pipeline 允许客户端一次性发送多条命令（而不是每条命令都等待一次 RTT 响应），服务端批量执行后一次性返回所有结果。这大大减少了网络往返次数（RTT），在批量操作场景（如批量写入缓存、批量查询）下性能提升显著。<strong>Pipeline 不保证原子性</strong>，命令之间相互独立，任意一条失败不影响其他命令。</p><p><strong>Lua 脚本</strong>：Redis 会将整个 Lua 脚本作为一个<strong>原子操作</strong>来执行，执行期间不会被其他命令打断（因为 Redis 是单线程的）。当需要多个命令组合在一起具有原子性时（如：判断 + 扣减，判断 + 设置），就需要 Lua 脚本。</p><p><strong>典型案例</strong>：</p><ul><li><strong>分布式限流（Lua）</strong>：美团 OpenAPI 防刷，用 Lua 脚本实现”计数自增 + 超限判断 + 设置过期”的原子操作。</li><li><strong>库存扣减（Lua）</strong>：医博系统号源扣减，用 Lua 实现”判断库存 &gt; 0 → 扣减”的原子操作，防止超卖。</li><li><strong>nonce 防重放（Lua 或 SET NX）</strong>：美团 OpenAPI 的 nonce 存储和检查，用 <code>SET nonce value NX EX 300</code> 保证原子性。</li><li><strong>批量缓存预热（Pipeline）</strong>：招行调度中台系统启动时，利用 Pipeline 批量将热点规则数据写入 Redis，提升预热速度。</li></ul></blockquote><hr><h2 id="六、本地缓存-Caffeine"><a href="#六、本地缓存-Caffeine" class="headerlink" title="六、本地缓存 Caffeine"></a>六、本地缓存 Caffeine</h2><h3 id="Q15：Caffeine-的-W-TinyLFU-算法原理"><a href="#Q15：Caffeine-的-W-TinyLFU-算法原理" class="headerlink" title="Q15：Caffeine 的 W-TinyLFU 算法原理"></a>Q15：Caffeine 的 W-TinyLFU 算法原理</h3><p><strong>核心考点</strong>：TinyLFU（频率过滤）+ SLRU（大小 LRU 段），为什么比 LRU 命中率更高。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Caffeine 采用 <strong>W-TinyLFU（Window Tiny LFU）</strong> 淘汰算法，这也是它被称为”现代最先进的本地缓存”的核心原因。</p><p><strong>W-TinyLFU 的两个区域</strong>：</p><ol><li><strong>Window Cache（窗口区，占 1%）</strong>：所有新进入的 Key 先放入窗口区（类似 LRU），给刚进入的 Key 一个”适应期”，防止刚进来的热数据被频率过低而立即淘汰（即解决 LFU 对突发流量不友好的问题）。</li><li><strong>Main Cache（主缓存，占 99%）</strong>：分为 Protected（保护区，80%）和 Probation（试用区，20%）两段，共同构成 SLRU（Segmented LRU）。高频访问的 Key 在保护区，低频的在试用区。</li></ol><p><strong>TinyLFU 准入机制（PK 门控）</strong>：当窗口区的 Key 需要晋升到主缓存，或新 Key 与主缓存的 Key 竞争时，TinyLFU 会比较两者的历史频率（通过 Count-Min Sketch 近似计数），频率高的才能进入或留在主缓存，频率低的被淘汰。</p><p><strong>为什么比 LRU 命中率更高</strong>：LRU 只考虑”最近被访问”，容易受到一次性大批量扫描数据（如批量报表查询）的影响，把真正的热数据从缓存中挤出去（”缓存污染”）。W-TinyLFU 通过频率统计，保护了真正的高频热数据不被偶发的批量访问影响，命中率提升可达 10%~30%。</p></blockquote><hr><h3 id="Q16：分布式多节点本地缓存一致性（结合招行项目）"><a href="#Q16：分布式多节点本地缓存一致性（结合招行项目）" class="headerlink" title="Q16：分布式多节点本地缓存一致性（结合招行项目）"></a>Q16：分布式多节点本地缓存一致性（结合招行项目）</h3><p><strong>核心考点</strong>：本地缓存无法自动同步，需要广播机制；Canal+Kafka 是最优雅的解法。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Caffeine 是 JVM 进程内的本地缓存，它的数据只在当前服务实例的内存中，在分布式多节点部署下，如果节点 A 更新了数据库并删除了自己的本地缓存，节点 B 和 C 的本地缓存中依然保留着旧数据，出现了<strong>缓存不一致问题</strong>。</p><p><strong>解决方案一：MQ 广播（业务侵入低）</strong>：业务代码在更新数据库的同时，向 Kafka&#x2F;RocketMQ 发布一条缓存失效消息（带上 Key 或数据标识）。所有服务节点都订阅这个 Topic，收到消息后立即执行 <code>caffeine.invalidate(key)</code>。<strong>优点</strong>：解耦彻底，对业务代码侵入小。<strong>缺点</strong>：如果消息丢失或消费延迟，会有短暂不一致。</p><p><strong>解决方案二：Redis Pub&#x2F;Sub 广播</strong>：一个节点完成缓存更新后，向 Redis 的特定 Channel 发布通知，所有节点订阅该 Channel，实时接收并失效本地缓存。延迟比 MQ 更低，但 Redis Pub&#x2F;Sub 不能持久化，如果某个节点消费失败会丢失通知。</p><p><strong>招行调度中台的最终方案（Canal + Kafka + Pub&#x2F;Sub）</strong>：Canal 监听 MySQL Binlog，数据变更时将事件推入 Kafka，消费服务接收到 Kafka 消息后：① 删除&#x2F;更新 Redis 中的分布式缓存；② 向 Redis Pub&#x2F;Sub Channel 广播失效消息，触发所有节点的 Caffeine 本地缓存失效。这种方案完全从数据库变更出发，无需业务代码感知缓存，是最优雅的多级缓存一致性方案。</p></blockquote><hr><h3 id="Q17：Redisson-分布式锁的可重入原理与红锁（Redlock）争议？"><a href="#Q17：Redisson-分布式锁的可重入原理与红锁（Redlock）争议？" class="headerlink" title="Q17：Redisson 分布式锁的可重入原理与红锁（Redlock）争议？"></a>Q17：Redisson 分布式锁的可重入原理与红锁（Redlock）争议？</h3><p><strong>核心考点</strong>：Hash 结构存储重入次数、Lua 脚本原子加锁与重入、tryLock() 内部等待逻辑、Redlock 争议（时钟漂移&#x2F;GC STW）。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>可重入原理</strong>：<br>Redisson 实现可重入锁，其底层使用 <strong>Redis 的 Hash 结构</strong> 来存储锁。Hash 的 key 是锁的名称（如 <code>myLock</code>），field 是持有锁的<strong>线程标识（<code>UUID + threadId</code>）</strong>，value 是<strong>重入次数</strong>（计数器）。</p><ul><li><strong>首次加锁</strong>：若锁不存在，则在 Hash 中创建对应的 field，将 value 设为 1，并设置过期时间。</li><li><strong>锁重入</strong>：若锁已存在，Redisson 会通过 Lua 脚本检查 field 是否与当前请求 of 线程标识一致。若一致，则将 value（重入次数）自增 1，并重新设置锁的过期时间。</li><li><strong>释放锁</strong>：每次释放锁，value 自减 1。只有当 value 减为 0 时，锁才会被彻底删除。这些判断与修改全部由 <strong>Lua 脚本</strong> 封装以保证原子性。</li></ul><p><strong><code>lock()</code> vs <code>tryLock()</code></strong>：</p><ul><li><code>lock()</code> 是一直阻塞等待，直至获取锁或报错。</li><li><code>tryLock(wait, lease, unit)</code> 支持超时获取。若在 <code>wait</code> 时间内未获取到锁，则返回 false。其底层通过<strong>订阅 Redis 的锁释放通道（Pub&#x2F;Sub）</strong>，在拿不到锁时，线程会订阅锁的释放事件并挂起，直到锁释放被唤醒或等待超时，避免了空转自旋对 Redis 的性能消耗。</li></ul><p><strong>红锁（Redlock）算法与争议</strong>：</p><ul><li><strong>为什么需要</strong>：为了解决哨兵或 Cluster 模式下，主从异步复制导致的“主宕机锁丢失”（客户端 A 在主节点加锁成功，主节点未同步给从节点就挂了，从节点还原为主节点，客户端 B 仍能加锁成功，导致锁失效）。</li><li><strong>算法流程</strong>：客户端向 N 个（N 必须是奇数，如 5）独立的、无主从的 Redis 实例发起加锁请求，只有当在<strong>过半数（如 3 个）实例上加锁成功</strong>，且总耗时小于锁的有效时间，才认为加锁成功。</li><li><strong>Martin Kleppmann 的争议点</strong>：<ol><li><strong>系统时钟漂移</strong>：Redlock 强依赖各服务器的本地系统时钟。如果某台服务器发生 NTP 时钟跳变，锁可能会提前过期，导致并发安全失效。</li><li><strong>垃圾回收（GC）引起的 STW 停顿</strong>：客户端在获取锁后发生长时间 GC，在此期间锁在 Redis 上已过期，GC 结束后客户端继续执行临界区代码，导致失效。</li><li>Martin 认为 Redlock 是一个“不上不下”的算法，对性能有影响却又无法保证绝对的数学安全性。如果需要强一致性，应使用 ZooKeeper 或基于 Raft 的强一致性协调系统。</li></ol></li></ul></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> Redis </tag>
            
            <tag> 缓存 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Kafka消息队列面试考题</title>
      <link href="/2026/06/08/Kafka%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/Kafka%E6%B6%88%E6%81%AF%E9%98%9F%E5%88%97%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="05-Kafka消息队列-·-高频面试考题背诵指南"><a href="#05-Kafka消息队列-·-高频面试考题背诵指南" class="headerlink" title="05 - Kafka消息队列 · 高频面试考题背诵指南"></a>05 - Kafka消息队列 · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1kafka-%E7%9A%84%E6%95%B4%E4%BD%93%E6%9E%B6%E6%9E%84%E6%A0%B8%E5%BF%83%E7%BB%84%E4%BB%B6%E5%85%B3%E7%B3%BB">Q1：Kafka 的整体架构？核心组件关系？</a></li><li><a href="#q2kafka-%E4%B8%BA%E4%BB%80%E4%B9%88%E8%BF%99%E4%B9%88%E5%BF%AB">Q2：Kafka 为什么这么快？</a></li><li><a href="#q3kafka-%E6%B6%88%E6%81%AF%E5%AD%98%E5%82%A8%E7%BB%93%E6%9E%84">Q3：Kafka 消息存储结构</a></li><li><a href="#q4isrhwleo-%E5%88%86%E5%88%AB%E6%98%AF%E4%BB%80%E4%B9%88">Q4：ISR、HW、LEO 分别是什么？</a></li><li><a href="#q5kafka-%E7%94%9F%E4%BA%A7%E8%80%85%E5%8F%91%E9%80%81%E6%B6%88%E6%81%AF%E7%9A%84%E5%AE%8C%E6%95%B4%E6%B5%81%E7%A8%8B">Q5：Kafka 生产者发送消息的完整流程</a></li><li><a href="#q6%E5%A6%82%E4%BD%95%E4%BF%9D%E9%9A%9C-kafka-%E6%B6%88%E6%81%AF%E9%9B%B6%E4%B8%A2%E5%A4%B1">Q6：如何保障 Kafka 消息零丢失？</a></li><li><a href="#q7kafka-%E7%94%9F%E4%BA%A7%E8%80%85%E5%B9%82%E7%AD%89%E6%80%A7%E5%8E%9F%E7%90%86%E5%A6%82%E4%BD%95%E5%BC%80%E5%90%AF">Q7：Kafka 生产者幂等性原理？如何开启？</a></li><li><a href="#q8kafka-%E6%B6%88%E8%B4%B9%E8%80%85%E7%BB%84%E7%9A%84%E5%B7%A5%E4%BD%9C%E6%9C%BA%E5%88%B6">Q8：Kafka 消费者组的工作机制</a></li><li><a href="#q9%E5%88%86%E5%8C%BA%E5%88%86%E9%85%8D%E7%AD%96%E7%95%A5range-roundrobin-sticky">Q9：分区分配策略（Range &#x2F; RoundRobin &#x2F; Sticky）</a></li><li><a href="#q10rebalance-%E5%86%8D%E5%B9%B3%E8%A1%A1%E7%9A%84%E8%A7%A6%E5%8F%91%E6%9D%A1%E4%BB%B6%E5%92%8C%E5%A6%82%E4%BD%95%E9%81%BF%E5%85%8D">Q10：Rebalance 再平衡的触发条件和如何避免</a></li><li><a href="#q11%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81-kafka-%E6%B6%88%E6%81%AF%E7%9A%84%E9%A1%BA%E5%BA%8F%E6%B6%88%E8%B4%B9">Q11：如何保证 Kafka 消息的顺序消费？</a></li><li><a href="#q12%E6%B6%88%E8%B4%B9%E8%80%85%E5%A6%82%E4%BD%95%E5%AE%9E%E7%8E%B0%E5%B9%82%E7%AD%89%E6%80%A7%E6%B6%88%E8%B4%B9">Q12：消费者如何实现幂等性消费？</a></li><li><a href="#q13%E6%B6%88%E6%81%AF%E7%A7%AF%E5%8E%8B%E5%A6%82%E4%BD%95%E7%B4%A7%E6%80%A5%E5%A4%84%E7%90%86%E7%BB%93%E5%90%88-srm-%E9%A1%B9%E7%9B%AE">Q13：消息积压如何紧急处理？（结合 SRM 项目）</a></li><li><a href="#q14kafka-%E4%BA%8B%E5%8A%A1%E6%B6%88%E6%81%AF%E5%8E%9F%E7%90%86exactly-once-%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81">Q14：Kafka 事务消息原理？Exactly Once 如何保证？</a></li><li><a href="#q15%E6%AD%BB%E4%BF%A1%E9%98%9F%E5%88%97dlq%E5%9C%A8%E9%A1%B9%E7%9B%AE%E4%B8%AD%E7%9A%84%E5%BA%94%E7%94%A8">Q15：死信队列（DLQ）在项目中的应用</a></li></ul><hr><span id="more"></span><h2 id="一、Kafka-核心概念"><a href="#一、Kafka-核心概念" class="headerlink" title="一、Kafka 核心概念"></a>一、Kafka 核心概念</h2><h3 id="Q1：Kafka-的整体架构？核心组件关系？"><a href="#Q1：Kafka-的整体架构？核心组件关系？" class="headerlink" title="Q1：Kafka 的整体架构？核心组件关系？"></a>Q1：Kafka 的整体架构？核心组件关系？</h3><p><strong>核心考点</strong>：Broker&#x2F;Topic&#x2F;Partition&#x2F;Replica&#x2F;Producer&#x2F;Consumer&#x2F;ZooKeeper&#x2F;Controller 各自职责。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Kafka 的核心组件</strong>：</p><ul><li><strong>Broker</strong>：Kafka 的服务节点，一个 Kafka 集群由多个 Broker 组成。每个 Broker 负责存储和提供一部分分区的数据。</li><li><strong>Topic（主题）</strong>：消息的逻辑分类，生产者向特定 Topic 发送消息，消费者从 Topic 订阅消息。</li><li><strong>Partition（分区）</strong>：Topic 的物理分片，每个 Topic 可以有多个 Partition，分布在不同的 Broker 上。Partition 内部消息有序，是 Kafka 并行消费的基础。<strong>Kafka 的最大并发度 &#x3D; Partition 数量</strong>。</li><li><strong>Replica（副本）</strong>：每个 Partition 有多个副本（一个 Leader + 多个 Follower）。Leader 负责所有读写，Follower 异步从 Leader 同步数据。Leader 所在的 Broker 宕机后，从 ISR 中选举新 Leader。</li><li><strong>ISR（In-Sync Replicas）</strong>：与 Leader 保持同步的副本列表。如果 Follower 长时间未同步（超过 <code>replica.lag.time.max.ms</code>，默认 10s），会被踢出 ISR。</li><li><strong>Producer（生产者）</strong>：向 Kafka 发送消息，可指定 Topic 和 Partition。</li><li><strong>Consumer Group（消费者组）</strong>：一组消费者的逻辑分组。同一个 Topic 的同一个 Partition，同一时刻只能被同一消费者组内的<strong>一个</strong>消费者消费，实现负载均衡；不同消费者组之间互相独立，实现广播效果。</li><li><strong>ZooKeeper（Kafka 3.0 前）</strong>：负责存储 Broker 元数据、Controller 选举、Topic 配置、Consumer Offset 等信息（新版本 Kafka 已用 KRaft 自管理替代 ZooKeeper）。</li></ul></blockquote><hr><h3 id="Q2：Kafka-为什么这么快？"><a href="#Q2：Kafka-为什么这么快？" class="headerlink" title="Q2：Kafka 为什么这么快？"></a>Q2：Kafka 为什么这么快？</h3><p><strong>核心考点</strong>：五大性能利器：顺序写磁盘、PageCache、零拷贝、批量发送、消息压缩。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Kafka 在大数据量下依然能保持极高吞吐的核心原因有以下几点：</p><ol><li><p><strong>顺序写磁盘（Sequential Write）</strong>：Kafka 的消息追加写到日志文件末尾，是顺序 IO 操作。机械硬盘的顺序写速度可达 600MB&#x2F;s，远超随机写的 100MB&#x2F;s，SSD 顺序写更快。Kafka 日志文件的设计决定了它的磁盘写性能极高。</p></li><li><p><strong>PageCache（内存映射）</strong>：Kafka 不直接将数据写到磁盘文件，而是先写到操作系统的 <strong>PageCache（内核缓冲区）</strong>，由 OS 异步刷盘。消费者读取最新消息时，数据大概率还在 PageCache 中，直接内存读取，无需磁盘 IO。</p></li><li><p><strong>零拷贝（Zero-Copy）</strong>：传统的文件传输需要 4 次数据拷贝（磁盘→内核缓冲区→用户空间→Socket缓冲区→网卡）。Kafka 使用 Linux 的 <code>sendfile()</code> 系统调用，直接将 PageCache 中的数据发送到网卡，只需 <strong>2 次拷贝</strong>，避免了数据经过用户空间的来回拷贝，大幅降低 CPU 消耗和延迟。</p></li><li><p><strong>批量发送（Batch）</strong>：Producer 不是每条消息都立即发送，而是通过 <code>batch.size</code> 和 <code>linger.ms</code> 配置，将多条消息攒成一批再一起发送，减少网络请求次数。</p></li><li><p><strong>消息压缩（Compression）</strong>：支持 GZIP、Snappy、LZ4、ZSTD 等压缩算法，减小消息体积，降低网络传输带宽消耗。压缩和解压都在 Producer&#x2F;Consumer 端完成，Broker 只负责存储压缩后的数据。</p></li></ol></blockquote><hr><h3 id="Q3：Kafka-消息存储结构"><a href="#Q3：Kafka-消息存储结构" class="headerlink" title="Q3：Kafka 消息存储结构"></a>Q3：Kafka 消息存储结构</h3><p><strong>核心考点</strong>：.log &#x2F; .index &#x2F; .timeindex 文件的关系，稀疏索引的查找过程。</p><p><strong>背诵话术</strong>：</p><blockquote><p>每个 Partition 对应一个目录，目录下按照”日志段（LogSegment）”分割成多组文件：</p><ul><li><strong><code>.log</code> 文件</strong>：存储实际的消息数据，文件名是该 Segment 中第一条消息的 offset（如 <code>00000000000000000000.log</code>）。消息以追加方式写入。</li><li><strong><code>.index</code> 文件（偏移量索引）</strong>：记录 <code>offset → 文件物理位置</code> 的映射关系，是<strong>稀疏索引</strong>（不是每条消息都建索引，而是每隔一定字节建一个索引项）。文件名与 <code>.log</code> 相同。</li><li><strong><code>.timeindex</code> 文件（时间戳索引）</strong>：记录 <code>timestamp → offset</code> 的映射，支持按时间戳查找消息。</li></ul><p><strong>消息查找过程</strong>（如查找 offset &#x3D; 368772 的消息）：</p><ol><li>对所有 LogSegment 的起始 offset 进行<strong>二分查找</strong>，定位到消息所在的 LogSegment（如起始 offset 为 368769 的 Segment）。</li><li>在该 Segment 的 <code>.index</code> 文件中二分查找，找到不超过目标 offset 的最大索引项，获取对应的物理位置（如 position &#x3D; 256）。</li><li>从 <code>.log</code> 文件的 position 256 开始<strong>顺序读取</strong>，直到找到 offset &#x3D; 368772 的消息。</li></ol></blockquote><hr><h3 id="Q4：ISR、HW、LEO-分别是什么？"><a href="#Q4：ISR、HW、LEO-分别是什么？" class="headerlink" title="Q4：ISR、HW、LEO 分别是什么？"></a>Q4：ISR、HW、LEO 分别是什么？</h3><p><strong>核心考点</strong>：ISR 的动态维护机制，HW 保证消费者只能读到已提交的消息。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><p><strong>LEO（Log End Offset）</strong>：每个副本（Leader 或 Follower）当前<strong>最新消息的下一个偏移量</strong>，代表该副本日志的末端位置。Leader 的 LEO 最大，Follower 会不断追赶 Leader 的 LEO。</p></li><li><p><strong>HW（High Watermark，高水位）</strong>：HW &#x3D; ISR 中所有副本 LEO 的<strong>最小值</strong>。消费者只能消费到 HW 之前的消息，HW 之后的消息即使已写入 Leader 也不可见（未提交）。这样保证了即使 Leader 宕机，从 ISR 中选出的新 Leader 上也一定有 HW 之前的所有消息，不会丢失已提交的数据。</p></li><li><p><strong>ISR（In-Sync Replicas）</strong>：与 Leader 保持同步的副本集合，由 Leader 动态维护。如果某个 Follower 超过 <code>replica.lag.time.max.ms</code>（默认 10s）未向 Leader 发送心跳或同步请求，Leader 会将其从 ISR 中踢出（降为 OSR）。Follower 追上后重新加入 ISR。<code>min.insync.replicas</code> 配置了保证消息不丢失所需的最小 ISR 数量。</p></li></ul></blockquote><hr><h2 id="二、生产者"><a href="#二、生产者" class="headerlink" title="二、生产者"></a>二、生产者</h2><h3 id="Q5：Kafka-生产者发送消息的完整流程"><a href="#Q5：Kafka-生产者发送消息的完整流程" class="headerlink" title="Q5：Kafka 生产者发送消息的完整流程"></a>Q5：Kafka 生产者发送消息的完整流程</h3><p><strong>核心考点</strong>：客户端 Sender 线程、RecordAccumulator 批次累积、网络发送。</p><p><strong>背诵话术</strong>：</p><blockquote><p>生产者调用 <code>send()</code> 方法后，消息不会立即发送，而是经过以下流程：</p><ol><li><strong>拦截器链（Interceptors）</strong>：对消息进行预处理（如添加头信息、过滤、监控统计）。</li><li><strong>序列化器（Serializer）</strong>：将 Key 和 Value 序列化为字节数组。</li><li><strong>分区器（Partitioner）</strong>：决定消息发送到哪个 Partition（默认按 Key 哈希，无 Key 时轮询或粘性分配）。</li><li><strong>RecordAccumulator（消息累加器）</strong>：消息被放入对应 Broker 的 <code>RecordBatch</code> 中，当 Batch 大小达到 <code>batch.size</code>（默认 16KB）或等待时间超过 <code>linger.ms</code>（默认 0ms，即不等待），Batch 被标记为就绪，等待发送。</li><li><strong>Sender 线程</strong>：从 RecordAccumulator 中取出就绪的 Batch，通过 <code>NetworkClient</code> 建立 TCP 连接，将 Batch 发送给对应的 Leader Broker。</li><li><strong>ACK 确认</strong>：根据 <code>acks</code> 配置等待 Broker 的确认响应，失败时按 <code>retries</code> 配置进行重试。</li></ol></blockquote><hr><h3 id="Q6：如何保障-Kafka-消息零丢失？"><a href="#Q6：如何保障-Kafka-消息零丢失？" class="headerlink" title="Q6：如何保障 Kafka 消息零丢失？"></a>Q6：如何保障 Kafka 消息零丢失？</h3><p><strong>核心考点</strong>：三端闭环：Producer（acks&#x3D;all + 幂等）+ Broker（min.insync.replicas≥2）+ Consumer（手动提交）。</p><p><strong>背诵话术</strong>：</p><blockquote><p>在招行调度中台和金融结算场景中，消息零丢失是底线，需要从生产者、Broker、消费者三端同时配置：</p><p><strong>Producer 端</strong>：</p><ul><li><code>acks = all</code>（或 <code>-1</code>）：消息必须被 Leader + ISR 中所有 Follower 都确认写入，才算发送成功，避免 Leader 宕机时 Follower 没有数据的情况。</li><li><code>retries = Integer.MAX_VALUE</code>：网络抖动等临时故障自动重试。</li><li><code>enable.idempotence = true</code>：开启幂等性，防止重试导致消息重复写入（详见 Q8）。</li></ul><p><strong>Broker 端</strong>：</p><ul><li><code>min.insync.replicas &gt;= 2</code>：要求 ISR 中至少有 2 个副本（含 Leader）才能确认消息，结合 <code>acks=all</code> 使用，确保至少有 1 个 Follower 也写入了数据，即使 Leader 宕机也不丢失。</li><li>设置 <code>unclean.leader.election.enable = false</code>：禁止落后的 Follower（不在 ISR 中）参与 Leader 选举，防止选举出的新 Leader 数据不全。</li></ul><p><strong>Consumer 端</strong>：</p><ul><li><code>enable.auto.commit = false</code>：关闭自动提交 offset。</li><li>必须在业务逻辑<strong>成功处理完消息</strong>并完成数据库持久化后，才<strong>手动提交 offset</strong>（调用 <code>consumer.commitSync()</code> 或 <code>commitAsync()</code>）。如果消费过程中抛出异常，不提交 offset，消息会被重新消费，配合幂等消费防止业务重复处理。</li></ul></blockquote><hr><h3 id="Q7：Kafka-生产者幂等性原理？如何开启？"><a href="#Q7：Kafka-生产者幂等性原理？如何开启？" class="headerlink" title="Q7：Kafka 生产者幂等性原理？如何开启？"></a>Q7：Kafka 生产者幂等性原理？如何开启？</h3><p><strong>核心考点</strong>：ProducerID + Sequence Number 在 Broker 端去重，防止 retry 导致重复消息。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>问题背景</strong>：<code>acks=all</code> + <code>retries</code> 配置下，如果 Broker 已成功写入消息但 ACK 响应在网络中丢失，Producer 会认为发送失败而重试，导致 Broker 收到重复的消息。</p><p><strong>幂等性原理</strong>：开启幂等性后（<code>enable.idempotence = true</code>，会同时强制设置 <code>acks=all</code>、<code>retries=MAX</code>），Kafka 给每个 Producer 分配一个全局唯一的 <strong>PID（Producer ID）</strong>，并为每条发往同一 Partition 的消息分配一个单调递增的 <strong>Sequence Number（序列号）</strong>。</p><p>Broker 端维护了每个 <code>(PID, Partition)</code> 组合的最大已接收 Sequence Number。收到消息时：</p><ul><li>如果收到的 Sequence Number &#x3D; 已记录的最大值 + 1：正常写入。</li><li>如果已接收过（Sequence Number ≤ 最大值）：<strong>识别为重复消息，丢弃，返回成功 ACK</strong>。这样 Producer 重试时，Broker 会自动去重。</li></ul><p><strong>局限性</strong>：幂等性只保证<strong>单个 Producer 会话内、单个 Partition 内</strong>的消息不重复，跨 Partition 或 Producer 重启后（PID 会改变），幂等性不再有效。跨 Partition 的幂等需要 Kafka 事务来保证。</p></blockquote><hr><h2 id="三、消费者"><a href="#三、消费者" class="headerlink" title="三、消费者"></a>三、消费者</h2><h3 id="Q8：Kafka-消费者组的工作机制"><a href="#Q8：Kafka-消费者组的工作机制" class="headerlink" title="Q8：Kafka 消费者组的工作机制"></a>Q8：Kafka 消费者组的工作机制</h3><p><strong>核心考点</strong>：分区与消费者的对应关系，并行度上限等于分区数。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Kafka 的消费者组（Consumer Group）遵循以下核心规则：</p><ul><li><strong>同一 Partition，同一时刻只能被同一 Consumer Group 中的一个 Consumer 消费</strong>。这保证了组内消费者对该分区的有序消费，避免了重复处理。</li><li><strong>不同 Consumer Group 之间完全独立</strong>，同一 Topic 的消息会分别广播给每个订阅的消费者组，实现”广播消费”效果（如日志数据同时被监控系统和数据分析系统各自独立消费）。</li></ul><p><strong>并行度</strong>：消费者组内的并行消费能力取决于 Partition 数量，<strong>增加消费者数量不能超过 Partition 数</strong>（超出的消费者会空闲）。因此，<strong>分区数是 Kafka 消费并行度的天花板</strong>，如果发现消费积压，在增加消费者之前，必须先确认 Partition 数量是否足够。</p></blockquote><hr><h3 id="Q9：分区分配策略（Range-RoundRobin-Sticky）"><a href="#Q9：分区分配策略（Range-RoundRobin-Sticky）" class="headerlink" title="Q9：分区分配策略（Range &#x2F; RoundRobin &#x2F; Sticky）"></a>Q9：分区分配策略（Range &#x2F; RoundRobin &#x2F; Sticky）</h3><p><strong>核心考点</strong>：三种策略的分配逻辑，Sticky 减少 Rebalance 时的数据移动。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong><code>Range</code>（默认）</strong>：按 Topic 维度，将该 Topic 的 Partition 连续分段分配给消费者组中的消费者。如果 Partition 数不整除消费者数，靠前的消费者会多分配一个 Partition。多个 Topic 时，每个 Topic 独立分配，可能导致靠前的消费者总是多分一个，产生<strong>负载不均</strong>。</p><p><strong><code>RoundRobin</code></strong>：将所有 Topic 的所有 Partition 按字典序排好，然后轮询分配给每个消费者。负载更均匀，但 Rebalance 时所有分配关系都要重算，迁移量大。</p><p><strong><code>Sticky</code>（推荐）</strong>：初始分配类似 RoundRobin，但在 Rebalance 时尽量<strong>保持现有的分配关系不变</strong>，只将离开的消费者所持有的 Partition 重新分配给其他消费者。减少了不必要的 Partition 迁移，降低了 Rebalance 对消费的影响。<strong>生产中推荐使用 Sticky</strong>，特别是消费者频繁上下线时。</p></blockquote><hr><h3 id="Q10：Rebalance-再平衡的触发条件和如何避免"><a href="#Q10：Rebalance-再平衡的触发条件和如何避免" class="headerlink" title="Q10：Rebalance 再平衡的触发条件和如何避免"></a>Q10：Rebalance 再平衡的触发条件和如何避免</h3><p><strong>核心考点</strong>：Rebalance 期间消费者暂停消费，触发条件，避免频繁触发的配置手段。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Rebalance 的触发条件</strong>：</p><ol><li>消费者组内有消费者<strong>加入或离开</strong>（扩容&#x2F;缩容&#x2F;消费者崩溃）。</li><li>消费者组订阅的 <strong>Topic 分区数变更</strong>（手动增加 Partition）。</li><li>消费者<strong>心跳超时</strong>（消费者未在 <code>session.timeout.ms</code> 内发送心跳，被 GroupCoordinator 认为已死亡）。</li></ol><p><strong>Rebalance 的代价</strong>：Rebalance 期间，所有消费者必须<strong>停止消费</strong>，等待新的分配方案执行完毕，可能造成几秒到几十秒的消息消费中断，影响消费延迟。</p><p><strong>避免频繁 Rebalance 的手段</strong>：</p><ul><li>调大 <code>session.timeout.ms</code>（默认 45s）和 <code>heartbeat.interval.ms</code>（默认 3s），防止因网络抖动导致的误判心跳超时。</li><li>调大 <code>max.poll.interval.ms</code>（默认 5 分钟），避免消费者处理单批消息耗时过长导致被踢出组（<strong>核心！</strong>）。如果业务逻辑处理时间可能超过 5 分钟，必须调大这个参数。</li><li>每次 <code>poll()</code> 拉取的消息数量（<code>max.poll.records</code>）不要设太大，避免处理时间超时。</li><li>使用 <code>Sticky</code> 分区策略减少 Rebalance 的迁移成本。</li></ul></blockquote><hr><h3 id="Q11：如何保证-Kafka-消息的顺序消费？"><a href="#Q11：如何保证-Kafka-消息的顺序消费？" class="headerlink" title="Q11：如何保证 Kafka 消息的顺序消费？"></a>Q11：如何保证 Kafka 消息的顺序消费？</h3><p><strong>核心考点</strong>：同一 Key → 同一 Partition → 单线程消费，三层保证。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Kafka 保证<strong>同一 Partition 内</strong>消息的有序性，但不保证跨 Partition 的全局有序。因此要保证消息顺序消费，需要三层配合：</p><ol><li><p><strong>Producer 侧（相同业务数据发同一 Partition）</strong>：为相关联的消息设置相同的 <code>Key</code>（如订单 ID、用户 ID），Kafka 的分区器会通过 <code>hash(key) % partitionNum</code> 确保相同 Key 的消息始终路由到同一个 Partition，从而保证这些消息的发送顺序。</p></li><li><p><strong>Partition 侧</strong>：Partition 内消息严格有序，只有一个消费者负责消费（Consumer Group 机制保证）。</p></li><li><p><strong>Consumer 侧（单线程消费该 Partition）</strong>：消费者对拿到的消息必须<strong>单线程按序处理</strong>，不能将消息分发到多线程并行处理（否则处理完成顺序无法保证）。如果确实需要提高消费速度，可以按 Key 进一步分组后再交给不同的线程处理（相同 Key 的消息始终在同一线程内串行），保证单个业务实体的消息有序。</p></li></ol><p><strong>注意</strong>：开启幂等性后（<code>enable.idempotence = true</code>），生产者重试消息的顺序也有保障（Kafka 通过 <code>max.in.flight.requests.per.connection</code> 控制）。如需严格有序且不重复，需结合幂等性 + 顺序 Key + 单线程消费。</p></blockquote><hr><h3 id="Q12：消费者如何实现幂等性消费？"><a href="#Q12：消费者如何实现幂等性消费？" class="headerlink" title="Q12：消费者如何实现幂等性消费？"></a>Q12：消费者如何实现幂等性消费？</h3><p><strong>核心考点</strong>：消息至少消费一次（At Least Once），业务侧的幂等性防重设计。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Kafka 默认语义是 <strong>At Least Once（至少一次）</strong>：Consumer 消费消息后，在提交 offset 之前服务重启，则该批消息会被重新消费。因此，消费者端的<strong>幂等性设计</strong>至关重要。</p><p><strong>常见幂等性方案</strong>：</p><ol><li><p><strong>数据库唯一索引</strong>：消费消息后，在数据库插入记录时，以消息中的唯一业务 ID（如订单号、流水号）作为唯一索引，重复消费时数据库会抛出唯一键冲突异常，捕获该异常并忽略，保证数据不重复插入。<strong>这是最简单可靠的方案，在招行结算和 SRM 采购中均使用。</strong></p></li><li><p><strong>Redis SETNX 防重</strong>：消费消息前，以消息的唯一 ID 为 Key，执行 <code>SET msg_id 1 NX EX 7d</code>（7 天过期），若返回成功则继续处理；若返回失败说明已处理过，直接跳过。适合对数据库写入压力敏感的场景。</p></li><li><p><strong>状态机检查</strong>：根据业务状态判断，如订单只能从”待支付”流转到”已支付”，重复消费”已支付”消息时，发现订单已是”已支付”状态，直接幂等返回成功，不做重复处理。</p></li><li><p><strong>Token 令牌机制</strong>：消息中携带一个唯一 token，处理前先从 Redis 中删除该 token（<code>DEL token_key</code>），删除成功则处理，失败则说明已被处理（token 已不存在），幂等返回。</p></li></ol></blockquote><hr><h3 id="Q13：消息积压如何紧急处理？（结合-SRM-项目）"><a href="#Q13：消息积压如何紧急处理？（结合-SRM-项目）" class="headerlink" title="Q13：消息积压如何紧急处理？（结合 SRM 项目）"></a>Q13：消息积压如何紧急处理？（结合 SRM 项目）</h3><p><strong>核心考点</strong>：先定位瓶颈，再扩容消费者或扩分区，临时中转是最后手段。</p><p><strong>背诵话术</strong>：</p><blockquote><p>在 SRM 采购平台，供应商活动期间可能出现询价消息大量积压的情况。我的紧急处理流程分三步：</p><p><strong>第一步：定位积压根因</strong>：通过 Kafka 监控（Lag 指标）确认积压的 Topic 和 Consumer Group，排查是消费异常（抛错导致重试卡死）、下游处理太慢（比价引擎计算耗时），还是数据库 IO 瓶颈导致消费速度跟不上生产速度。</p><p><strong>第二步：扩容消费者（最快的手段）</strong>：</p><ul><li><strong>Partition 数量充足</strong>（如有 10 个分区）：立即对消费者服务进行<strong>水平扩容</strong>，将实例数增加到与分区数持平（最多 10 个消费者），让每个分区都有消费者并行处理。</li><li><strong>Partition 数量不足</strong>（如只有 2 个分区）：<strong>增加消费者实例无用</strong>（多出来的实例会空闲）。需要先扩 Partition，但在线扩 Partition 可能影响已有的 Key 路由（hash 值会变化）。此时最稳妥的方案是：启动一个<strong>临时中转服务</strong>，专门负责快速消费旧 Topic 的消息并立即 ACK，将消息转投到一个新建的、有足够 Partition 数（如 20 个）的临时 Topic，再启动 20 个消费者实例消费临时 Topic。</li></ul><p><strong>第三步：临时降级处理</strong>：关闭消费端一些非关键的业务逻辑（如积分回调、日志埋点），只保留核心的询价处理，释放 CPU 和 IO 资源，加快消费速度，待积压消费完后再恢复。</p></blockquote><hr><h2 id="四、Kafka-高级特性"><a href="#四、Kafka-高级特性" class="headerlink" title="四、Kafka 高级特性"></a>四、Kafka 高级特性</h2><h3 id="Q14：Kafka-事务消息原理？Exactly-Once-如何保证？"><a href="#Q14：Kafka-事务消息原理？Exactly-Once-如何保证？" class="headerlink" title="Q14：Kafka 事务消息原理？Exactly Once 如何保证？"></a>Q14：Kafka 事务消息原理？Exactly Once 如何保证？</h3><p><strong>核心考点</strong>：TransactionCoordinator + 两阶段提交，幂等性 + 事务 &#x3D; Exactly Once。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Kafka 的 Exactly Once（精确一次）语义 &#x3D; <strong>幂等性（单 Partition 不重复）+ 事务（跨 Partition&#x2F;跨 Topic 的原子性）</strong>。</p><p><strong>Kafka 事务的两阶段提交</strong>：</p><ol><li><strong>Prepare 阶段</strong>：Producer 向特定 Broker（<code>TransactionCoordinator</code>）申请一个事务 ID（<code>transactional.id</code>），并开始一个事务（<code>beginTransaction()</code>）。随后向一个或多个 Partition 发送消息，这些消息写入 Broker 日志，但对消费者<strong>不可见</strong>（被标记为未提交状态）。</li><li><strong>Commit &#x2F; Abort 阶段</strong>：Producer 调用 <code>commitTransaction()</code> 后，TransactionCoordinator 向所有参与的 Partition 写入一个<strong>事务提交标记（Transaction Marker）</strong>，只有当消费者读到这个标记时，才能消费该事务中的消息。如果调用 <code>abortTransaction()</code>，写入回滚标记，消费者忽略这批消息。</li></ol><p><strong>读已提交消费</strong>：消费者需设置 <code>isolation.level = read_committed</code>，只读取已提交的事务消息，未提交的消息对该消费者不可见，从而保证消费端不会读到”中间态”数据。</p></blockquote><hr><h3 id="Q15：死信队列（DLQ）在项目中的应用"><a href="#Q15：死信队列（DLQ）在项目中的应用" class="headerlink" title="Q15：死信队列（DLQ）在项目中的应用"></a>Q15：死信队列（DLQ）在项目中的应用</h3><p><strong>核心考点</strong>：消费失败消息的最终归宿，避免无限重试阻塞正常消费。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>死信队列（Dead Letter Queue, DLQ）</strong> 用于处理无法被正常消费的消息，防止它们因无限重试而阻塞整个消费进程。</p><p><strong>标准处理流程</strong>：</p><ol><li>Consumer 消费某条消息失败（如下游服务不可用、数据格式错误）。</li><li>进行有限次数的本地重试（如 3 次）。</li><li>如果重试依然失败，将该消息连同错误原因、重试次数等元信息，转投到一个专门的<strong>死信 Topic</strong>（如 <code>original_topic_name.DLQ</code>）。</li><li>消费者正常提交 offset，继续消费后续消息，不会被单条失败消息阻塞。</li><li>死信 Topic 由<strong>专门的死信处理服务</strong>监听，进行告警通知（钉钉&#x2F;邮件）和人工排查。</li><li>问题修复后，可以从死信 Topic 重放消息进行补偿处理。</li></ol><p><strong>在招行调度中台的应用</strong>：核心金融流程调度消息如果消费失败，会经过 3 次重试后进入死信 Topic，触发高优先级告警，运维人员在 10 分钟内介入处理，确保金融流程不会因消息处理失败而长时间卡顿。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> Kafka </tag>
            
            <tag> 消息队列 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>JVM调优面试考题</title>
      <link href="/2026/06/08/JVM%E8%B0%83%E4%BC%98%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/JVM%E8%B0%83%E4%BC%98%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="06-JVM调优-·-高频面试考题背诵指南"><a href="#06-JVM调优-·-高频面试考题背诵指南" class="headerlink" title="06 - JVM调优 · 高频面试考题背诵指南"></a>06 - JVM调优 · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1jvm-%E5%86%85%E5%AD%98%E5%8C%BA%E5%9F%9F%E5%88%92%E5%88%86%E5%90%84%E5%8C%BA%E5%9F%9F%E5%AD%98%E4%BB%80%E4%B9%88%E5%93%AA%E4%BA%9B%E5%8C%BA%E5%9F%9F%E4%BC%9A-oom">Q1：JVM 内存区域划分？各区域存什么？哪些区域会 OOM？</a></li><li><a href="#q2jdk8-%E7%9A%84%E5%85%83%E7%A9%BA%E9%97%B4metaspace%E4%B8%BA%E4%BB%80%E4%B9%88%E6%9B%BF%E6%8D%A2%E6%B0%B8%E4%B9%85%E4%BB%A3permgen">Q2：JDK8 的元空间（Metaspace）为什么替换永久代（PermGen）？</a></li><li><a href="#q3%E5%AF%B9%E8%B1%A1%E7%9A%84%E5%86%85%E5%AD%98%E5%B8%83%E5%B1%80%E5%AF%B9%E8%B1%A1%E5%A4%B4-%E5%AE%9E%E4%BE%8B%E6%95%B0%E6%8D%AE-%E5%AF%B9%E9%BD%90%E5%A1%AB%E5%85%85">Q3：对象的内存布局（对象头 + 实例数据 + 对齐填充）</a></li><li><a href="#q4%E4%BB%80%E4%B9%88%E6%98%AF%E9%80%83%E9%80%B8%E5%88%86%E6%9E%90jvm-%E7%9A%84%E4%B8%89%E7%A7%8D%E9%80%83%E9%80%B8%E4%BC%98%E5%8C%96">Q4：什么是逃逸分析？JVM 的三种逃逸优化？</a></li><li><a href="#q5%E4%BB%80%E4%B9%88%E6%98%AF-gc-roots%E5%93%AA%E4%BA%9B%E5%AF%B9%E8%B1%A1%E5%8F%AF%E4%BB%A5%E4%BD%9C%E4%B8%BA-gc-roots">Q5：什么是 GC Roots？哪些对象可以作为 GC Roots？</a></li><li><a href="#q6%E4%B8%89%E7%A7%8D-gc-%E7%AE%97%E6%B3%95%E7%9A%84%E5%8E%9F%E7%90%86%E4%BC%98%E7%BC%BA%E7%82%B9">Q6：三种 GC 算法的原理、优缺点</a></li><li><a href="#q7%E6%96%B0%E7%94%9F%E4%BB%A3%E5%92%8C%E8%80%81%E5%B9%B4%E4%BB%A3%E5%AF%B9%E8%B1%A1%E6%99%8B%E5%8D%87%E7%9A%84%E6%9D%A1%E4%BB%B6">Q7：新生代和老年代？对象晋升的条件？</a></li><li><a href="#q8cms-%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86%E5%99%A8%E7%9A%84%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86%E5%92%8C%E7%BC%BA%E7%82%B9">Q8：CMS 垃圾收集器的工作原理和缺点</a></li><li><a href="#q9g1-%E5%9E%83%E5%9C%BE%E6%94%B6%E9%9B%86%E5%99%A8%E7%9A%84-region-%E8%AE%BE%E8%AE%A1%E5%92%8C%E5%B7%A5%E4%BD%9C%E5%8E%9F%E7%90%86">Q9：G1 垃圾收集器的 Region 设计 and 工作原理</a></li><li><a href="#q10zgc-%E7%9A%84%E5%8E%9F%E7%90%86%E5%92%8C%E4%B8%BA%E4%BB%80%E4%B9%88%E8%83%BD%E5%AE%9E%E7%8E%B0-10ms-%E7%9A%84-stw">Q10：ZGC 的原理和为什么能实现 &lt;10ms 的 STW？</a></li><li><a href="#q11cpu-%E9%A3%99%E9%AB%98-100-%E7%9A%84%E5%AE%8C%E6%95%B4%E6%8E%92%E6%9F%A5%E6%AD%A5%E9%AA%A4%E7%BB%93%E5%90%88%E6%8B%9B%E8%A1%8C%E9%A1%B9%E7%9B%AE">Q11：CPU 飙高 100% 的完整排查步骤（结合招行项目）</a></li><li><a href="#q12oom-%E5%86%85%E5%AD%98%E6%BA%A2%E5%87%BA%E7%9A%84%E5%AE%8C%E6%95%B4%E6%8E%92%E6%9F%A5%E6%AD%A5%E9%AA%A4%E7%BB%93%E5%90%88%E5%8C%BB%E5%8D%9A%E9%A1%B9%E7%9B%AE">Q12：OOM 内存溢出的完整排查步骤（结合医博项目）</a></li><li><a href="#q13%E9%A2%91%E7%B9%81-full-gc-%E7%9A%84%E6%8E%92%E6%9F%A5%E6%80%9D%E8%B7%AF">Q13：频繁 Full GC 的排查思路</a></li><li><a href="#q14%E7%94%9F%E4%BA%A7%E7%8E%AF%E5%A2%83%E6%A0%B8%E5%BF%83-jvm-%E5%8F%82%E6%95%B0">Q14：生产环境核心 JVM 参数</a></li><li><a href="#q15arthas-%E5%B8%B8%E7%94%A8%E8%AF%8A%E6%96%AD%E5%91%BD%E4%BB%A4">Q15：Arthas 常用诊断命令</a></li><li><a href="#q16%E7%B1%BB%E5%8A%A0%E8%BD%BD%E8%BF%87%E7%A8%8B%E4%BB%80%E4%B9%88%E6%98%AF%E5%8F%8C%E4%BA%B2%E5%A7%94%E6%B4%BE%E6%A8%A1%E5%9E%8B%E4%B8%BA%E4%BB%80%E4%B9%88%E8%A6%81%E7%A0%B4%E5%9D%8F%E5%AE%83">Q16：类加载过程？什么是双亲委派模型？为什么要破坏它？</a></li></ul><hr><span id="more"></span><h2 id="一、JVM-内存结构"><a href="#一、JVM-内存结构" class="headerlink" title="一、JVM 内存结构"></a>一、JVM 内存结构</h2><h3 id="Q1：JVM-内存区域划分？各区域存什么？哪些区域会-OOM？"><a href="#Q1：JVM-内存区域划分？各区域存什么？哪些区域会-OOM？" class="headerlink" title="Q1：JVM 内存区域划分？各区域存什么？哪些区域会 OOM？"></a>Q1：JVM 内存区域划分？各区域存什么？哪些区域会 OOM？</h3><p><strong>核心考点</strong>：5 大内存区域的职责，线程共享 vs 线程私有，各区域可能的错误。</p><p><strong>背诵话术</strong>：</p><blockquote><p>JVM 内存区域分为以下几块：</p><p><strong>线程私有区域（每个线程独享）</strong>：</p><ol><li><strong>程序计数器（PC Register）</strong>：记录当前线程正在执行的字节码指令的地址（行号）。线程切换后恢复执行时靠它找回位置。它是 JVM 规范中<strong>唯一不会 OOM 的区域</strong>。</li><li><strong>Java 虚拟机栈（JVM Stack）</strong>：每调用一个方法，就压入一个<strong>栈帧（Stack Frame）</strong>，栈帧中存储局部变量表、操作数栈、动态链接、方法出口等信息。方法返回时弹出栈帧。<strong><code>StackOverflowError</code></strong>：递归调用过深，栈超出深度限制（-Xss 参数控制）。<strong><code>OutOfMemoryError</code></strong>：线程数过多时，JVM 无法申请到足够的栈内存。</li><li><strong>本地方法栈（Native Method Stack）</strong>：与 JVM 栈类似，为 Native 方法（C&#x2F;C++ 方法）服务。同样可能 SOE&#x2F;OOM。</li></ol><p><strong>线程共享区域（所有线程共享）</strong>：<br>4. <strong>堆（Heap）</strong>：JVM 中最大的内存区域，存储所有<strong>对象实例和数组</strong>。GC 的主要管理区域。分为新生代（Young，包含 Eden 和 Survivor 区）和老年代（Old）。<strong><code>OutOfMemoryError: Java heap space</code></strong> 是最常见的 OOM，如内存泄漏、对象过多。<br>5. <strong>方法区（Method Area）&#x2F; 元空间（Metaspace）</strong>：存储已加载的类信息（类结构、方法字节码、常量池、静态变量等）。JDK8 以前是”永久代（PermGen）”，JDK8 起改为<strong>元空间（Metaspace，使用直接内存）</strong>。动态生成大量类（如 CGlib 代理、JSP 编译、大量反射）时可能 OOM：<code>OutOfMemoryError: Metaspace</code>。</p></blockquote><hr><h3 id="Q2：JDK8-的元空间（Metaspace）为什么替换永久代（PermGen）？"><a href="#Q2：JDK8-的元空间（Metaspace）为什么替换永久代（PermGen）？" class="headerlink" title="Q2：JDK8 的元空间（Metaspace）为什么替换永久代（PermGen）？"></a>Q2：JDK8 的元空间（Metaspace）为什么替换永久代（PermGen）？</h3><p><strong>核心考点</strong>：PermGen 大小固定易 OOM，Metaspace 使用本地内存动态扩展。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>永久代（PermGen）的缺点</strong>：</p><ol><li><strong>大小固定</strong>：通过 <code>-XX:MaxPermSize</code> 指定，默认只有 64MB 或 256MB。在运行时动态生成大量类（如 Spring&#x2F;Hibernate 使用大量 CGLIB 代理、部署多个 Web 应用）时，极容易出现 <code>java.lang.OutOfMemoryError: PermGen space</code> 错误，且很难调优（调大了又浪费，调小了容易 OOM）。</li><li><strong>GC 效率低</strong>：永久代中的类信息回收（类卸载）条件苛刻，且发生在 Full GC 时，效率低下。</li></ol><p><strong>元空间（Metaspace）的优势</strong>：</p><ol><li><strong>使用本地内存（Native Memory）</strong>：不受 JVM 堆大小限制，默认可以动态增长直到机器物理内存耗尽，大幅减少了 OOM 的发生。可以通过 <code>-XX:MaxMetaspaceSize</code> 设置上限。</li><li><strong>GC 改进</strong>：类信息的回收不再强绑定 Full GC，类卸载效率有所提升。</li></ol></blockquote><hr><h3 id="Q3：对象的内存布局（对象头-实例数据-对齐填充）"><a href="#Q3：对象的内存布局（对象头-实例数据-对齐填充）" class="headerlink" title="Q3：对象的内存布局（对象头 + 实例数据 + 对齐填充）"></a>Q3：对象的内存布局（对象头 + 实例数据 + 对齐填充）</h3><p><strong>核心考点</strong>：Mark Word 的内容随锁状态变化，对象头是 synchronized 锁升级的物理基础。</p><p><strong>背诵话术</strong>：</p><blockquote><p>Java 对象在堆内存中的布局分三部分：</p><p><strong>1. 对象头（Header）</strong>：</p><ul><li><strong>Mark Word（8 字节）</strong>：存储对象的运行时元数据，内容随锁状态动态变化：<ul><li><strong>无锁状态</strong>：hashCode（31 bit）+ GC 年龄（4 bit）+ 锁标志位（01）</li><li><strong>偏向锁状态</strong>：线程 ID（54 bit）+ Epoch（2 bit）+ 锁标志位（01）</li><li><strong>轻量级锁状态</strong>：指向栈帧 Lock Record 的指针（62 bit）+ 锁标志位（00）</li><li><strong>重量级锁状态</strong>：指向 Monitor 对象的指针（62 bit）+ 锁标志位（10）</li><li><strong>GC 标记状态</strong>：空（62 bit）+ 锁标志位（11）</li></ul></li><li><strong>类型指针（Klass Pointer，4 或 8 字节）</strong>：指向该对象所属类的元数据（在元空间中），JVM 通过它确定对象的类型。开启指针压缩（<code>-XX:+UseCompressedOops</code>，默认开启）时占 4 字节。</li></ul><p><strong>2. 实例数据（Instance Data）</strong>：对象的字段值，包括父类继承的字段。引用类型字段占 4 字节（开启指针压缩时），基本类型按各自大小。</p><p><strong>3. 对齐填充（Padding）</strong>：JVM 要求对象起始地址必须是 <strong>8 字节的整数倍</strong>，不足时用填充字节补齐，这对 CPU 缓存行访问更友好。</p></blockquote><hr><h3 id="Q4：什么是逃逸分析？JVM-的三种逃逸优化？"><a href="#Q4：什么是逃逸分析？JVM-的三种逃逸优化？" class="headerlink" title="Q4：什么是逃逸分析？JVM 的三种逃逸优化？"></a>Q4：什么是逃逸分析？JVM 的三种逃逸优化？</h3><p><strong>核心考点</strong>：JIT 的运行时优化，减少堆内存分配压力。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>逃逸分析</strong>是 JIT 编译器在运行时分析对象的作用域范围的优化技术：如果一个对象的引用<strong>不会逃逸出方法范围</strong>（不会被其他线程访问、不会作为方法返回值传出），JVM 就可以对该对象进行以下优化：</p><ol><li><p><strong>栈上分配（Stack Allocation）</strong>：将对象分配在线程的 JVM 栈上，而不是堆上。对象随栈帧的销毁而自动释放，无需 GC 参与，大幅减少 GC 压力和堆内存分配开销。</p></li><li><p><strong>标量替换（Scalar Replacement）</strong>：将对象拆解为若干个基本类型的标量（如将 <code>Point(x, y)</code> 对象分解为两个 int 字段），直接在栈中存储这些标量，不再创建对象本身，进一步消除了对象头等开销。</p></li><li><p><strong>同步消除（Synchronization Elimination）</strong>：如果分析出某个锁对象不会逃逸（只有一个线程会访问），则锁操作没有实际意义，JIT 会将 <code>synchronized</code> 代码块中的加锁&#x2F;解锁操作直接消除，提升性能。</p></li></ol></blockquote><hr><h2 id="二、垃圾回收"><a href="#二、垃圾回收" class="headerlink" title="二、垃圾回收"></a>二、垃圾回收</h2><h3 id="Q5：什么是-GC-Roots？哪些对象可以作为-GC-Roots？"><a href="#Q5：什么是-GC-Roots？哪些对象可以作为-GC-Roots？" class="headerlink" title="Q5：什么是 GC Roots？哪些对象可以作为 GC Roots？"></a>Q5：什么是 GC Roots？哪些对象可以作为 GC Roots？</h3><p><strong>核心考点</strong>：可达性分析算法，从 GC Roots 出发不可达的对象是”垃圾”。</p><p><strong>背诵话术</strong>：</p><blockquote><p>JVM 判断对象是否”存活”，使用的是<strong>可达性分析算法</strong>（而不是引用计数法）：从一系列 <strong>GC Roots（根对象）</strong> 出发，沿引用链向下遍历，能到达的对象是存活的，不可达的对象将被 GC 回收。</p><p><strong>可以作为 GC Roots 的对象</strong>：</p><ol><li><strong>JVM 栈（局部变量表）中引用的对象</strong>：即正在执行的方法中的局部变量所引用的对象。这是最常见的 GC Root，方法执行期间其局部变量引用的对象不能被回收。</li><li><strong>方法区中类静态属性引用的对象</strong>：<code>static</code> 修饰的字段所引用的对象，只要类不卸载，这些对象就不会被回收。这是常见内存泄漏的来源之一（大对象被 static 字段持有）。</li><li><strong>方法区中常量引用的对象</strong>：如字符串常量池中的字符串。</li><li><strong>本地方法栈（Native Stack）中 JNI 引用的对象</strong>：Native 方法中通过 JNI 创建或引用的 Java 对象。</li><li><strong>JVM 内部引用</strong>：如基本数据类型对应的 Class 对象、系统类加载器、常驻的异常对象（NullPointerException 等）。</li></ol></blockquote><hr><h3 id="Q6：三种-GC-算法的原理、优缺点"><a href="#Q6：三种-GC-算法的原理、优缺点" class="headerlink" title="Q6：三种 GC 算法的原理、优缺点"></a>Q6：三种 GC 算法的原理、优缺点</h3><p><strong>核心考点</strong>：三种算法各自解决了什么问题，又引入了什么新问题。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>1. 标记-清除（Mark-Sweep）</strong>：</p><ul><li><strong>原理</strong>：第一阶段标记所有存活对象，第二阶段清除所有未标记的对象，释放内存。</li><li><strong>优点</strong>：实现简单，不需要移动对象。</li><li><strong>缺点</strong>：回收后内存<strong>碎片化严重</strong>，大量不连续的内存空间可能导致大对象无法分配（触发 Full GC）；标记和清除两次扫描效率较低。</li></ul><p><strong>2. 标记-整理（Mark-Compact）</strong>：</p><ul><li><strong>原理</strong>：标记存活对象后，将所有存活对象向内存的一端移动，然后清理边界以外的空间，消除了碎片。</li><li><strong>优点</strong>：无内存碎片，内存利用率高。</li><li><strong>缺点</strong>：移动对象需要修改所有指向该对象的引用，<strong>开销大，STW 时间长</strong>，适合老年代（存活对象少，移动成本相对低）。</li></ul><p><strong>3. 复制算法（Copying）</strong>：</p><ul><li><strong>原理</strong>：将内存分为两块相等的区域（From 和 To），每次只使用 From 区，GC 时将 From 区存活的对象复制到 To 区，然后清空 From 区，两区互换角色。</li><li><strong>优点</strong>：无内存碎片，分配速度快（只需移动指针）。</li><li><strong>缺点</strong>：内存利用率只有 50%；如果存活对象多，复制代价大。适合新生代（存活对象少，大部分是垃圾，复制代价小）。</li></ul></blockquote><hr><h3 id="Q7：新生代和老年代？对象晋升的条件？"><a href="#Q7：新生代和老年代？对象晋升的条件？" class="headerlink" title="Q7：新生代和老年代？对象晋升的条件？"></a>Q7：新生代和老年代？对象晋升的条件？</h3><p><strong>核心考点</strong>：Eden:S0:S1 &#x3D; 8:1:1，四种晋升条件，动态年龄判断。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>JVM 堆的分代结构</strong>：</p><ul><li><strong>新生代（Young Generation）</strong>：占堆的 1&#x2F;3，分为 Eden 区（80%）和两个 Survivor 区（S0&#x2F;S1，各 10%），比例为 <strong>Eden:S0:S1 &#x3D; 8:1:1</strong>。大多数对象在新生代被快速创建和回收（朝生夕死），通过 Minor GC 回收。</li><li><strong>老年代（Old Generation）</strong>：占堆的 2&#x2F;3，存储生命周期较长的对象。通过 Major GC 或 Full GC 回收。</li></ul><p><strong>对象晋升老年代的四个条件</strong>：</p><ol><li><strong>年龄阈值（Age Threshold）</strong>：对象每经历一次 Minor GC 存活下来，年龄 +1。当年龄达到 <code>MaxTenuringThreshold</code>（默认 15）时，晋升老年代。</li><li><strong>大对象直接进入老年代</strong>：超过 <code>-XX:PretenureSizeThreshold</code> 大小的大对象（如大数组）直接分配在老年代，防止在新生代反复复制。</li><li><strong>动态年龄判断（重要！）</strong>：Survivor 区中，如果相同年龄所有对象的总大小超过 Survivor 空间的 50%，则年龄大于等于该年龄的对象直接晋升老年代，无需等到 MaxTenuringThreshold。这是防止 Survivor 区满的自适应机制。</li><li><strong>空间担保失败</strong>：Minor GC 前，JVM 会检查老年代剩余空间是否大于新生代所有对象的总大小（保守担保），如果不够，则直接触发 Full GC。</li></ol></blockquote><hr><h3 id="Q8：CMS-垃圾收集器的工作原理和缺点"><a href="#Q8：CMS-垃圾收集器的工作原理和缺点" class="headerlink" title="Q8：CMS 垃圾收集器的工作原理和缺点"></a>Q8：CMS 垃圾收集器的工作原理和缺点</h3><p><strong>核心考点</strong>：四阶段并发收集，浮动垃圾，内存碎片，ConcurrentModeFailure。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>CMS（Concurrent Mark-Sweep）</strong> 是以获取<strong>最短停顿时间</strong>为目标的老年代收集器，是第一个真正意义上的”并发”GC。</p><p><strong>四个工作阶段</strong>：</p><ol><li><strong>初始标记（Initial Mark，STW）</strong>：仅标记 GC Roots 直接关联的对象，速度极快，但需要 STW。</li><li><strong>并发标记（Concurrent Mark，与用户线程并发）</strong>：从初始标记的对象出发，遍历所有可达对象进行标记，耗时最长，但与用户线程并发执行，不影响服务。</li><li><strong>重新标记（Remark，STW）</strong>：修正并发标记阶段因用户线程继续运行而产生的标记变动（增量更新），需要 STW，但耗时比初始标记长一些。</li><li><strong>并发清除（Concurrent Sweep，与用户线程并发）</strong>：清除死亡对象，释放内存，与用户线程并发执行。</li></ol><p><strong>CMS 的缺点</strong>：</p><ol><li><strong>浮动垃圾（Floating Garbage）</strong>：并发标记和并发清除阶段，用户线程还在运行，可能产生新的垃圾（在已标记为存活的对象之后又变成了垃圾），这些垃圾只能留待下次 GC 再清理。</li><li><strong>内存碎片</strong>：CMS 使用”标记-清除”算法，不整理内存，长时间运行后会产生大量内存碎片，导致大对象无法分配，提前触发 Full GC（串行的 Full GC）。</li><li><strong><code>ConcurrentModeFailure</code></strong>：CMS 在并发清理时，如果老年代剩余空间不足以分配新晋升的对象，会触发 <code>Concurrent Mode Failure</code>，退化为 Serial Old 单线程收集，STW 时间极长。</li></ol></blockquote><hr><h3 id="Q9：G1-垃圾收集器的-Region-设计和工作原理"><a href="#Q9：G1-垃圾收集器的-Region-设计和工作原理" class="headerlink" title="Q9：G1 垃圾收集器的 Region 设计和工作原理"></a>Q9：G1 垃圾收集器的 Region 设计和工作原理</h3><p><strong>核心考点</strong>：Region 取代分代硬边界，可预测的停顿时间，Mixed GC。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>G1（Garbage First）</strong> 是 JDK9 的默认 GC，设计目标是<strong>可预测的停顿时间（Predictable Pause Time）</strong>。</p><p><strong>核心设计——Region（区域）</strong>：G1 将整个堆划分为多个大小相等的独立 <strong>Region</strong>（默认 1~32MB，由堆大小自动决定），每个 Region 可以动态担任 Eden、Survivor、Old 或 Humongous（大对象专区）等角色。打破了传统分代 GC 的物理边界，使 GC 可以灵活选择”垃圾最多的 Region”优先回收（Garbage First 的得名原因）。</p><p><strong>G1 的工作阶段</strong>：</p><ol><li><strong>Young GC（Minor GC）</strong>：仅回收 Eden 和 Survivor Region，存活对象复制到新的 Survivor 或 Old Region。</li><li><strong>全局并发标记（Concurrent Marking）</strong>：并发完成全堆的可达性分析，找出垃圾最多的 Region 并排序（类似 CMS 的并发标记阶段）。</li><li><strong>Mixed GC</strong>：在 Young GC 的同时，也回收部分”垃圾最多”的 Old Region，逐步清理老年代，避免触发 Full GC。</li></ol><p><strong>可预测停顿的原理</strong>：G1 通过 <code>-XX:MaxGCPauseMillis</code>（默认 200ms）设置期望停顿时间，每次 GC 时根据各 Region 的垃圾量和预测回收耗时，<strong>动态决定本次回收哪些 Region</strong>，使停顿时间控制在预期范围内。</p></blockquote><hr><h3 id="Q10：ZGC-的原理和为什么能实现"><a href="#Q10：ZGC-的原理和为什么能实现" class="headerlink" title="Q10：ZGC 的原理和为什么能实现 &lt;10ms 的 STW？"></a>Q10：ZGC 的原理和为什么能实现 &lt;10ms 的 STW？</h3><p><strong>核心考点</strong>：着色指针（Colored Pointer）和读屏障（Load Barrier）实现并发转移。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>ZGC</strong> 是 JDK11 引入、JDK15 正式商用的低延迟 GC，设计目标是<strong>停顿时间不超过 10ms，且不随堆大小增长（可扩展到 TB 级堆）</strong>。</p><p><strong>核心技术 1——着色指针（Colored Pointer）</strong>：ZGC 在 64 位指针的高 4 位（第 41~44 bit）上嵌入了 GC 状态信息（Finalizable、Remapped、Marked1、Marked0），这些标记位用来表示对象当前处于哪个 GC 阶段，无需独立的标记位图数组，内存效率更高。</p><p><strong>核心技术 2——读屏障（Load Barrier）</strong>：ZGC 在每次读取对象引用时（<code>Object ref = obj.field</code>），会插入一小段代码（读屏障），检查指针是否需要修复（如对象是否已经被转移到新地址）。如果对象已被转移，读屏障会自动将指针更新为新地址（自愈），从而实现<strong>对象的并发转移</strong>，而不需要 STW。</p><p><strong>为何 STW 极短</strong>：ZGC 只有两个极短的 STW 阶段（初始标记和再标记），标记、转移和重映射等耗时操作全部与用户线程并发执行，因此 STW 时间几乎不随堆大小增加而增长，稳定在 &lt;10ms 内。</p></blockquote><hr><h2 id="三、线上排障实战"><a href="#三、线上排障实战" class="headerlink" title="三、线上排障实战"></a>三、线上排障实战</h2><h3 id="Q11：CPU-飙高-100-的完整排查步骤（结合招行项目）"><a href="#Q11：CPU-飙高-100-的完整排查步骤（结合招行项目）" class="headerlink" title="Q11：CPU 飙高 100% 的完整排查步骤（结合招行项目）"></a>Q11：CPU 飙高 100% 的完整排查步骤（结合招行项目）</h3><p><strong>核心考点</strong>：top → top -Hp → printf → jstack grep，或 Arthas thread -n。</p><p><strong>背诵话术</strong>：</p><blockquote><p>在招行流程调度中台曾发生过一次，因接收到非规范的复杂 XML 报文，触发了自研报文解析引擎中的 <code>while</code> 死循环，导致服务器 CPU 瞬间飙到 100%。</p><p><strong>标准排查步骤（纯命令行，无 Arthas）</strong>：</p><ol><li><strong><code>top</code></strong>：查看系统整体资源情况，找到 CPU 占用最高的 Java 进程，获取其 PID（假设是 <code>1203</code>）。</li><li><strong><code>top -Hp 1203</code></strong>：打印该进程下所有线程的 CPU 占用，找出占用最高的线程 PID（假设是 <code>1215</code>）。</li><li><strong><code>printf &quot;%x\n&quot; 1215</code></strong>：将十进制线程 PID 转为十六进制（得到 <code>4bf</code>），因为 jstack 输出中的线程 ID 是十六进制。</li><li><strong><code>jstack 1203 | grep -A 30 &quot;4bf&quot;</code></strong>：过滤出该线程的堆栈信息，清晰展示了死循环发生在报文解析工具类的第 218 行，定位到具体问题代码。</li></ol><p><strong>使用 Arthas（更快）</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br></pre></td><td class="code"><pre><span class="line">java -jar arthas-boot.jar  <span class="comment"># 启动 Arthas，选择目标 Java 进程</span></span><br><span class="line">thread -n 3                 <span class="comment"># 自动打印 CPU 占用最高的前 3 个线程的堆栈，一步到位</span></span><br></pre></td></tr></table></figure><p>10 秒内就能精准定位问题线程。</p></blockquote><hr><h3 id="Q12：OOM-内存溢出的完整排查步骤（结合医博项目）"><a href="#Q12：OOM-内存溢出的完整排查步骤（结合医博项目）" class="headerlink" title="Q12：OOM 内存溢出的完整排查步骤（结合医博项目）"></a>Q12：OOM 内存溢出的完整排查步骤（结合医博项目）</h3><p><strong>核心考点</strong>：预设 JVM 参数自动 Dump → MAT 分析大对象 → 追溯 GC Root 引用链。</p><p><strong>背诵话术</strong>：</p><blockquote><p>在医博系统导出大宽表（50 万患者就诊记录）时，原生 Apache POI 一次性将所有数据加载到堆内存，导致老年代被撑满，发生 OOM。</p><p><strong>事前准备（必须提前配置的 JVM 参数）</strong>：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br></pre></td><td class="code"><pre><span class="line">-XX:+HeapDumpOnOutOfMemoryError    <span class="comment"># OOM 时自动生成堆转储文件</span></span><br><span class="line">-XX:HeapDumpPath=/data/logs/       <span class="comment"># 指定 Dump 文件路径</span></span><br><span class="line">-XX:+PrintGCDateStamps             <span class="comment"># 打印 GC 日志（便于分析 GC 频率）</span></span><br><span class="line">-Xloggc:/data/logs/gc.log          <span class="comment"># GC 日志输出路径</span></span><br></pre></td></tr></table></figure><p><strong>事中（发生 OOM 后）</strong>：</p><ol><li>JVM 在 OOM 时自动生成 <code>.hprof</code> 堆转储文件（位于 <code>-XX:HeapDumpPath</code> 指定路径）。</li><li>如果没有提前配置，可手动执行 <code>jmap -dump:live,format=b,file=heap.hprof &lt;pid&gt;</code> 生成（会暂停应用一会儿）。</li></ol><p><strong>事后（MAT 分析）</strong>：</p><ol><li>将 <code>.hprof</code> 文件下载到本地，用 <strong>Eclipse MAT（Memory Analyzer Tool）</strong> 打开。</li><li>查看 <strong>Leak Suspects Report（内存泄漏猜想报告）</strong>，MAT 会自动分析并列出占用内存最大的可疑对象。</li><li>查看 <strong>Dominator Tree（支配树）</strong>：按占用内存从大到小排列所有对象，医博系统中立刻就能看到 <code>ArrayList&lt;PatientRecord&gt;</code> 一个对象占用了 90% 的堆内存。</li><li>右键该对象 → <strong>Path to GC Roots</strong>：追溯从 GC Roots 到该对象的完整引用链，找到是哪段代码持有了这个大 List，确认是 POI 导出方法中一次性查询并加载所有数据的代码。</li><li><strong>修复</strong>：将 POI 替换为 <strong>EasyExcel 的 SAX 流式写出</strong>，按批次（每批 1000 条）从数据库读取并写盘，JVM 内存占用降低 95%，OOM 彻底消除。</li></ol></blockquote><hr><h3 id="Q13：频繁-Full-GC-的排查思路"><a href="#Q13：频繁-Full-GC-的排查思路" class="headerlink" title="Q13：频繁 Full GC 的排查思路"></a>Q13：频繁 Full GC 的排查思路</h3><p><strong>核心考点</strong>：Full GC 的触发根因，分析 GC 日志。</p><p><strong>背诵话术</strong>：</p><blockquote><p>频繁 Full GC 会导致长时间 STW，严重影响服务可用性。排查思路如下：</p><p><strong>第一步：查看 GC 日志</strong>：通过 <code>-Xloggc</code> 输出的 GC 日志，或 Arthas 的 <code>gc</code> 命令，观察 Full GC 的频率、每次 Full GC 的持续时间、回收前后老年代内存变化。</p><p><strong>常见 Full GC 原因及处理</strong>：</p><ol><li><strong>大量大对象直接进入老年代</strong>：检查代码中是否有大数组或大 List 的一次性创建，改为流式处理或分批处理。调大 <code>-XX:PretenureSizeThreshold</code> 让大对象进入老年代前先尝试在新生代分配。</li><li><strong>老年代长期存活对象过多</strong>：检查是否有内存泄漏（如 ThreadLocal 未 remove、static 集合持有大量对象），用 MAT 分析 Dominator Tree。</li><li><strong>元空间不足（Metaspace OOM）</strong>：动态生成了大量类（如 Groovy&#x2F;BeanShell 脚本、大量 CGlib 代理），检查是否有类加载器泄漏（类无法卸载）。</li><li><strong>System.gc() 被主动调用</strong>：在代码中搜索 <code>System.gc()</code> 调用，或第三方库触发，加 JVM 参数 <code>-XX:+DisableExplicitGC</code> 禁止主动触发。</li><li><strong>Promotion Failure（晋升失败）</strong>：Minor GC 时新生代存活对象要晋升老年代，但老年代空间不足，触发 Full GC。考虑适当调大堆大小或老年代比例。</li></ol></blockquote><hr><h2 id="四、JVM-调优参数与工具"><a href="#四、JVM-调优参数与工具" class="headerlink" title="四、JVM 调优参数与工具"></a>四、JVM 调优参数与工具</h2><h3 id="Q14：生产环境核心-JVM-参数"><a href="#Q14：生产环境核心-JVM-参数" class="headerlink" title="Q14：生产环境核心 JVM 参数"></a>Q14：生产环境核心 JVM 参数</h3><p><strong>核心考点</strong>：各参数的含义和常用设置值，一次说清楚。</p><p><strong>背诵话术</strong>：</p><blockquote><p>生产环境常用的 JVM 启动参数（以 Spring Boot 应用为例）：</p><figure class="highlight bash"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br><span class="line">6</span><br><span class="line">7</span><br><span class="line">8</span><br><span class="line">9</span><br><span class="line">10</span><br><span class="line">11</span><br><span class="line">12</span><br><span class="line">13</span><br><span class="line">14</span><br><span class="line">15</span><br><span class="line">16</span><br><span class="line">17</span><br><span class="line">18</span><br><span class="line">19</span><br><span class="line">20</span><br><span class="line">21</span><br><span class="line">22</span><br><span class="line">23</span><br><span class="line">24</span><br><span class="line">25</span><br><span class="line">26</span><br><span class="line">27</span><br><span class="line">28</span><br><span class="line">29</span><br><span class="line">30</span><br><span class="line">31</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment"># 堆内存设置（初始 = 最大，避免动态扩容触发 GC）</span></span><br><span class="line">-Xms4g -Xmx4g</span><br><span class="line"></span><br><span class="line"><span class="comment"># 新生代大小（通常设为堆的 1/4~1/3）</span></span><br><span class="line">-Xmn1g</span><br><span class="line"></span><br><span class="line"><span class="comment"># 每个线程的栈大小（默认 512k~1m，递归深、局部变量多时适当调大）</span></span><br><span class="line">-Xss512k</span><br><span class="line"></span><br><span class="line"><span class="comment"># 元空间上限（防止无限扩展占用系统内存）</span></span><br><span class="line">-XX:MaxMetaspaceSize=256m</span><br><span class="line"></span><br><span class="line"><span class="comment"># 使用 G1 垃圾收集器（JDK9+ 默认，也可显式指定）</span></span><br><span class="line">-XX:+UseG1GC</span><br><span class="line"></span><br><span class="line"><span class="comment"># G1 的期望最大停顿时间（毫秒）</span></span><br><span class="line">-XX:MaxGCPauseMillis=200</span><br><span class="line"></span><br><span class="line"><span class="comment"># OOM 时自动生成堆转储文件（必配！）</span></span><br><span class="line">-XX:+HeapDumpOnOutOfMemoryError</span><br><span class="line">-XX:HeapDumpPath=/data/logs/heapdump.hprof</span><br><span class="line"></span><br><span class="line"><span class="comment"># 打印详细 GC 日志（含时间戳）</span></span><br><span class="line">-XX:+PrintGCDetails</span><br><span class="line">-XX:+PrintGCDateStamps</span><br><span class="line">-Xloggc:/data/logs/gc.log</span><br><span class="line"></span><br><span class="line"><span class="comment"># GC 日志文件滚动（防止日志文件无限增大）</span></span><br><span class="line">-XX:+UseGCLogFileRotation</span><br><span class="line">-XX:NumberOfGCLogFiles=5</span><br><span class="line">-XX:GCLogFileSize=20m</span><br></pre></td></tr></table></figure></blockquote><hr><h3 id="Q15：Arthas-常用诊断命令"><a href="#Q15：Arthas-常用诊断命令" class="headerlink" title="Q15：Arthas 常用诊断命令"></a>Q15：Arthas 常用诊断命令</h3><p><strong>核心考点</strong>：常用命令的功能和使用场景，面试时能流利说出 5~8 个。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>Arthas</strong> 是阿里开源的 Java 在线诊断工具，无需重启即可动态分析 JVM：</p><table><thead><tr><th>命令</th><th>功能</th><th>典型使用场景</th></tr></thead><tbody><tr><td><code>dashboard</code></td><td>实时展示系统概览（线程、GC、内存、CPU）</td><td>快速评估系统整体健康状态</td></tr><tr><td><code>thread -n 3</code></td><td>打印 CPU 占用最高的前 N 个线程堆栈</td><td>CPU 飙高时快速定位问题线程</td></tr><tr><td><code>thread -b</code></td><td>找出当前所有阻塞其他线程的罪魁祸首</td><td>排查死锁</td></tr><tr><td><code>jad com.xx.Service</code></td><td>反编译指定类为 Java 源码</td><td>确认线上运行的字节码是否和预期一致</td></tr><tr><td><code>watch com.xx.Service methodName &quot;{params,returnObj,throwExp}&quot;</code></td><td>监控方法的入参、返回值、异常</td><td>排查方法逻辑问题，无需加日志重启</td></tr><tr><td><code>trace com.xx.Service methodName</code></td><td>追踪方法内部各子调用的耗时分布</td><td>定位方法中哪一步最慢</td></tr><tr><td><code>ognl &quot;@com.xx.ConfigHolder@instance.getConfig()&quot;</code></td><td>直接执行 Java 表达式，访问静态变量&#x2F;调用方法</td><td>在线查看配置、缓存内容</td></tr><tr><td><code>heapdump /tmp/dump.hprof</code></td><td>生成堆内存快照</td><td>在线排查内存问题，不需要 jmap</td></tr><tr><td><code>classloader</code></td><td>查看类加载器信息</td><td>排查类加载冲突、Metaspace OOM</td></tr></tbody></table></blockquote><hr><h2 id="五、类加载机制"><a href="#五、类加载机制" class="headerlink" title="五、类加载机制"></a>五、类加载机制</h2><h3 id="Q16：类加载过程？什么是双亲委派模型？为什么要破坏它？"><a href="#Q16：类加载过程？什么是双亲委派模型？为什么要破坏它？" class="headerlink" title="Q16：类加载过程？什么是双亲委派模型？为什么要破坏它？"></a>Q16：类加载过程？什么是双亲委派模型？为什么要破坏它？</h3><p><strong>核心考点</strong>：类加载的三层 ClassLoader，双亲委派向上委托，Tomcat&#x2F;JDBC SPI 为什么破坏。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>类的完整生命周期</strong>：加载 → 验证 → 准备 → 解析 → 初始化 → 使用 → 卸载。</p></blockquote><blockquote><p><strong>类加载的三个核心 ClassLoader（JDK8）</strong>：</p><ol><li><strong>Bootstrap ClassLoader（启动类加载器）</strong>：C++ 实现，加载 <code>JAVA_HOME/jre/lib/</code> 下的核心类库（<code>rt.jar</code>、<code>java.lang.*</code>）。它是所有 ClassLoader 的”始祖”，Java 中无法直接引用它（用 <code>null</code> 表示）。</li><li><strong>Extension ClassLoader（扩展类加载器）</strong>：加载 <code>JAVA_HOME/jre/lib/ext/</code> 下的扩展类库。JDK9 被 <strong>Platform ClassLoader</strong> 取代。</li><li><strong>Application ClassLoader（应用类加载器）</strong>：加载 <code>classpath</code> 下用户自己写的类（<code>com.xxx.*</code>）。<code>ClassLoader.getSystemClassLoader()</code> 返回的就是它。</li></ol></blockquote><blockquote><p><strong>双亲委派模型（Parent Delegation）</strong>：一个类加载器收到类加载请求时，<strong>不会自己先加载，而是先把请求委派给父加载器</strong>，父加载器无法加载时（在自己的加载范围内找不到该类），才由子加载器自己尝试加载。最终形成一条链：Application → Extension → Bootstrap（找不到再逐层回退）。</p></blockquote><blockquote><p><strong>为什么需要双亲委派</strong>：防止核心类库被篡改。比如用户自己写了一个 <code>java.lang.String</code>，如果没有双亲委派，JVM 可能在用户 classpath 中加载了这个假的 String，造成安全漏洞。有了双亲委派后，Bootstrap ClassLoader 会优先加载 <code>rt.jar</code> 中真正的 <code>java.lang.String</code>，用户的冒牌货永远不会被加载。</p></blockquote><blockquote><p><strong>破坏双亲委派的三大场景</strong>：</p></blockquote><blockquote><ol><li><strong>Tomcat（Web 容器的类隔离）</strong>：Tomcat 需要支持多个 Web 应用独立部署，如果所有应用共享同一个 ClassLoader，则不同应用的同名类会互相冲突。Tomcat 为每个 Web 应用创建独立的 <code>WebAppClassLoader</code>，<strong>优先自己加载应用内的类</strong>（打破双亲委派，先自己尝试，失败了才委托给父加载器），实现了”应用 A 用 Spring 5、应用 B 用 Spring 6 互不干扰”的类隔离。</li></ol></blockquote><blockquote><ol start="2"><li><strong>JDBC SPI（ServiceLoader 困境）</strong>：<code>DriverManager</code> 类在 <code>rt.jar</code> 中（由 Bootstrap 加载），但具体的数据库驱动（如 <code>com.mysql.cj.jdbc.Driver</code>）在应用 classpath 下（由 Application 加载）。Bootstrap 加载的类无法通过常规的向上委托去加载 classpath 下的类。JDK 引入了 <strong>线程上下文类加载器（Thread Context ClassLoader）</strong>：在 Bootstrap 类中通过 <code>Thread.currentThread().getContextClassLoader()</code> 拿到应用的 ClassLoader，用它去加载具体驱动类，打破了双亲委派。</li></ol></blockquote><blockquote><ol start="3"><li><strong>热部署&#x2F;代码热替换</strong>：如 Arthas 的 <code>redefine</code> 功能、OSGi 模块化框架，需要在运行时卸载并重新加载某个类，传统的双亲委派模型不允许卸载类，只能通过自定义 ClassLoader 来打破这个限制。</li></ol></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> JVM </tag>
            
            <tag> 调优 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>Elasticsearch面试考题</title>
      <link href="/2026/06/08/Elasticsearch%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/Elasticsearch%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="07-Elasticsearch-·-高频面试考题背诵指南"><a href="#07-Elasticsearch-·-高频面试考题背诵指南" class="headerlink" title="07 - Elasticsearch · 高频面试考题背诵指南"></a>07 - Elasticsearch · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1%E4%BB%80%E4%B9%88%E6%98%AF%E5%80%92%E6%8E%92%E7%B4%A2%E5%BC%95%E5%92%8C%E6%AD%A3%E6%8E%92%E7%B4%A2%E5%BC%95%E6%9C%89%E4%BB%80%E4%B9%88%E5%8C%BA%E5%88%AB">Q1：什么是倒排索引？和正排索引有什么区别？</a></li><li><a href="#q2es-%E7%9A%84%E5%86%99%E5%85%A5%E6%B5%81%E7%A8%8B%E6%95%B0%E6%8D%AE%E5%A6%82%E4%BD%95%E8%90%BD%E7%9B%98">Q2：ES 的写入流程（数据如何落盘）？</a></li><li><a href="#q3%E4%B8%BA%E4%BB%80%E4%B9%88-es-%E6%98%AF%E8%BF%91%E5%AE%9E%E6%97%B6near-real-time%E8%80%8C%E9%9D%9E%E7%9C%9F%E6%AD%A3%E7%9A%84%E5%AE%9E%E6%97%B6">Q3：为什么 ES 是”近实时（Near Real-Time）”而非真正的实时？</a></li><li><a href="#q4es-%E7%9A%84%E6%9F%A5%E8%AF%A2%E6%B5%81%E7%A8%8Bquery-then-fetch">Q4：ES 的查询流程（Query Then Fetch）？</a></li><li><a href="#q5es-%E6%B7%B1%E5%88%86%E9%A1%B5from--size%E4%B8%BA%E4%BB%80%E4%B9%88%E6%85%A2%E5%A6%82%E4%BD%95%E8%A7%A3%E5%86%B3">Q5：ES 深分页（from + size）为什么慢？如何解决？</a></li><li><a href="#q6es-%E9%9B%86%E7%BE%A4%E7%9A%84%E6%A0%B8%E5%BF%83%E6%A6%82%E5%BF%B5node--shard--replica">Q6：ES 集群的核心概念（Node &#x2F; Shard &#x2F; Replica）</a></li><li><a href="#q7es-%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E5%86%99%E4%B8%80%E8%87%B4%E6%80%A7">Q7：ES 如何保证写一致性？</a></li><li><a href="#q8es-%E5%92%8C-mysql-%E5%9C%A8%E6%90%9C%E7%B4%A2%E5%9C%BA%E6%99%AF%E4%B8%8B%E5%A6%82%E4%BD%95%E9%85%8D%E5%90%88">Q8：ES 和 MySQL 在搜索场景下如何配合？</a></li><li><a href="#q9%E5%88%86%E7%89%87%E6%95%B0%E5%92%8C%E5%89%AF%E6%9C%AC%E6%95%B0%E7%9A%84%E8%AE%BE%E8%AE%A1%E5%8E%9F%E5%88%99">Q9：分片数和副本数的设计原则？</a></li><li><a href="#q10es-%E4%B8%BA%E4%BB%80%E4%B9%88%E6%AF%94-mysql-%E7%9A%84-like-%E6%9F%A5%E8%AF%A2%E5%BF%AB">Q10：ES 为什么比 MySQL 的 LIKE 查询快？</a></li><li><a href="#q11es-%E5%92%8C-solr-%E7%9A%84%E9%80%89%E5%9E%8B%E5%AF%B9%E6%AF%94">Q11：ES 和 Solr 的选型对比？</a></li></ul><hr><span id="more"></span><h2 id="一、核心原理"><a href="#一、核心原理" class="headerlink" title="一、核心原理"></a>一、核心原理</h2><h3 id="Q1：什么是倒排索引？和正排索引有什么区别？"><a href="#Q1：什么是倒排索引？和正排索引有什么区别？" class="headerlink" title="Q1：什么是倒排索引？和正排索引有什么区别？"></a>Q1：什么是倒排索引？和正排索引有什么区别？</h3><p><strong>核心考点</strong>：倒排索引的 Term → PostingList 映射结构，是 ES 全文检索的基石。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>正排索引</strong>：以文档 ID 为 Key，记录文档中包含了哪些词（文档 → 关键词）。MySQL 的 B+Tree 索引就是正排索引。适合按 ID 精确查找文档，但不适合”搜索包含某关键词的所有文档”——需要全表扫描每篇文档的内容。</p><p><strong>倒排索引</strong>：以<strong>词条（Term）</strong> 为 Key，记录这个词出现在哪些文档中（关键词 → 文档 ID 列表）。结构如下：</p><ul><li><strong>Term Dictionary（词条字典）</strong>：所有去重后的词条，按字典序排序存储，使用 <strong>FST（Finite State Transducer，有限状态转换器）</strong> 做前缀压缩，内存占用极低。</li><li><strong>Posting List（倒排列表）</strong>：每个 Term 对应的文档 ID 列表，记录该词在哪些文档中出现、出现频率（TF）、出现位置（Position）等信息。多个 Posting List 之间的交集（AND）和并集（OR）就是布尔查询的本质。</li><li><strong>Term Index（词条索引）</strong>：Term Dictionary 的 B-Tree 索引，加速定位目标 Term，避免全量扫描字典。</li></ul><p><strong>搜索过程示例</strong>：搜索”招行”时，ES 先去 Term Index 找到”招行”在 Term Dictionary 中的位置，读取对应的 Posting List（文档 ID 列表），再根据这些 ID 去取文档内容，返回结果。整个过程不需要逐文档扫描。</p></blockquote><hr><h3 id="Q2：ES-的写入流程（数据如何落盘）？"><a href="#Q2：ES-的写入流程（数据如何落盘）？" class="headerlink" title="Q2：ES 的写入流程（数据如何落盘）？"></a>Q2：ES 的写入流程（数据如何落盘）？</h3><p><strong>核心考点</strong>：BUFFER → OS Cache(refresh) → Translog → 磁盘(flush)，近实时的物理原因。</p><p><strong>背诵话术</strong>：</p><blockquote><p>ES 的写入流程涉及多个组件，核心是<strong>延迟写磁盘 + 写 Translog 保障可靠性</strong>：</p></blockquote><blockquote><ol><li>客户端发送一条写请求到某分片（路由到主分片，主分片写完转发到副本分片）。</li><li>数据先写入<strong>内存 Buffer</strong>（此时不可被搜索）。</li><li>同时将写入操作记录到 <strong>Translog（事务日志）</strong>，Translog 每 5 秒 <code>fsync</code> 一次到磁盘（或每次写请求都 <code>fsync</code>，取决于 <code>index.translog.durability</code> 配置）。</li><li><strong>refresh（默认 1s 一次）</strong>：将内存 Buffer 中的数据生成一个新的 <strong>Segment（段）</strong>，写入 <strong>OS Cache（操作系统页缓存）</strong>，此时数据变为<strong>可搜索</strong>。同时清空内存 Buffer。</li><li><strong>flush（默认 30 分钟一次 &#x2F; Translog 达到 512MB）</strong>：通过 <code>fsync</code> 将 OS Cache 中的 Segment 强制刷到磁盘，并清空旧的 Translog（生成新的 Translog 文件），形成<strong>段提交点（Commit Point）</strong>。</li></ol></blockquote><blockquote><p><strong>关键点</strong>：refresh 之前的文档不可搜索；refresh 之后、flush 之前，文档在 OS Cache 中（内存可查但磁盘没有持久化），此时节点宕机会丢失数据，但可通过 Translog 回放恢复。</p></blockquote><hr><h3 id="Q3：为什么-ES-是”近实时（Near-Real-Time）”而非真正的实时？"><a href="#Q3：为什么-ES-是”近实时（Near-Real-Time）”而非真正的实时？" class="headerlink" title="Q3：为什么 ES 是”近实时（Near Real-Time）”而非真正的实时？"></a>Q3：为什么 ES 是”近实时（Near Real-Time）”而非真正的实时？</h3><p><strong>核心考点</strong>：默认 1s 的 refresh 间隔是根本原因。</p><p><strong>背诵话术</strong>：</p><blockquote><p>ES 被称为”近实时”（NRT），原因是：数据写入后不会立即被搜索到，必须等到默认<strong>每 1 秒</strong>执行一次 <strong>refresh</strong> 操作（生成新 Segment 并刷新到 OS Cache）之后，新数据才会对搜索可见。这 1 秒的 delay 就是”近”的来源。</p></blockquote><blockquote><p>如果要强制实时可见，可以在写入后显式调用 <code>GET /index/_refresh</code> API 主动触发 refresh，但这会消耗大量资源（频繁生成小 Segment），生产不推荐。</p></blockquote><blockquote><p><strong>为什么 MySQL 也是近实时</strong>：类似地，MySQL InnoDB 的 Buffer Pool 中脏页不立即刷盘，事务提交后数据先写 Redo Log，通过 Checkpoint 机制定期刷盘，也有一段时间窗口。</p></blockquote><hr><h3 id="Q4：ES-的查询流程（Query-Then-Fetch）？"><a href="#Q4：ES-的查询流程（Query-Then-Fetch）？" class="headerlink" title="Q4：ES 的查询流程（Query Then Fetch）？"></a>Q4：ES 的查询流程（Query Then Fetch）？</h3><p><strong>核心考点</strong>：两阶段查询，协调节点先拿 ID 和评分，再按 ID 拉取文档内容。</p><p><strong>背诵话术</strong>：</p><blockquote><p>ES 的查询分<strong>两个阶段</strong>：<strong>Query 阶段</strong> + <strong>Fetch 阶段</strong>。</p></blockquote><blockquote><p><strong>Query 阶段（打分 + 过滤）</strong>：</p><ol><li>协调节点（Coordinator Node）接收到查询请求，将请求转发给索引涉及的<strong>所有分片</strong>（每个主分片或副本分片）。</li><li>每个分片在本地执行查询（打分、排序、分页），将结果中的 <strong>文档 ID + 排序值</strong>（<code>_id</code>、<code>_score</code>、<code>sort</code> 字段）返回给协调节点，并返回分片级结果总数。</li><li>协调节点合并各分片返回的结果，按分数排序后，确定最终需要的文档 ID 列表（优先级队列）。</li></ol></blockquote><blockquote><p><strong>Fetch 阶段（内容拉取）</strong>：</p><ol><li>协调节点根据确定的文档 ID，向相关分片发送 Multi-Get 请求，拉取完整的文档内容（<code>_source</code>）。</li><li>取得完整的文档后，组装为最终响应返回给客户端。</li></ol></blockquote><blockquote><p><strong>为什么分两阶段</strong>：如果直接拉取所有分片的完整文档，网络传输数据量巨大，协调节点排序压力也很大。先拿轻量的 ID+分数，排序后再按需拉取内容，效率最高。</p></blockquote><hr><h3 id="Q5：ES-深分页（from-size）为什么慢？如何解决？"><a href="#Q5：ES-深分页（from-size）为什么慢？如何解决？" class="headerlink" title="Q5：ES 深分页（from + size）为什么慢？如何解决？"></a>Q5：ES 深分页（from + size）为什么慢？如何解决？</h3><p><strong>核心考点</strong>：from+size 需要每个分片取出 top(from+size) 条，协调节点再合并排序丢弃前 from 条。</p><p><strong>背诵话术</strong>：</p><blockquote><p><code>GET /index/_search { &quot;from&quot;: 10000, &quot;size&quot;: 10 }</code> 慢的根本原因：</p><ul><li>假设有 5 个分片，请求 <code>from=10000, size=10</code> 时，协调节点要求<strong>每个分片</strong>都取出 <strong>Top 10010</strong> 条结果（文档 ID + 分数），总共需要处理 <strong>5 × 10010 &#x3D; 50050 条数据</strong>。</li><li>协调节点在内存中合并这 50050 条数据并全局排序，再丢弃前 10000 条，只返回 10 条。</li><li>分片越多、from 越大，内存和 CPU 消耗越高，ES 通过 <code>max_result_window</code> 默认限制 from + size ≤ 10000。</li></ul></blockquote><blockquote><p><strong>三种解决方案</strong>：</p><table><thead><tr><th>方案</th><th>适用场景</th><th>原理</th><th>缺点</th></tr></thead><tbody><tr><td><strong>Scroll</strong></td><td>全量数据导出、离线批处理</td><td>生成一个快照游标，后续翻页基于该快照顺序拉取</td><td>占用 ES 资源（有存活时间）、不支持实时数据、不可跳页</td></tr><tr><td><strong>Search After</strong>（推荐）</td><td>实时搜索的深度翻页</td><td>每次查询记录最后一条的排序值，下次用 <code>search_after</code> 接续查询</td><td>只能点”下一页”，不能跳页、需配合唯一排序值（如 <code>_id</code> 做 tiebreaker）</td></tr><tr><td><strong>限制翻页深度（业务层面）</strong></td><td>用户搜索场景</td><td>对搜索结果限制最多翻到第 N 页（如百度只显示前 76 页），深度翻页需求走异步导出</td><td>不满足所有业务需求</td></tr></tbody></table></blockquote><hr><h2 id="二、集群架构"><a href="#二、集群架构" class="headerlink" title="二、集群架构"></a>二、集群架构</h2><h3 id="Q6：ES-集群的核心概念（Node-Shard-Replica）"><a href="#Q6：ES-集群的核心概念（Node-Shard-Replica）" class="headerlink" title="Q6：ES 集群的核心概念（Node &#x2F; Shard &#x2F; Replica）"></a>Q6：ES 集群的核心概念（Node &#x2F; Shard &#x2F; Replica）</h3><p><strong>核心考点</strong>：主分片负责写、副本分片负责读，分片数是水平扩展的基础。</p><p><strong>背诵话术</strong>：</p><blockquote><p>ES 集群由以下核心概念组成：</p><ul><li><p><strong>Node（节点）</strong>：ES 的服务实例。按角色分为：</p><ul><li><strong>Master Node</strong>：负责集群元数据管理（索引创建&#x2F;删除、分片分配、节点上下线），不处理读写。通过 Bully 算法选举（ES 7.x 之后）。</li><li><strong>Data Node</strong>：存储数据，处理查询和写入。是集群的工作主力。</li><li><strong>Coordinating Node</strong>：接收客户端请求，分发到数据节点并汇总结果。每个节点都可以充当协调节点。</li><li><strong>Ingest Node</strong>：对写入数据做预处理（如 Grok 解析日志、字段增加）。</li></ul></li><li><p><strong>Index（索引）</strong>：逻辑上的数据库，划分为多个物理分片。</p></li><li><p><strong>Primary Shard（主分片）</strong>：数据的第一存放地，所有写请求先写入主分片。主分片数量<strong>索引创建时确定，不能修改</strong>（影响数据路由的 hash 算法）。</p></li><li><p><strong>Replica Shard（副本分片）</strong>：主分片的备份，提供高可用和分担查询压力。副本数可以动态调整。</p></li></ul></blockquote><blockquote><p><strong>数据路由</strong>：<code>shard = hash(_routing) % num_primary_shards</code>（默认 <code>_routing</code> &#x3D; <code>_id</code>），所以主分片数不能修改（改了 hash 结果全变）。</p></blockquote><blockquote><p><strong>在招行项目中</strong>：规则库的检索量达百万级，我们部署 3 个 Data Node，设置每个索引 6 个主分片 + 1 个副本（共 12 个分片分布在 3 个节点上），确保单节点故障不影响搜索可用性。</p></blockquote><hr><h3 id="Q7：ES-如何保证写一致性？"><a href="#Q7：ES-如何保证写一致性？" class="headerlink" title="Q7：ES 如何保证写一致性？"></a>Q7：ES 如何保证写一致性？</h3><p><strong>核心考点</strong>：<code>wait_for_active_shards</code> 和 <code>consistency</code> 配置，主分片 → 副本分片的同步写入。</p><p><strong>背诵话术</strong>：</p><blockquote><p>ES 的写入一致性通过两个机制保证：</p></blockquote><blockquote><ol><li><strong><code>wait_for_active_shards</code></strong>：控制写操作需要等待多少个分片确认才返回成功。<code>wait_for_active_shards=1</code>（默认）表示只要主分片写入成功就返回，不等待副本分片（性能高但有数据丢失风险）；<code>wait_for_active_shards=all</code> 表示必须主分片 + 所有副本分片都写入成功。</li></ol></blockquote><blockquote><ol start="2"><li><strong>Translog 持久化</strong>：即使主分片返回成功后立即宕机，写入操作也会记录在 Translog 中，节点恢复时通过重放 Translog 恢复数据。类似 MySQL Redo Log 的设计。</li></ol></blockquote><blockquote><p><strong>与 Kafka ISR 的对比</strong>：ES 的 <code>wait_for_active_shards=all</code> 类似 Kafka 的 <code>acks=all</code>；ES 的 Translog 类似 Kafka 的 Partition Log。两者都是用”先写日志 + 异步刷盘”来平衡性能与可靠性。</p></blockquote><hr><h2 id="三、ES-vs-MySQL-对比"><a href="#三、ES-vs-MySQL-对比" class="headerlink" title="三、ES vs MySQL 对比"></a>三、ES vs MySQL 对比</h2><h3 id="Q8：ES-和-MySQL-在搜索场景下如何配合？"><a href="#Q8：ES-和-MySQL-在搜索场景下如何配合？" class="headerlink" title="Q8：ES 和 MySQL 在搜索场景下如何配合？"></a>Q8：ES 和 MySQL 在搜索场景下如何配合？</h3><p><strong>核心考点</strong>：各自适用的查询场景，强一致性数据在 MySQL，全文检索和聚合在 ES。</p><p><strong>背诵话术</strong>：</p><blockquote><p>MySQL 擅长精确查询和事务操作，ES 擅长全文检索、模糊匹配、聚合分析。大规模系统里两者配合使用：</p></blockquote><blockquote><ul><li><strong>MySQL 作为 System of Record（记录系统）</strong>：保存业务数据的权威版本。</li><li><strong>ES 作为 Search Index（搜索索引）</strong>：通过 Canal 或数据同步工具，将 MySQL 业务数据近实时同步到 ES，构建搜索索引。</li></ul><p><strong>招行项目的实际方案</strong>：调度规则配置数据存储在 MySQL，通过 Canal 订阅 Binlog，同时更新 Redis 缓存 + ES 搜索索引。业务人员按规则名称、业务线等关键词检索时，直接查 ES（毫秒级返回），不查 MySQL 做模糊 LIKE 查询（会导致全表扫描），配置详情查 Redis&#x2F;MySQL 获取实时精确数据。</p><p><strong>核心原则</strong>：ES 是衍生的搜索视图，MySQL 是权威数据源，数据不一致时以 MySQL 为准。</p></blockquote><hr><h2 id="四、分片设计与优化"><a href="#四、分片设计与优化" class="headerlink" title="四、分片设计与优化"></a>四、分片设计与优化</h2><h3 id="Q9：分片数和副本数的设计原则？"><a href="#Q9：分片数和副本数的设计原则？" class="headerlink" title="Q9：分片数和副本数的设计原则？"></a>Q9：分片数和副本数的设计原则？</h3><p><strong>核心考点</strong>：分片数由数据量和写入 QPS 决定，副本数由查询 QPS 和可靠性要求决定。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><p><strong>主分片数设计</strong>：</p><ul><li>单个分片的数据量建议在 <strong>10~50GB</strong>，过大影响迁移和恢复速度，过小元数据开销大。</li><li>分片数不宜过多：每个分片是一个 Lucene 索引，占用文件句柄和内存，ES 每个节点的分片数建议不超过堆内存的每 GB 20 个分片（如 8GB 堆 ≤ 160 个分片）。</li><li>写并发高时，适当增加主分片数（写请求分散到更多主分片上并行处理）。</li></ul></li><li><p><strong>副本数设计</strong>：</p><ul><li>至少 1 个副本（保证单节点故障数据不丢失）。</li><li>读 QPS 高时增加副本（副本可分担查询请求），但要付出额外的存储和写入同步成本。</li><li>副本数 &#x3D; N，意味着可以容 N 个节点故障同时数据不丢失。</li></ul></li></ul><p><strong>招行项目配置</strong>：规则库数据量约 40GB，设置了 6 个主分片（每分片 6~7GB）+ 1 个副本，3 个 Data Node 各承载 4 个分片（包含主副混合），查询 QPS 高峰约 5000 时保持稳定。</p></blockquote><hr><h2 id="五、面试必问对比题"><a href="#五、面试必问对比题" class="headerlink" title="五、面试必问对比题"></a>五、面试必问对比题</h2><h3 id="Q10：ES-为什么比-MySQL-的-LIKE-查询快？"><a href="#Q10：ES-为什么比-MySQL-的-LIKE-查询快？" class="headerlink" title="Q10：ES 为什么比 MySQL 的 LIKE 查询快？"></a>Q10：ES 为什么比 MySQL 的 LIKE 查询快？</h3><p><strong>核心考点</strong>：倒排索引是 Term → DocID 的 O(1) 命中，LIKE ‘%xxx%’ 是全表扫描。</p><p><strong>背诵话术</strong>：</p><blockquote><p>MySQL 的 <code>LIKE &#39;%xxx%&#39;</code> 前导通配符导致 B+Tree 索引完全失效（无法确定前缀，索引有序性用不上），必须全表扫描每行数据。百万行表做一次 <code>LIKE</code>  就扫百万行，并发一大直接打崩。</p></blockquote><blockquote><p>ES 通过倒排索引，把所有词条预先建立好 Term → DocID 的映射。搜索”招行”时，直接去 Term Dictionary 中二分查找该 Term，拿到 Posting List（包含该词的文档 ID 列表），整个过程是 O(logN + M)（N&#x3D;词条数，M&#x3D;匹配文档数），不需要逐文档扫描，比全表扫描快几个数量级。</p></blockquote><blockquote><p><strong>核心差异</strong>：MySQL 是”读文档 → 找词条”（正排），适合精确查一条数据；ES 是”找词条 → 读文档”（倒排），适合”搜索包含某词的所有文档”。</p></blockquote><hr><h3 id="Q11：ES-和-Solr-的选型对比？"><a href="#Q11：ES-和-Solr-的选型对比？" class="headerlink" title="Q11：ES 和 Solr 的选型对比？"></a>Q11：ES 和 Solr 的选型对比？</h3><p><strong>核心考点</strong>：生态活跃度、实时性、分布式易用性。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li>ES 基于 JSON 的 RESTful API 更易上手，Solr 传统使用 XML。</li><li>ES 的分布式集群管理更成熟（自动分片分配、节点发现），Solr 依赖 ZooKeeper。</li><li>ES 社区活跃度远超 Solr，与 ELK（Elasticsearch + Logstash + Kibana）生态深度绑定。</li><li>所以生产项目（包括招行）选型时，ELK 生态的日志检索、监控告警是一大加分项。</li></ul></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> Elasticsearch </tag>
            
            <tag> ES </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>计算机网络基础面试考题</title>
      <link href="/2026/06/08/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E5%9F%BA%E7%A1%80%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/%E8%AE%A1%E7%AE%97%E6%9C%BA%E7%BD%91%E7%BB%9C%E5%9F%BA%E7%A1%80%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="08-计算机网络基础-·-高频面试考题背诵指南"><a href="#08-计算机网络基础-·-高频面试考题背诵指南" class="headerlink" title="08 - 计算机网络基础 · 高频面试考题背诵指南"></a>08 - 计算机网络基础 · 高频面试考题背诵指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#q1tcp-%E4%B8%89%E6%AC%A1%E6%8F%A1%E6%89%8B%E8%BF%87%E7%A8%8B%E4%B8%BA%E4%BB%80%E4%B9%88%E6%98%AF%E4%B8%89%E6%AC%A1%E8%80%8C%E4%B8%8D%E6%98%AF%E4%B8%A4%E6%AC%A1%E6%88%96%E5%9B%9B%E6%AC%A1">Q1：TCP 三次握手过程？为什么是三次而不是两次或四次？</a></li><li><a href="#q2tcp-%E5%9B%9B%E6%AC%A1%E6%8C%A5%E6%89%8B%E8%BF%87%E7%A8%8B%E4%B8%BA%E4%BB%80%E4%B9%88%E6%98%AF%E5%9B%9B%E6%AC%A1time_wait-%E4%B8%BA%E4%BB%80%E4%B9%88%E6%98%AF-2msl">Q2：TCP 四次挥手过程？为什么是四次？TIME_WAIT 为什么是 2MSL？</a></li><li><a href="#q3tcp-%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81%E5%8F%AF%E9%9D%A0%E4%BC%A0%E8%BE%93">Q3：TCP 如何保证可靠传输？</a></li><li><a href="#q4tcp-%E6%8B%A5%E5%A1%9E%E6%8E%A7%E5%88%B6%E7%9A%84%E5%9B%9B%E4%B8%AA%E9%98%B6%E6%AE%B5">Q4：TCP 拥塞控制的四个阶段</a></li><li><a href="#q5http-10--11--20--30-%E7%9A%84%E6%A0%B8%E5%BF%83%E5%8C%BA%E5%88%AB">Q5：HTTP 1.0 &#x2F; 1.1 &#x2F; 2.0 &#x2F; 3.0 的核心区别</a></li><li><a href="#q6https-%E7%9A%84%E6%8F%A1%E6%89%8B%E8%BF%87%E7%A8%8Btls-12--13">Q6：HTTPS 的握手过程？（TLS 1.2 &#x2F; 1.3）</a></li><li><a href="#q7bio--nio--aio-%E7%9A%84%E5%8C%BA%E5%88%AB">Q7：BIO &#x2F; NIO &#x2F; AIO 的区别？</a></li><li><a href="#q8select--poll--epoll-%E5%8C%BA%E5%88%AB">Q8：select &#x2F; poll &#x2F; epoll 区别？</a></li><li><a href="#q9cookiesessionjwt--token-%E7%9A%84%E5%8C%BA%E5%88%AB">Q9：Cookie、Session、JWT &#x2F; Token 的区别？</a></li><li><a href="#q10%E6%B5%8F%E8%A7%88%E5%99%A8%E8%BE%93%E5%85%A5-url-%E5%90%8E%E5%8F%91%E7%94%9F%E4%BA%86%E4%BB%80%E4%B9%88">Q10：浏览器输入 URL 后发生了什么？</a></li></ul><hr><span id="more"></span><h2 id="一、TCP"><a href="#一、TCP" class="headerlink" title="一、TCP"></a>一、TCP</h2><h3 id="Q1：TCP-三次握手过程？为什么是三次而不是两次或四次？"><a href="#Q1：TCP-三次握手过程？为什么是三次而不是两次或四次？" class="headerlink" title="Q1：TCP 三次握手过程？为什么是三次而不是两次或四次？"></a>Q1：TCP 三次握手过程？为什么是三次而不是两次或四次？</h3><p><strong>核心考点</strong>：握手的目的（同步序列号 + 验证双方收发能力）决定必须是三次。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>三次握手的过程</strong>：</p><ol><li>客户端 → 服务端：<code>SYN = 1, seq = x</code>（客户端进入 SYN-SENT 状态）</li><li>服务端 → 客户端：<code>SYN = 1, ACK = 1, seq = y, ack = x + 1</code>（服务端进入 SYN-RCVD 状态）</li><li>客户端 → 服务端：<code>ACK = 1, seq = x + 1, ack = y + 1</code>（双方进入 ESTABLISHED 状态）</li></ol></blockquote><blockquote><p><strong>为什么是三次？</strong></p><ul><li><strong>核心目的</strong>：三次握手的本质是让<strong>双方确认自己的发送能力和对方的接收能力都正常</strong>。<ul><li>第一次：服务端确认客户端的发送能力正常。</li><li>第二次：客户端确认服务端的收发能力都正常。</li><li>第三次：服务端确认客户端的接收能力正常（服务端的发送能力被客户端收到并回应了）。</li></ul></li><li><strong>为什么不是两次</strong>：两次握手只能确认客户端的发送和服务端的接收，但服务端不知道客户端的接收是否正常，也无法确认客户端是否还活着。如果客户端已关闭，服务端单方面建立连接会造成资源浪费。</li><li><strong>为什么不是四次</strong>：第二次握手中 <code>SYN + ACK</code> 可以合并为一个报文发送，没必要分开发，三次已经是最优解。</li></ul></blockquote><hr><h3 id="Q2：TCP-四次挥手过程？为什么是四次？TIME-WAIT-为什么是-2MSL？"><a href="#Q2：TCP-四次挥手过程？为什么是四次？TIME-WAIT-为什么是-2MSL？" class="headerlink" title="Q2：TCP 四次挥手过程？为什么是四次？TIME_WAIT 为什么是 2MSL？"></a>Q2：TCP 四次挥手过程？为什么是四次？TIME_WAIT 为什么是 2MSL？</h3><p><strong>核心考点</strong>：全双工通信决定了挥手必须是四次；TIME_WAIT 保证最后的 ACK 能重传。</p><p><strong>背诵话术</strong>：</p><blockquote><p><strong>四次挥手的过程</strong>：</p><ol><li>主动关闭方 → 被动方：<code>FIN = 1, seq = u</code>（主动方进入 FIN-WAIT-1）</li><li>被动方 → 主动方：<code>ACK = 1, seq = v, ack = u + 1</code>（被动方进入 CLOSE-WAIT，主动方进入 FIN-WAIT-2）</li><li>被动方 → 主动方：<code>FIN = 1, ACK = 1, seq = w, ack = u + 1</code>（被动方进入 LAST-ACK）</li><li>主动方 → 被动方：<code>ACK = 1, seq = u + 1, ack = w + 1</code>（主动方进入 TIME-WAIT，2MSL 后关闭；被动方收到后直接关闭）</li></ol></blockquote><blockquote><p><strong>为什么是四次？</strong> TCP 是<strong>全双工</strong>通信，每个方向的数据通道需要独立关闭。被动方收到 FIN 后，可能还有数据要发送（<code>ACK</code> 和 <code>FIN</code> 不能合并），所以需要四次。如果被动方没有数据要发，可以合并步骤 2 和 3（变成三次挥手），HTTP 长连接关闭时常见。</p></blockquote><blockquote><p><strong>TIME_WAIT 为什么是 2MSL（Maximum Segment Lifetime，最大报文生存时间）？</strong></p><ul><li><strong>MSL</strong> 是 TCP 报文在网络上最长的存活时间（通常 30s~2 分钟）。</li><li><strong>2MSL ≈ 一去一回的最大时间</strong>：确保主动方最后发送的 ACK 要么被对方收到（正常关闭），要么对方超时重传 FIN 时主动方能再次响应 —— 如果主动方直接关闭，重传的 FIN 无人响应，被动方无法正常关闭。</li><li><strong>让旧连接的所有报文都在网络中彻底消失</strong>，防止旧连接的报文被新连接（相同四元组）误收。在有大量的短连接的服务器上，过多的 TIME_WAIT（<code>netstat -an | grep TIME_WAIT</code> 特别多）会消耗端口资源，可通过内核参数 <code>tcp_tw_reuse</code> 复用或调优。</li></ul></blockquote><hr><h3 id="Q3：TCP-如何保证可靠传输？"><a href="#Q3：TCP-如何保证可靠传输？" class="headerlink" title="Q3：TCP 如何保证可靠传输？"></a>Q3：TCP 如何保证可靠传输？</h3><p><strong>核心考点</strong>：校验和 + 序列号与确认 + 重传超时 + 滑动窗口（流量控制）+ 拥塞控制，五大机制。</p><p><strong>背诵话术</strong>：</p><blockquote><p>TCP 通过以下五大机制保证可靠传输：</p></blockquote><blockquote><ol><li><strong>校验和（Checksum）</strong>：TCP 报文头部包含 16 位的校验和字段，接收方会重新计算校验和并与报文头中的对比，若不一致说明数据在传输中损坏，丢弃该报文并触发重传。</li></ol></blockquote><blockquote><ol start="2"><li><strong>序列号与确认应答（ACK）</strong>：每个 TCP 报文携带一个头部序列号（seq），接收方收到后会返回一个 <code>ACK</code> 应答信号（ack &#x3D; seq + 数据长度），告诉发送方”这些数据我收到了，请发下一批”。发送方发完后如果没在规定时间内收到 ACK，会触发超时重传。</li></ol></blockquote><blockquote><ol start="3"><li><strong>超时重传（RTO）</strong>：TCP 每发送一个报文后启动一个计时器，如果超时（RTO）内未收到 ACK，就重传该报文。RTO 通过 RTT（Round Trip Time，往返时间）动态计算（使用 Jacobson 公式），比固定值更适应网络变化。</li></ol></blockquote><blockquote><ol start="4"><li><strong>滑动窗口／流量控制</strong>：接收方通过”窗口”大小告诉发送方自己还有多少缓冲区空间可用，发送方最多发送窗口大小内的数据而不等 ACK，<strong>发送窗口 &#x3D; min(拥塞窗口, 接收窗口)</strong>。防止发送方<code>塞满</code>接收方的缓冲区。</li></ol></blockquote><blockquote><ol start="5"><li><strong>拥塞控制</strong>（详见 Q4）：防止发送方向网络注入过多数据，引发网络拥塞（路由器缓冲区溢出丢包）。</li></ol></blockquote><hr><h3 id="Q4：TCP-拥塞控制的四个阶段"><a href="#Q4：TCP-拥塞控制的四个阶段" class="headerlink" title="Q4：TCP 拥塞控制的四个阶段"></a>Q4：TCP 拥塞控制的四个阶段</h3><p><strong>核心考点</strong>：慢启动 → 拥塞避免 → 快重传 → 快恢复。</p><p><strong>背诵话术</strong>：</p><blockquote><p>TCP 的拥塞控制通过<strong>拥塞窗口（cwnd）</strong> 动态调整发送速率，分四个阶段：</p></blockquote><blockquote><ol><li><strong>慢启动（Slow Start）</strong>：连接刚建立时 cwnd &#x3D; 1 个 MSS（最大报文段大小），每收到一个 ACK，cwnd 翻倍（指数增长），直到达到 <code>ssthresh</code>（慢启动阈值）或发生丢包。</li></ol></blockquote><blockquote><ol start="2"><li><strong>拥塞避免（Congestion Avoidance）</strong>：cwnd ≥ ssthresh 后，每收到一个 ACK，cwnd +&#x3D; 1（线性增长），慢慢试探网络的承受上限。</li></ol></blockquote><blockquote><ol start="3"><li><strong>快重传（Fast Retransmit）</strong>：如果发送方连续收到 3 个相同的 ACK（Duplicate ACK），说明中间有一个报文丢失了（后面的都到了但缺这一个），不等 RTO 超时，立刻重传该丢失的报文。</li></ol></blockquote><blockquote><ol start="4"><li><strong>快恢复（Fast Recovery）</strong>：发生快重传后，不等 RTO，将 ssthresh 设为当前 cwnd&#x2F;2，cwnd &#x3D; ssthresh + 3，然后进入拥塞避免阶段线性增长。相比 RTO 后 cwnd 直接从 1 重新慢启动，快恢复大幅缩减了丢包后的吞吐恢复时间。</li></ol></blockquote><blockquote><p><strong>如果没有快重传&#x2F;快恢复</strong>：发生丢包后只能等 RTO 超时（几百毫秒），期间暂停发送数据，吞吐严重下降。</p></blockquote><hr><h2 id="二、HTTP"><a href="#二、HTTP" class="headerlink" title="二、HTTP"></a>二、HTTP</h2><h3 id="Q5：HTTP-1-0-1-1-2-0-3-0-的核心区别"><a href="#Q5：HTTP-1-0-1-1-2-0-3-0-的核心区别" class="headerlink" title="Q5：HTTP 1.0 &#x2F; 1.1 &#x2F; 2.0 &#x2F; 3.0 的核心区别"></a>Q5：HTTP 1.0 &#x2F; 1.1 &#x2F; 2.0 &#x2F; 3.0 的核心区别</h3><p><strong>核心考点</strong>：从短连接→长连接→多路复用→QUIC 的演进路线。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><p><strong>HTTP 1.0</strong>：每次请求都要建立一个新的 TCP 连接（短连接），请求结束后立即关闭。连接建立和释放的开销很大（三次握手 + 四次挥手）。每个请求&#x2F;响应都必须按顺序一一对应。</p></li><li><p><strong>HTTP 1.1</strong>：引入了<strong>长连接（Keep-Alive）</strong>，一个 TCP 连接可以复用多次请求&#x2F;响应，减少了大量握手开销。支持<strong>管线化（Pipelining）</strong>，一次发送多个请求不等响应（但响应必须按顺序返回，<strong>队头阻塞（Head-of-Line Blocking）问题依然严重</strong>）。增加了 <code>Host</code> 头，支持一台服务器托管多个虚拟主机。</p></li><li><p><strong>HTTP 2.0</strong>（基于二进制帧，不是文本）：</p><ul><li><strong>多路复用（Multiplexing）</strong>：在一个 TCP 连接上同时发送多个请求的帧（Stream），通过 Stream ID 区分，响应可以交错返回，真正解决了 HTTP 层的队头阻塞。</li><li><strong>头部压缩（HPACK）</strong>：使用哈夫曼编码和静态&#x2F;动态表压缩请求头。</li><li><strong>服务器推送（Server Push）</strong>：服务端可主动向客户端推送资源（如 HTML 中的 CSS 文件），减少额外请求。</li><li><strong>仍然有 TCP 层的队头阻塞问题</strong>：因为 TCP 是面向字节流的，如果底层 TCP 有一个报文丢失，后面所有的 HTTP Stream 都得等待这个丢失包重传，即使它们的帧已到达也无法使用（HTTP 层的队头阻塞解决了，但 TCP 层的队头阻塞仍在）。</li></ul></li><li><p><strong>HTTP 3.0（基于 QUIC，底层为 UDP）</strong>：</p><ul><li>用 <strong>QUIC 协议替代了 TCP</strong>，底层基于 UDP，在 QUIC 层实现了可靠传输（类似 TCP 的确认&#x2F;重传）。</li><li><strong>彻底解决了队头阻塞</strong>：QUIC 的多个 Stream 之间完全独立，一个 Stream 丢包只影响该 Stream，不影响其他 Stream 的交付。</li><li><strong>0-RTT 快速握手</strong>：QUIC 的握手只需要 0~1 次 RTT（往返时间），大幅降低连接建立延迟。</li><li><strong>连接迁移</strong>：QUIC 使用 Connection ID 标识连接，切换网络（WIFI → 4G）不需要重新建连。</li></ul></li></ul></blockquote><hr><h3 id="Q6：HTTPS-的握手过程？（TLS-1-2-1-3）"><a href="#Q6：HTTPS-的握手过程？（TLS-1-2-1-3）" class="headerlink" title="Q6：HTTPS 的握手过程？（TLS 1.2 &#x2F; 1.3）"></a>Q6：HTTPS 的握手过程？（TLS 1.2 &#x2F; 1.3）</h3><p><strong>核心考点</strong>：非对称加密传递对称密钥 → 对称加密传输数据，CA 证书防中间人攻击。</p><p><strong>背诵话术</strong>：</p><blockquote><p>HTTPS &#x3D; HTTP + TLS&#x2F;SSL，核心解决 HTTP 的三大问题：<strong>加密防窃听（机密性）、验证防伪装（身份认证）、摘要防篡改（完整性）</strong>。</p></blockquote><blockquote><p><strong>TLS 1.2 握手（4 次 RTT 包括 TCP 握手）</strong>：</p><ol><li><strong>Client Hello</strong>：客户端发送支持的 TLS 版本、加密套件（Cipher Suite）列表、随机数 1。</li><li><strong>Server Hello</strong>：服务端选择一个加密套件（如 <code>ECDHE_RSA_AES128_GCM_SHA256</code>），返回证书（包含公钥）、随机数 2。</li><li><strong>客户端验证</strong>：CA 验证证书合法性 → 用 ECDHE 算法结合随机数 1 和随机数 2 生成<strong>预主密钥</strong>（Premaster Secret），用服务端公钥加密后发给服务端 → 双方各自用 HMAC 算法基于预主密钥 + 两个随机数计算出<strong>同一个会话密钥（Session Key）</strong>。</li><li><strong>服务端解密</strong>：用自己的私钥解出预主密钥，同样计算出会话密钥 → 后续所有数据用会话密钥对称加密传输。</li></ol></blockquote><blockquote><p><strong>TLS 1.3 的改进</strong>：握手仅需 <strong>1-RTT</strong>（总 2-RTT 含 TCP），简化了加密套件（去掉 RSA 密钥交换、只保留 ECDHE 等前向安全套件），去掉了 CBC 等不安全算法。支持 0-RTT 恢复（之前连过的客户端用 PSK 直接发加密数据，适合 Web 页面重复访问）。</p></blockquote><blockquote><p><strong>中间人攻击如何防止</strong>：攻击者可以截获并转发通信，但无法通过 CA 证书验证——攻击者伪造的证书不受客户端信任的 CA 签发。浏览器&#x2F;操作系统内置了可信 CA 的公钥列表，任何不被这些 CA 签发或过期的证书都会被浏览器拦截。</p></blockquote><hr><h2 id="三、IO-模型"><a href="#三、IO-模型" class="headerlink" title="三、IO 模型"></a>三、IO 模型</h2><h3 id="Q7：BIO-NIO-AIO-的区别？"><a href="#Q7：BIO-NIO-AIO-的区别？" class="headerlink" title="Q7：BIO &#x2F; NIO &#x2F; AIO 的区别？"></a>Q7：BIO &#x2F; NIO &#x2F; AIO 的区别？</h3><p><strong>核心考点</strong>：阻塞、非阻塞（多路复用）、异步，Java 中的实现。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><p><strong>BIO（Blocking IO，同步阻塞）</strong>：每个客户端连接分配一个线程，<code>accept()</code> 和 <code>read()</code> 都会阻塞等待。当连接数增多时，线程数膨胀，大量线程被阻塞 IO 操作占用，线程切换开销巨大，CPU 大部分时间在切线程而非处理业务。Tomcat 默认的连接模型在早期版本是 BIO。</p></li><li><p><strong>NIO（Non-Blocking IO，同步非阻塞 + IO 多路复用）</strong>：一个线程通过 <strong>Selector（多路复用器）</strong> 管理多个 SocketChannel，Selector 轮询所有 Channel，只处理就绪的（有数据可读&#x2F;可写）。相比 BIO 大幅减少了线程数（一个线程可管理上万个连接）。Java NIO 底层在 Linux 上通过 <strong><code>epoll</code></strong> 实现（Windows 用 <code>IOCP</code>）。Tomcat 8 起默认使用 NIO。</p></li><li><p><strong>AIO（Asynchronous IO，异步非阻塞）</strong>：用户线程发起 IO 请求后立即返回，内核完成 IO 操作后通过回调或 Future 通知用户线程。Java AIO 底层在 Linux 上仍是 <code>epoll</code> 模拟（真正的异步 IO 需要 <code>io_uring</code>，Linux 5.1+ 支持，吞吐更高，延迟更低）。</p></li></ul></blockquote><blockquote><p><strong>面试加分表述</strong>：”NIO 的高性能本质是用一个 Selector 线程 + 少数 Worker 线程，替代了 BIO 的每个连接一个线程的模式，通过 epoll 的事件驱动机制减少线程切换开销。”</p></blockquote><hr><h3 id="Q8：select-poll-epoll-区别？"><a href="#Q8：select-poll-epoll-区别？" class="headerlink" title="Q8：select &#x2F; poll &#x2F; epoll 区别？"></a>Q8：select &#x2F; poll &#x2F; epoll 区别？</h3><p><strong>核心考点</strong>：epoll 是 Linux 高性能网络编程（Nginx、Redis、Netty）的基石。</p><p><strong>背诵话术</strong>：</p><blockquote><table><thead><tr><th></th><th>select</th><th>poll</th><th>epoll</th></tr></thead><tbody><tr><td>数据结构</td><td>固定长度的 <code>fd_set</code> 位图</td><td>动态的 <code>pollfd</code> 数组（链表）</td><td>基于红黑树 + 就绪链表</td></tr><tr><td>fd 数量限制</td><td>默认 1024（<code>FD_SETSIZE</code>）</td><td>无限制</td><td>无限制，受系统内存上限</td></tr><tr><td>扫描方式</td><td>每次调用全量扫描 fd_set</td><td>每次调用全量扫描 pollfd 数组</td><td><strong>事件驱动，只扫描就绪 fd 链表</strong></td></tr><tr><td>数据结构拷贝</td><td>每次调用 <code>fd_set</code> 从用户态拷贝到内核态</td><td>每次 <code>pollfd</code> 数组拷贝</td><td><strong>红黑树在内核维护，只需添加一次 fd</strong></td></tr><tr><td>时间复杂度</td><td>O(N)</td><td>O(N)</td><td><strong>O(1)</strong>（就绪 fd 数量固定）</td></tr></tbody></table><p><strong>epoll 为什么快</strong>：</p><ol><li>epoll 在内核中维护一棵<strong>红黑树</strong>来存储所有监听的 fd，添加&#x2F;删除 fd 只需操作这棵树，避免了每次调用都复制整个 fd 集合到内核。</li><li>当某个 fd 就绪时，通过<strong>回调机制</strong>将其从红黑树移到<strong>就绪链表</strong>，<code>epoll_wait()</code> 只处理这个链表，不需要遍历所有 fd。</li><li>支持 <strong>水平触发（LT，默认）</strong> 和 <strong>边缘触发（ET）</strong>。ET 模式下只通知一次（直到变为未就绪再变为就绪），需要非阻塞 IO + 循环读写直到 <code>EWOULDBLOCK</code>，编程复杂度高但性能更好。</li></ol></blockquote><hr><h2 id="四、Cookie-Session-Token"><a href="#四、Cookie-Session-Token" class="headerlink" title="四、Cookie &#x2F; Session &#x2F; Token"></a>四、Cookie &#x2F; Session &#x2F; Token</h2><h3 id="Q9：Cookie、Session、JWT-Token-的区别？"><a href="#Q9：Cookie、Session、JWT-Token-的区别？" class="headerlink" title="Q9：Cookie、Session、JWT &#x2F; Token 的区别？"></a>Q9：Cookie、Session、JWT &#x2F; Token 的区别？</h3><p><strong>核心考点</strong>：存储位置（客户端 vs 服务端）、无状态 vs 有状态。</p><p><strong>背诵话术</strong>：</p><blockquote><ul><li><strong>Cookie</strong>：存储在<strong>浏览器端</strong>的小段文本（≤4KB），由服务端通过 <code>Set-Cookie</code> 响应头设置，浏览器后续请求自动携带在 <code>Cookie</code> 请求头中。可设置 <code>HttpOnly</code>（禁止 JS 读取，防 XSS）、<code>Secure</code>（仅 HTTPS 传输）、<code>SameSite</code>（防 CSRF）。</li></ul></blockquote><blockquote><ul><li><strong>Session</strong>：存储在<strong>服务端</strong>的用户会话数据（通常放 Redis），浏览器只存一个 <code>JSESSIONID</code> Cookie。服务收到请求后通过 JSESSIONID 查找对应的 Session 对象获取用户信息。<strong>缺点</strong>：服务端需要维护 Session 状态，不适合分布式环境（需要 Redis 集中存储 Session），也不适合 App&#x2F;小程序（没有浏览器 Cookie 机制）。</li></ul></blockquote><blockquote><ul><li><strong>JWT（JSON Web Token）</strong>：<strong>无状态自包含令牌</strong>，由 Header（指定算法 RS256&#x2F;HS256）+ Payload（存储用户 ID、过期时间等非敏感信息）+ Signature（签名防篡改）三部分组成，Base64 编码后拼接。<strong>优点</strong>：服务端不需要存储 Token（没有状态），适合分布式微服务架构、移动端；<strong>缺点</strong>：无法主动撤销（即使封禁用户，Token 在过期前依然有效），Payload 只 Base64 编码（可解码查看，不能存敏感信息），体积比 Session ID 大。</li></ul></blockquote><blockquote><p><strong>选型建议</strong>：招行这类金融系统内网环境使用 Session + Redis 集中存储（可主动失效）；对外开放 API 使用 <code>AppKey + HMAC 签名</code>（类似美团 OpenAPI）；非敏感业务可使用 JWT 简化架构。</p></blockquote><hr><h2 id="五、高频面试题"><a href="#五、高频面试题" class="headerlink" title="五、高频面试题"></a>五、高频面试题</h2><h3 id="Q10：浏览器输入-URL-后发生了什么？"><a href="#Q10：浏览器输入-URL-后发生了什么？" class="headerlink" title="Q10：浏览器输入 URL 后发生了什么？"></a>Q10：浏览器输入 URL 后发生了什么？</h3><p><strong>核心考点</strong>：DNS 解析 → TCP 连接 → HTTP 请求 → 服务端处理 → 渲染，考查全局理解。</p><p><strong>背诵话术</strong>：</p><blockquote><ol><li><p><strong>DNS 解析</strong>：浏览器先查本地缓存（浏览器 DNS → 操作系统 hosts → 路由器 → ISP DNS 服务器），递归解析域名（如 <code>www.example.com</code>）得到目标 IP 地址。</p></li><li><p><strong>TCP 连接（三次握手）</strong>：浏览器向目标 IP 的 443（HTTPS）&#x2F; 80（HTTP）端口发起 TCP 连接。</p></li><li><p><strong>TLS 握手（HTTPS）</strong>：如果是 HTTPS，进行 TLS 1.2&#x2F;1.3 握手，协商加密套件和会话密钥。</p></li><li><p><strong>HTTP 请求</strong>：浏览器发送 HTTP 请求报文（含请求方法、URL、Header、Body 等）。</p></li><li><p><strong>服务端处理</strong>：请求到达 Nginx（反向代理 + 负载均衡）→ 路由到后端微服务 → Spring MVC 处理（DispatcherServlet → Controller → Service → DAO → MySQL&#x2F;Redis），生成响应报文。</p></li><li><p><strong>浏览器接收并渲染</strong>：浏览器收到响应 → 解析 HTML 构建 DOM Tree → 解析 CSS 构建 CSSOM → 合并为 Render Tree → Layout（计算位置）→ Paint（绘制）→ 请求和加载 JS &#x2F; 图片等外部资源。</p></li></ol><p>有经验的面试官会追问”DNS 用的是 TCP 还是 UDP”，答案是：<strong>默认 UDP，响应超过 512 字节时切换到 TCP</strong>。</p></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> 网络 </tag>
            
            <tag> 协议 </tag>
            
        </tags>
      
    </entry>
    
    
    
    <entry>
      <title>项目STAR与场景题面试考题</title>
      <link href="/2026/06/08/%E9%A1%B9%E7%9B%AESTAR%E4%B8%8E%E5%9C%BA%E6%99%AF%E9%A2%98%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/"/>
      <url>/2026/06/08/%E9%A1%B9%E7%9B%AESTAR%E4%B8%8E%E5%9C%BA%E6%99%AF%E9%A2%98%E9%9D%A2%E8%AF%95%E8%80%83%E9%A2%98/</url>
      
        <content type="html"><![CDATA[<h1 id="09-项目STAR自述与高频场景题-·-面试通关指南"><a href="#09-项目STAR自述与高频场景题-·-面试通关指南" class="headerlink" title="09 - 项目STAR自述与高频场景题 · 面试通关指南"></a>09 - 项目STAR自述与高频场景题 · 面试通关指南</h1><hr><h2 id="目录"><a href="#目录" class="headerlink" title="目录"></a>目录</h2><ul><li><a href="#%E4%B8%80%E5%9B%9B%E5%A4%A7%E9%A1%B9%E7%9B%AE-star-%E5%AE%8C%E6%95%B4%E8%87%AA%E8%BF%B0">一、四大项目 STAR 完整自述</a><ul><li><a href="#%E9%A1%B9%E7%9B%AE%E4%B8%80%E5%8C%BB%E9%99%A2%E5%90%8E%E5%8F%B0%E7%AE%A1%E7%90%86%E7%B3%BB%E7%BB%9F%E5%8C%BB%E5%8D%9A%E7%B3%BB%E7%BB%9F202006--202208">项目一：医院后台管理系统（医博系统）</a></li><li><a href="#%E9%A1%B9%E7%9B%AE%E4%BA%8C%E7%BE%8E%E5%9B%A2%E6%99%BA%E8%83%BD%E5%8C%96%E9%A4%90%E9%A5%AE-saas-%E7%AE%A1%E5%AE%B6%E8%BF%90%E8%90%A5%E5%BC%95%E6%93%8E%E7%BA%AC%E5%88%9B%E8%BD%AF%E4%BB%B6202210--202406">项目二：美团智能化餐饮 SaaS 管家运营引擎</a></li><li><a href="#%E9%A1%B9%E7%9B%AE%E4%B8%89%E6%8B%9B%E5%95%86%E9%93%B6%E8%A1%8C%E5%88%86%E5%B8%83%E5%BC%8F%E6%A0%B8%E5%BF%83%E6%B5%81%E7%A8%8B%E8%B0%83%E5%BA%A6%E4%B8%AD%E5%8F%B0%E6%8B%93%E4%BF%9D%E8%BD%AF%E4%BB%B6202407--202511">项目三：招商银行分布式核心流程调度中台</a></li><li><a href="#%E9%A1%B9%E7%9B%AE%E5%9B%9Bsrm-%E9%87%87%E8%B4%AD%E7%AB%AF%E5%85%A8%E9%93%BE%E8%B7%AF%E8%87%AA%E5%8A%A8%E5%8C%96%E7%AE%A1%E7%90%86%E5%B9%B3%E5%8F%B0%E4%B8%AD%E7%94%B5%E9%87%91%E4%BF%A1202601--%E8%87%B3%E4%BB%8A">项目四：SRM 采购端全链路自动化管理平台</a></li></ul></li><li><a href="#%E4%BA%8C%E6%8A%80%E6%9C%AF%E5%BA%95%E5%B1%82%E6%B7%B1%E5%BA%A6%E6%8C%96%E6%8E%98">二、技术底层深度挖掘</a><ul><li><a href="#q1%E5%8A%A8%E6%80%81%E5%AE%A1%E6%89%B9%E6%B5%81%E5%BC%95%E6%93%8E%E5%A6%82%E4%BD%95%E8%AE%BE%E8%AE%A1%E8%B4%A3%E4%BB%BB%E9%93%BE--%E6%95%B0%E6%8D%AE%E5%BA%93%E9%85%8D%E7%BD%AE">Q1：动态审批流引擎如何设计（责任链 + 数据库配置）？</a></li></ul></li><li><a href="#%E4%B8%89%E9%AB%98%E9%A2%91%E5%9C%BA%E6%99%AF%E9%A2%98%E7%AD%94%E8%BE%A9">三、高频场景题答辩</a><ul><li><a href="#%E5%9C%BA%E6%99%AF%E9%A2%98-1srm-%E8%AF%A2%E4%BB%B7-kafka-%E6%B6%88%E6%81%AF%E5%A4%A7%E9%87%8F%E7%A7%AF%E5%8E%8B%E5%A6%82%E4%BD%95%E5%A4%84%E7%90%86">场景题 1：SRM 询价 Kafka 消息大量积压，如何处理？</a></li><li><a href="#%E5%9C%BA%E6%99%AF%E9%A2%98-2%E6%8B%9B%E8%A1%8C%E4%B8%AD%E5%8F%B0%E4%B8%8B%E6%B8%B8%E6%9C%8D%E5%8A%A1%E5%81%87%E6%AD%BB%E5%AF%BC%E8%87%B4%E8%BF%9E%E6%8E%A5%E6%B1%A0%E8%80%97%E5%B0%BD%E5%A6%82%E4%BD%95%E5%BF%AB%E9%80%9F%E6%81%A2%E5%A4%8D">场景题 2：招行中台下游服务假死导致连接池耗尽，如何快速恢复？</a></li><li><a href="#%E5%9C%BA%E6%99%AF%E9%A2%98-3%E5%A6%82%E4%BD%95%E8%AE%BE%E8%AE%A1%E4%B8%80%E4%B8%AA%E9%80%9A%E7%94%A8%E7%9A%84%E5%88%86%E5%B8%83%E5%BC%8F-id-%E7%94%9F%E6%88%90%E6%96%B9%E6%A1%88">场景题 3：如何设计一个通用的分布式 ID 生成方案？</a></li><li><a href="#%E5%9C%BA%E6%99%AF%E9%A2%98-4%E7%A7%92%E6%9D%80%E7%B3%BB%E7%BB%9F%E7%9A%84%E5%AE%8C%E6%95%B4%E8%AE%BE%E8%AE%A1">场景题 4：秒杀系统的完整设计</a></li><li><a href="#%E5%9C%BA%E6%99%AF%E9%A2%98-5%E5%BE%AE%E6%9C%8D%E5%8A%A1%E6%8E%A5%E5%8F%A3%E5%B9%82%E7%AD%89%E6%80%A7%E5%A6%82%E4%BD%95%E4%BF%9D%E8%AF%81">场景题 5：微服务接口幂等性如何保证？</a></li><li><a href="#%E5%9C%BA%E6%99%AF%E9%A2%98-6%E5%A6%82%E4%BD%95%E8%AE%BE%E8%AE%A1%E6%94%AF%E6%8C%81%E4%BA%BF%E7%BA%A7%E6%95%B0%E6%8D%AE%E7%9A%84%E5%AE%9A%E6%97%B6%E4%BB%BB%E5%8A%A1%E8%B0%83%E5%BA%A6%E7%B3%BB%E7%BB%9F">场景题 6：如何设计支持亿级数据的定时任务调度系统？</a></li></ul></li></ul><hr><span id="more"></span><h2 id="一、四大项目-STAR-完整自述"><a href="#一、四大项目-STAR-完整自述" class="headerlink" title="一、四大项目 STAR 完整自述"></a>一、四大项目 STAR 完整自述</h2><h3 id="项目一：医院后台管理系统（医博系统，2020-06-2022-08）"><a href="#项目一：医院后台管理系统（医博系统，2020-06-2022-08）" class="headerlink" title="项目一：医院后台管理系统（医博系统，2020.06 ~ 2022.08）"></a>项目一：医院后台管理系统（医博系统，2020.06 ~ 2022.08）</h3><p><strong>STAR 自述模板</strong>：</p><blockquote><p><strong>S（背景）</strong>：我毕业后第一份工作是在医博系统技术（深圳），参与开发面向中小型私立医院的数字化管理后台，系统涵盖挂号预约、就诊结算积分核算、药品管理、患者病历管理等模块，服务于多家连锁医院的日常运营。</p><p><strong>T（挑战）</strong>：主要面临两个核心挑战：一是患者数据（身份证、手机号、病历）的安全合规问题，系统需要通过医疗数据安全审计；二是医生排班、号源等热点数据频繁被查询，数据库压力大，查询响应慢。</p><p><strong>A（行动）</strong>：</p><ul><li>设计并实现了基于 <strong>AOP 注解驱动的动态脱敏拦截器</strong>，对标注了 <code>@SensitiveField</code> 注解的字段（身份证号保留前 3 后 4 位，手机号保留前 3 后 4 位），在接口响应时自动进行正则替换脱敏，业务代码零改动。</li><li>引入 <strong>Redis 缓存排班热点数据</strong>，使用 List 结构缓存每个医生的可用号源，结合逻辑过期策略（Lua 脚本）防止热点 Key 缓存击穿。</li><li>针对大宽表（50 万患者记录）导出的 OOM 问题，将 Apache POI 替换为 <strong>EasyExcel SAX 流式分批写盘</strong>，分批大小 1000 条，JVM 堆内存使用降低 95%。</li><li>强化了细粒度的 <strong>RBAC 权限控制</strong>，基于角色+资源+操作三维度控制接口访问权限，通过拦截器统一鉴权。</li></ul><p><strong>R（成果）</strong>：系统顺利通过国家医疗健康数据安全合规审计；大宽表导出彻底消除 OOM；排班查询响应时间从 800ms 降至 50ms 以内。</p></blockquote><p><strong>面试官必问 Q&amp;A</strong>：</p><p><strong>Q：你说的动态脱敏拦截器是如何实现的，详细说一下？</strong></p><blockquote><p>“我们在实体类的敏感字段上自定义了一个 <code>@SensitiveField(type = SensitiveType.PHONE)</code> 注解。然后编写了一个 <code>SensitiveSerializeAspect</code>（AOP 切面）在所有标注了 <code>@RequireDesensitize</code> 的接口方法返回后触发，使用 Java 反射遍历返回对象的所有字段，如果字段上有 <code>@SensitiveField</code> 注解，就通过对应的脱敏规则（正则表达式）将字段值替换为脱敏后的值再返回。这样所有接口的脱敏逻辑完全统一收口，新增敏感字段只需加注解，无需改业务代码，完全符合开闭原则。”</p></blockquote><p><strong>Q：号源缓存击穿是如何防止的？</strong></p><blockquote><p>“我们医院系统中，某些热门专家号在开放挂号的瞬间会有数百个请求同时涌来。为防止缓存击穿，我们采用了<strong>逻辑过期</strong>方案：Redis 中的号源 Key 永远不设置 TTL（不会真正过期），而是在 Value 里存储一个逻辑过期时间戳。每次读取时先判断逻辑时间是否超过当前时间，如果超过，用 <code>SET NX</code> 加一把互斥锁，只有抢到锁的线程去数据库重建缓存（异步进行），其他线程直接返回旧的缓存数据。这样既防止了大量请求同时击穿数据库，也避免了用互斥锁时其他请求一直阻塞等待的问题。”</p></blockquote><p><strong>Q：EasyExcel 和 Apache POI 的区别？</strong></p><blockquote><p>“Apache POI 的 <code>XSSFWorkbook</code> 会将所有 Sheet 数据全部加载到 JVM 堆内存中，对于 50 万行数据会产生数 GB 的内存膨胀，导致频繁 Full GC 甚至 OOM。EasyExcel 基于 SAX 流式解析机制，写出时以 Row 为单位逐行处理：从数据库分批查（每批 1000 条）→ 逐行写入 Excel 临时文件 → 写完立即释放对象。JVM 中同时存在的对象数量始终很少，内存占用稳定且极低，从根本上解决了 OOM 问题。”</p></blockquote><hr><h3 id="项目二：美团智能化餐饮-SaaS-管家运营引擎（纬创软件，2022-10-2024-06）"><a href="#项目二：美团智能化餐饮-SaaS-管家运营引擎（纬创软件，2022-10-2024-06）" class="headerlink" title="项目二：美团智能化餐饮 SaaS 管家运营引擎（纬创软件，2022.10 ~ 2024.06）"></a>项目二：美团智能化餐饮 SaaS 管家运营引擎（纬创软件，2022.10 ~ 2024.06）</h3><p><strong>STAR 自述模板</strong>：</p><blockquote><p><strong>S（背景）</strong>：在纬创软件外包至美团的项目中，我参与开发美团餐饮 SaaS 管家核心运营引擎，该系统服务于海量 B 端餐饮商户，承载单日百万级的商户运营请求，支撑商户进行营销活动、对账报表、积分管理等全流程操作，同时对外提供 OpenAPI 供商户系统集成。</p><p><strong>T（挑战）</strong>：两大核心挑战：一是商户高频对账单导出时产生大量慢 SQL（最慢接口 3.2 秒），导致服务频繁超时熔断，CPU 剧烈震荡；二是 OpenAPI 对外开放，面临接口重放攻击和非法调用的安全威胁。</p><p><strong>A（行动）</strong>：</p><ul><li>主导<strong>慢 SQL 专项治理</strong>：使用 <code>EXPLAIN</code> 分析执行计划，发现大量 <code>Using filesort</code> 和 <code>Using temporary</code>，将 3 表 <code>LEFT JOIN</code> 的复杂查询拆分为高效单表查询 + Java 内存聚合，建立联合覆盖索引 <code>(shop_id, status, create_time)</code>，消除全表扫描和磁盘排序。</li><li>设计并落地 <strong>HMAC-SHA256 + nonce + 时间戳的 OpenAPI 防重放防刷体系</strong>，在 API 网关层实现两道拦截：5 分钟时间戳过期校验 + Redis nonce 去重（5 分钟 TTL 自动回收）。</li><li>搭建<strong>插件总线架构</strong>，支持 50+ 业态功能的动态开关，核心业务逻辑和差异化功能完全解耦，新业态接入无需修改主流程代码。</li></ul><p><strong>R（成果）</strong>：Top 10 慢接口平均响应时间从 3.2s 降至 0.4s，服务超时熔断故障率下降 25%；OpenAPI 接口安全性通过第三方安全审计；插件总线上线后，新业态接入时间从 2 周缩短至 3 天。</p></blockquote><p><strong>面试官必问 Q&amp;A</strong>：</p><p><strong>Q：慢 SQL 优化 3.2s → 0.4s 的完整过程，详细说一下？（这是必考题！）</strong></p><blockquote><p>“当时对账导出接口出现 CPU 毛刺和大量超时告警，我把慢 SQL 从 APM 监控中提取出来，用 <code>EXPLAIN</code> 分析。</p><p><strong>发现的问题</strong>：</p><ol><li><code>type = ALL</code>：全表扫描（数据量 200 万行）。</li><li><code>Extra = Using filesort</code>：<code>ORDER BY create_time</code> 没有命中索引，需要磁盘文件排序。</li><li><code>Extra = Using temporary</code>：GROUP BY 相关操作产生了临时表。</li><li>SQL 是 3 个 <code>LEFT JOIN</code> 的大表连接，驱动表选择也有问题。</li></ol><p><strong>优化方案</strong>：</p><ul><li><strong>第一步：拆 SQL</strong>。坚决禁止多表 JOIN，将联合查询拆为 3 个独立的高效单表查询，每个表各自按主键查，在 Java 层用 Stream + Map 做数据关联（例如 <code>Map&lt;Long, ShopInfo&gt; shopMap</code> 按 ID 映射）。</li><li><strong>第二步：建联合覆盖索引</strong>。主查询表建立 <code>idx_shop_status_time(shop_id, status, create_time)</code> 联合索引，同时把 SELECT 列精简为只查展示字段，使 EXPLAIN 的 Extra 显示 <code>Using index</code>（覆盖索引，无需回表）。</li></ul><p><strong>效果</strong>：优化后 EXPLAIN 的 type 从 <code>ALL</code> → <code>ref</code>，Extra 从 <code>Using filesort; Using temporary</code> → <code>Using index</code>，接口响应时间从 3.2s 降至 0.4s，数据库 CPU 使用率下降了 60%。”</p></blockquote><p><strong>Q：HMAC + nonce 防重放的完整链路？nonce 存 Redis 会不会撑爆内存？</strong></p><blockquote><p>“防重放体系是这样工作的：客户端调用 API 时，生成一个 UUID 作为 <code>nonce</code>，记录当前毫秒级时间戳 <code>timestamp</code>，将所有请求参数按字典序排序后拼接上 <code>AppSecret</code>，用 HMAC-SHA256 算出签名，放入 HTTP Header。</p><p>网关收到请求后，<strong>两道校验</strong>：</p><ol><li><strong>时间戳校验</strong>：拿服务器时间与 timestamp 比对，差值超过 5 分钟直接拦截（重放攻击的报文时间戳已过期）。</li><li><strong>nonce 防重放</strong>：去 Redis 查 <code>SETNX nonce 1 EX 300</code>，若返回 0 说明 5 分钟内该 nonce 已用过，直接拦截；返回 1 则放行，同时记录该 nonce（5 分钟自动过期）。</li></ol><p><strong>不会撑爆内存</strong>：因为 nonce 的 TTL 和时间戳校验窗口都是 5 分钟，Redis 中最多只存 5 分钟内请求的 nonce，以每秒 1000 QPS、每个 nonce UUID（36 字节）+ Key 开销估算，5 分钟 &#x3D; 300 秒，最多 30 万个 Key，约占 20~30MB，完全可接受。过期的 nonce 被 Redis 自动清除，不会无限增长。”</p></blockquote><hr><h3 id="项目三：招商银行分布式核心流程调度中台（拓保软件，2024-07-2025-11）"><a href="#项目三：招商银行分布式核心流程调度中台（拓保软件，2024-07-2025-11）" class="headerlink" title="项目三：招商银行分布式核心流程调度中台（拓保软件，2024.07 ~ 2025.11）"></a>项目三：招商银行分布式核心流程调度中台（拓保软件，2024.07 ~ 2025.11）</h3><p><strong>STAR 自述模板</strong>：</p><blockquote><p><strong>S（背景）</strong>：在拓保软件外包至招商银行的项目中，我负责核心流程调度中台的后端研发，该中台作为招行网贷、对公等核心业务线的调度引擎，处理流程规则的流转、路由和调度计算，日均调用量百万级，对响应时间和高可用性要求极高。</p><p><strong>T（挑战）</strong>：规则库数据频繁被各微服务节点高并发查询，直接访问 MySQL 导致数据库不堪重负；同时规则一旦变更，需要实时同步到所有运行中的微服务节点，否则会出现规则不一致导致的计算错误。</p><p><strong>A（行动）</strong>：</p><ul><li>设计并落地 <strong>Caffeine + Redis Cluster 多级缓存体系</strong>：热点规则数据先从 Caffeine 本地缓存读取（亚毫秒级响应），Caffeine 未命中再读 Redis Cluster（毫秒级），Redis 未命中才访问 MySQL。</li><li>引入 <strong>Canal 订阅 MySQL Binlog</strong>，实时捕获规则库数据变更，推送 Kafka Topic，各节点的消费服务接收变更事件后：① 失效对应 Redis 缓存；② 通过 Redis Pub&#x2F;Sub 广播通知所有节点主动失效 Caffeine 本地缓存，实现毫秒级多节点缓存同步。</li><li>基于报文驱动设计了 <strong>XML&#x2F;定长报文解析引擎</strong>，通过策略工厂 + 声明式注解映射，支持不同报文格式的自动路由解析。</li></ul><p><strong>R（成果）</strong>：核心读取接口响应延迟降低 40%，90% 以上的数据库读请求被缓存层拦截，MySQL QPS 从高峰 10 万降至 1 万，系统从容支撑了日均百万次的高并发调用。</p></blockquote><p><strong>面试官必问 Q&amp;A</strong>：</p><p><strong>Q：Canal 是怎么感知到 MySQL 数据变更的？Canal HA 是如何实现的？</strong></p><blockquote><p>“Canal 的底层原理是模拟 <strong>MySQL Slave（从库）的复制协议</strong>。Canal 将自己伪装成一个 MySQL 从库，向 MySQL 主库发送 dump 协议请求。主库收到后，将 Binlog 二进制日志持续推送给 Canal。Canal 解析这些 ROW 格式的 Binlog（得到行级别的增删改数据），将其封装为标准 JSON 变更事件，推送到 Kafka 对应的 Topic。</p><p><strong>Canal HA（高可用）</strong>：我们部署了两个 Canal 实例，通过 <strong>ZooKeeper 进行 Leader 选举</strong>。两个实例同时向 ZooKeeper 的同一个临时节点抢注册，成功的成为 Active（工作节点），失败的成为 Standby（备用节点）。Active 节点故障时，ZooKeeper 的临时节点过期删除，Standby 节点监听到后立即抢占并接管，同时从 ZooKeeper 中读取上一次记录的 <strong>Binlog Position</strong>，从断点位置继续拉取，保证一条 Binlog 事件都不丢失，整个切换过程在毫秒级完成。”</p></blockquote><p><strong>Q：多级缓存架构的完整读写流程是怎样的？</strong></p><blockquote><p>“完整的读流程：请求到达 → 先查 <strong>Caffeine 本地缓存</strong>（HashMap 级别的纳秒响应，命中率约 80%）→ Caffeine 未命中则查 <strong>Redis Cluster</strong>（网络 IO，毫秒级，命中率约 18%）→ Redis 未命中才查 <strong>MySQL</strong>（磁盘 IO，最慢，只承受约 2% 的穿透请求）→ 查到后同时写入 Redis 和 Caffeine，下次访问直接从缓存取。</p><p>写流程（数据变更时）：业务代码更新 MySQL → Canal 感知到 Binlog 变更 → 推入 Kafka → 消费服务接收消息 → 删除 Redis 对应 Key（缓存失效）→ 发布 Redis Pub&#x2F;Sub 消息 → 所有微服务节点的订阅监听器收到通知 → 调用 <code>caffeine.invalidate(key)</code> 失效本地缓存 → 下次读取时从 MySQL 重建缓存。整个链路延迟在 100ms 以内，对于规则一致性来说完全可以接受。”</p></blockquote><hr><h3 id="项目四：SRM-采购端全链路自动化管理平台（中电金信，2026-01-至今）"><a href="#项目四：SRM-采购端全链路自动化管理平台（中电金信，2026-01-至今）" class="headerlink" title="项目四：SRM 采购端全链路自动化管理平台（中电金信，2026.01 ~ 至今）"></a>项目四：SRM 采购端全链路自动化管理平台（中电金信，2026.01 ~ 至今）</h3><p><strong>STAR 自述模板</strong>：</p><blockquote><p><strong>S（背景）</strong>：我目前在中电金信负责 SRM 采购端全链路自动化管理平台的核心研发，系统服务于中大型制造业企业，覆盖采购询价、自动寻源匹配、多供应商报价比价、采购订单生成及审批的全链路自动化，并通过防腐层对接企业内部的 ERP、OA 等异构系统。</p><p><strong>T（挑战）</strong>：两大挑战：一是寻源匹配规则复杂（不同物料类别有不同的寻源权重策略），业务扩展频繁，需要高度可扩展的架构；二是需要并发调用多个供应商的报价接口进行实时比价，串行调用会严重超时。</p><p><strong>A（行动）</strong>：</p><ul><li>基于 <strong>策略模式 + Spring Map 依赖注入</strong>设计动态寻源算法引擎：将每种物料类别的寻源策略抽象为 <code>SourceStrategy</code> 接口的实现类，Spring 容器自动注入 <code>Map&lt;String, SourceStrategy&gt;</code>，新增策略只需实现接口加 <code>@Component</code>，无需改动主流程代码，完美遵循开闭原则。</li><li>基于 <strong>CompletableFuture + 自定义隔离线程池</strong>实现多供应商并发报价：10 个供应商接口并发调用，总耗时从串行 5s 降至单次 500ms，<code>.exceptionally()</code> 对单个接口失败进行降级处理，保证整体比价不中断。</li><li>设计 <strong>防腐层（Anti-Corruption Layer, ACL）</strong> 隔离外部 ERP&#x2F;OA 系统的不稳定依赖：为每个外部系统定义统一的接口适配器，将外部系统的协议、数据格式差异封装在 ACL 内部，内部业务代码只依赖内部接口定义，外部系统迭代变更时只需修改 ACL 层。</li><li>基于 <strong>责任链模式 + 数据库配置驱动</strong>实现动态审批流引擎，审批节点配置化，支持会签、顺签、加签、超时转办等复杂审批场景，修改审批流程无需发版。</li></ul><p><strong>R（成果）</strong>：询价匹配周期从天级压缩至小时级，比价准确率 100%；防腐层成功隔离了 3 个外部系统的接口变更，零业务中断；动态审批流落地后，业务团队自助配置审批流，研发工作量降低 60%。</p></blockquote><p><strong>面试官必问 Q&amp;A</strong>：</p><p><strong>Q：策略模式 + Spring Map 注入如何实现动态寻源扩展？</strong></p><blockquote><p>“我们定义了一个寻源策略接口 <code>SourceStrategy</code>，有一个核心方法 <code>match(SourceContext ctx): List&lt;Supplier&gt;</code>。然后为每种物料类别实现具体的策略类，如 <code>MaterialSourceStrategy</code>（原材料）、<code>StandardPartSourceStrategy</code>（标准件）等，每个类上加 <code>@Component(value = &quot;MATERIAL&quot;)</code> 注解，Bean 名称就是策略标识符。</p><p>在核心的 <code>SourceEngine</code> 服务中，我声明了 <code>@Autowired Map&lt;String, SourceStrategy&gt; strategyMap</code>，Spring 会自动将所有 <code>SourceStrategy</code> 实现类按 Bean Name 注入到这个 Map 中。运行时只需 <code>strategyMap.get(materialType).match(ctx)</code> 就能动态调用对应策略。</p><p>新增一种物料的寻源规则时，只需新建一个实现类加上对应的 <code>@Component</code>，<strong>主流程代码零改动</strong>，完全符合开闭原则。”</p></blockquote><p><strong>Q：防腐层（ACL）是如何设计的？解决了什么问题？</strong></p><blockquote><p>“SRM 系统需要对接 SAP ERP（获取物料信息）、OA 系统（提交审批单）和财务系统（生成付款单），这三个都是外部异构系统，有各自的接口协议（SOAP&#x2F;REST）、数据模型和错误码体系。</p><p>没有防腐层的话，业务代码直接依赖外部系统的接口定义和数据模型，一旦外部系统接口变更，业务代码到处改动，维护噩梦。</p><p><strong>ACL 的设计</strong>：在 SRM 内部定义统一的领域接口，如 <code>ErpMaterialPort</code>（获取物料信息），不依赖任何外部系统的具体实现。然后在 ACL 层为每个外部系统实现一个适配器，如 <code>SapErpAdapter implements ErpMaterialPort</code>，在适配器内部处理 SAP 的 SOAP 调用、XML 解析、异常转换、重试等所有外部系统相关的脏活。业务代码只依赖 <code>ErpMaterialPort</code> 接口，完全感知不到底层是 SAP 还是其他 ERP。当 SAP 接口升级时，只需修改 <code>SapErpAdapter</code>，业务代码零改动。”</p></blockquote><hr><h2 id="二、技术底层深度挖掘"><a href="#二、技术底层深度挖掘" class="headerlink" title="二、技术底层深度挖掘"></a>二、技术底层深度挖掘</h2><h3 id="Q1：动态审批流引擎如何设计（责任链-数据库配置）？"><a href="#Q1：动态审批流引擎如何设计（责任链-数据库配置）？" class="headerlink" title="Q1：动态审批流引擎如何设计（责任链 + 数据库配置）？"></a>Q1：动态审批流引擎如何设计（责任链 + 数据库配置）？</h3><p><strong>背诵话术</strong>：</p><blockquote><p>动态审批流引擎的核心思想是：<strong>审批节点配置化，执行逻辑代码化</strong>。</p><p><strong>数据模型设计</strong>：在数据库中维护 <code>approval_flow_template</code>（审批流模板：如”采购合同审批流”）和 <code>approval_flow_node</code>（节点定义：包含节点序号、节点类型（顺签&#x2F;会签&#x2F;角色审批）、审批角色、超时时间、超时动作（转办&#x2F;自动通过））。</p><p><strong>责任链执行</strong>：系统启动时根据数据库中的节点配置，动态构建责任链。当一个采购申请触发审批时，从第一个节点开始依次执行：</p><ol><li><strong>顺签节点</strong>：按序号逐人审批，一人通过才流转到下一人。</li><li><strong>会签节点</strong>：多人并行审批，全部通过才流转下一阶段。</li><li><strong>超时转办</strong>：通过定时任务扫描超时未审批的节点，按配置自动转派给替代审批人或自动通过。</li></ol><p>修改审批流程时，业务人员直接在管理后台修改数据库中的节点配置，无需发布代码，审批流变更立即生效。</p></blockquote><hr><h2 id="三、高频场景题答辩"><a href="#三、高频场景题答辩" class="headerlink" title="三、高频场景题答辩"></a>三、高频场景题答辩</h2><h3 id="场景题-1：SRM-询价-Kafka-消息大量积压，如何处理？"><a href="#场景题-1：SRM-询价-Kafka-消息大量积压，如何处理？" class="headerlink" title="场景题 1：SRM 询价 Kafka 消息大量积压，如何处理？"></a>场景题 1：SRM 询价 Kafka 消息大量积压，如何处理？</h3><p><strong>回答思路</strong>：</p><blockquote><p>“处理消息积压，我分三步走：</p><p><strong>第一步：定位根因</strong>。通过 Kafka 监控（Consumer Lag 指标）确认是哪个 Topic 和消费者组积压，日志里查是消费抛错（消费者卡死在重试）、下游处理太慢，还是生产速度远超消费速度。</p><p><strong>第二步：扩容消费者</strong>：</p><ul><li>如果分区数充足（如 10 个分区）：立即水平扩容消费者实例数到与分区数持平，每个分区都有消费者并行消费，是最快最有效的手段。</li><li>如果分区数不足（如只有 2 个分区）：扩消费者没用，需上线一个<strong>临时中转服务</strong>，快速消费旧 Topic 并 ACK，把消息转投到新建的有足够分区数（如 20 个）的临时 Topic，再用 20 个消费者实例消费临时 Topic，快速消化积压。</li></ul><p><strong>第三步：临时降级</strong>。关闭非核心消费逻辑（如埋点上报、积分回调），只保留核心报价处理，释放 CPU 和 DB IO 资源，加速消费速度。等积压消费完后恢复完整逻辑。”</p></blockquote><hr><h3 id="场景题-2：招行中台下游服务假死导致连接池耗尽，如何快速恢复？"><a href="#场景题-2：招行中台下游服务假死导致连接池耗尽，如何快速恢复？" class="headerlink" title="场景题 2：招行中台下游服务假死导致连接池耗尽，如何快速恢复？"></a>场景题 2：招行中台下游服务假死导致连接池耗尽，如何快速恢复？</h3><p><strong>回答思路</strong>：</p><blockquote><p>“下游服务’假死’（响应极慢但不报错），如果上游没有防护，会导致 Feign 调用持续等待，Tomcat 线程被占满，连接池耗尽，上游服务也随之崩溃（雪崩）。</p><p>我们在招行项目中的防护层次：</p><ol><li><strong>Sentinel 慢调用熔断</strong>：为每个 Feign 接口配置慢调用比例熔断规则（如 10s 内平均响应 &gt; 2s 且比例 &gt; 30%，熔断 30s），熔断后所有请求直接在本地返回 Fallback 降级数据，立即释放线程，不再发起网络请求。这是最核心的防护。</li><li><strong>线程隔离（舱壁模式）</strong>：为不同的下游服务分配独立的线程池（如 <code>@FeignClient</code> 配置 thread-pool），即使某个下游服务彻底挂死，也只占满它自己的线程池（20 个线程），不影响其他接口和主 Tomcat 容器，实现’舱壁隔离’。</li><li><strong>配置合理超时</strong>：<code>connect-timeout=2s</code>，<code>read-timeout=5s</code>，防止单次调用无限等待。</li></ol><p>以上三层防护组合，能在下游服务故障时，将影响面控制在最小范围，快速自愈。”</p></blockquote><hr><h3 id="场景题-3：如何设计一个通用的分布式-ID-生成方案？"><a href="#场景题-3：如何设计一个通用的分布式-ID-生成方案？" class="headerlink" title="场景题 3：如何设计一个通用的分布式 ID 生成方案？"></a>场景题 3：如何设计一个通用的分布式 ID 生成方案？</h3><p><strong>回答思路</strong>：</p><blockquote><p>“分布式 ID 的核心要求：全局唯一、趋势递增（有序）、高性能、高可用。主流方案对比：</p><p><strong>雪花算法（Snowflake，推荐）</strong>：64 位 Long 型 ID &#x3D; 1 位符号位 + 41 位时间戳（精确到毫秒，可用约 69 年）+ 10 位机器 ID（5 位数据中心 + 5 位机器号）+ 12 位序列号（同毫秒内最多 4096 个 ID）。优点：趋势递增、高性能（单机每秒可生成 400 万 ID）、不依赖外部存储。<strong>核心缺点：时钟回拨问题</strong>（如果机器时钟被调回，可能生成重复 ID）。</p><p><strong>时钟回拨解决方案</strong>：</p><ol><li><strong>等待法</strong>：发现当前时间小于上次生成时间时，等待时间追上后再生成，适合回拨时间很短（&lt; 5ms）的场景。</li><li><strong>备用时间序列</strong>：预留额外的若干 bit 作为时钟回拨计数器，回拨发生时递增计数器而不是等待，保证 ID 不重复。</li><li><strong>Leaf-Snowflake（美团 Leaf 方案）</strong>：启动时从 ZooKeeper 获取机器 ID，并向 NTP 服务校准时间；运行时若发现时钟回拨，抛出异常等待人工处理，或利用 ZooKeeper 的 Epoch 机制降级处理。</li></ol><p><strong>我们 SRM 项目的做法</strong>：使用 Hutool 或 Mybatis-Plus 自带的雪花算法实现，并在应用启动时通过机器 IP 的最后两段计算 <code>workerId</code>，避免机器 ID 冲突，同时设置了时钟回拨检测告警。”</p></blockquote><hr><h3 id="场景题-4：秒杀系统的完整设计"><a href="#场景题-4：秒杀系统的完整设计" class="headerlink" title="场景题 4：秒杀系统的完整设计"></a>场景题 4：秒杀系统的完整设计</h3><p><strong>回答思路</strong>：</p><blockquote><p>“秒杀系统的核心挑战是：瞬时高并发（几万 QPS）+ 数据一致性（不能超卖）。我的设计分为以下几层：</p><p><strong>第一层：流量收束（前置过滤）</strong>：</p><ul><li><strong>Nginx 限流</strong>：对秒杀接口配置 <code>limit_req</code>，限制单 IP 的请求速率。</li><li><strong>验证码&#x2F;答题</strong>：秒杀前要求用户输入验证码，拦截机器刷接口的流量，同时打散请求的时间分布。</li><li><strong>按钮防抖</strong>：前端禁止重复点击，减少重复请求。</li></ul><p><strong>第二层：Sentinel 流量控制</strong>：在接入层（网关或应用层）对秒杀接口配置 Sentinel QPS 限流规则（如最多 500 QPS），超出的请求直接返回”系统繁忙，请稍后重试”，不进入后端。</p><p><strong>第三层：Redis 原子性预扣库存（核心！）</strong>：</p><figure class="highlight lua"><table><tr><td class="gutter"><pre><span class="line">1</span><br><span class="line">2</span><br><span class="line">3</span><br><span class="line">4</span><br><span class="line">5</span><br></pre></td><td class="code"><pre><span class="line"><span class="comment">-- Lua 脚本保证原子性</span></span><br><span class="line"><span class="keyword">local</span> stock = redis.call(<span class="string">&#x27;GET&#x27;</span>, KEYS[<span class="number">1</span>])</span><br><span class="line"><span class="keyword">if</span> <span class="built_in">tonumber</span>(stock) &lt;= <span class="number">0</span> <span class="keyword">then</span> <span class="keyword">return</span> <span class="number">-1</span> <span class="keyword">end</span>  <span class="comment">-- 库存不足</span></span><br><span class="line">redis.call(<span class="string">&#x27;DECR&#x27;</span>, KEYS[<span class="number">1</span>])</span><br><span class="line"><span class="keyword">return</span> <span class="number">1</span>  <span class="comment">-- 扣减成功</span></span><br></pre></td></tr></table></figure><p>Redis 单线程执行 Lua 脚本，原子性判断库存并扣减，99% 的无效请求在 Redis 层被过滤。</p><p><strong>第四层：Kafka 异步下单（削峰）</strong>：Redis 扣减成功后，立即返回”下单受理中”给用户，并将下单请求投入 Kafka。后台消费服务从 Kafka 消费，进行数据库库存扣减（带 <code>WHERE stock &gt; 0</code> 乐观锁兜底）、创建订单记录、触发支付流程。</p><p><strong>第五层：结果异步通知</strong>：消费服务处理完后，通过 WebSocket 或轮询接口将最终下单结果（成功&#x2F;失败）推送给用户。”</p></blockquote><hr><h3 id="场景题-5：微服务接口幂等性如何保证？"><a href="#场景题-5：微服务接口幂等性如何保证？" class="headerlink" title="场景题 5：微服务接口幂等性如何保证？"></a>场景题 5：微服务接口幂等性如何保证？</h3><p><strong>回答思路</strong>：</p><blockquote><p>“幂等性是指同一个请求执行多次和执行一次的结果完全相同，是分布式系统中防止重复请求（网络重试、消息重消费）的关键。主要方案：</p><p><strong>方案一：唯一主键&#x2F;唯一索引（最简单可靠）</strong>：在数据库表中对业务唯一标识（如 <code>order_no</code>、<code>trade_id</code>）建唯一索引，重复请求插入时数据库抛出唯一键冲突异常，业务代码捕获该异常并返回成功（幂等处理），适合插入类操作。</p><p><strong>方案二：Token 令牌机制</strong>：客户端在真正操作前先请求服务端获取一个唯一 Token（存入 Redis），提交请求时携带该 Token，服务端用 <code>DEL token_key</code> 删除 Token（原子操作），删除成功才执行业务，删除失败（Token 已不存在）则判定为重复请求，直接返回成功。适合表单防重复提交。</p><p><strong>方案三：状态机检查</strong>：根据业务状态流转规则判断幂等性。如订单只能从”待支付”流转到”已支付”，重复的”支付”请求检查订单已是”已支付”状态，直接幂等返回，不做重复处理。</p><p><strong>方案四：Redis SETNX 防重（适合 Kafka 消费幂等）</strong>：消费消息前执行 <code>SET msg_id 1 NX EX 7d</code>，成功则处理，失败则说明已处理过，直接 commit offset 跳过。</p><p>在我们的 SRM 采购系统中，询价回调接口（供应商回调报价结果）采用了唯一索引 + Token 令牌双重保险，确保即使供应商系统超时重试也不会重复处理报价数据。”</p></blockquote><hr><h3 id="场景题-6：如何设计支持亿级数据的定时任务调度系统？"><a href="#场景题-6：如何设计支持亿级数据的定时任务调度系统？" class="headerlink" title="场景题 6：如何设计支持亿级数据的定时任务调度系统？"></a>场景题 6：如何设计支持亿级数据的定时任务调度系统？</h3><p><strong>回答思路</strong>：</p><blockquote><p>“亿级数据的定时任务，核心挑战是单台机器无法在时间窗口内处理完，必须分布式并行处理。</p><p><strong>核心设计：XXL-Job 分片任务 + 分库分表</strong>：</p><ol><li><strong>XXL-Job 分片广播</strong>：使用 XXL-Job 的”分片广播”任务类型，有多少个执行器实例，任务就分成多少个分片。每个执行器通过 <code>ShardingContext</code> 获取自己的 <code>shardIndex</code>（当前分片序号）和 <code>shardTotal</code>（总分片数），然后只处理 <code>id % shardTotal == shardIndex</code> 的数据，实现数据的均匀拆分并行处理。</li><li><strong>分页游标处理</strong>：每个分片内，使用游标分页（<code>WHERE id &gt; lastId AND id % shardTotal = shardIndex LIMIT 1000</code>）分批处理数据，避免一次加载大量数据到内存导致 OOM。</li><li><strong>分库分表支持</strong>：数据按 ID 分片后，可以分散在不同的库&#x2F;表中，分片任务对应访问各自负责的库&#x2F;表，避免跨库查询。</li><li><strong>任务幂等性</strong>：每条记录处理前，通过 Redis SETNX 加任务锁（<code>task:{taskName}:{recordId}</code>），防止多个执行器实例（负载均衡漂移时）重复处理同一条记录。</li><li><strong>任务进度持久化</strong>：每个分片定期将当前处理的游标写入 Redis，任务失败重启后从断点继续，而不是从头开始。”</li></ol></blockquote>]]></content>
      
      
      <categories>
          
          <category> 面试复习 </category>
          
      </categories>
      
      
        <tags>
            
            <tag> 面试 </tag>
            
            <tag> 项目实战 </tag>
            
            <tag> 场景题 </tag>
            
        </tags>
      
    </entry>
    
    
  
  
</search>
